# autoqa task tracker

Persistent, cross-session task list for the autoqa project. **Read this first, before CLAUDE.md's prose**, at the start of any session — Claude Code's built-in TaskCreate/TaskList tool is scoped to one session and invisible across jobs, so anything tracked only there is lost the moment that session ends or crashes. This file is the single source of truth for open work; update it directly (check items off, add new ones, don't let CLAUDE.md's prose accumulate more ad-hoc "task #N" mentions — link back here instead).

IDs here are unique by construction. CLAUDE.md's own historical numbering is **not** reliable — #14 and #16 were each independently reused for two unrelated things by different sessions before this file existed. Where a task below descends from one of those prose mentions, the old number is noted for continuity, but the ID in this file is canonical.

Full narrative/evidence for anything here lives in CLAUDE.md (search by date or task ID) — this file is deliberately terse; don't duplicate the write-up, just enough to act on and a pointer.

---

## Current status — priority order

Status key: unmarked heading = open; 🟡 = partially fixed; 👀 = watch-only; ✅ = completed.

### 🟡 HIGH — `#modal-obstruction-recovery-and-broad-in-app-reporting` — close blocking overlays and report every primary issue

- A scene-detail modal remained open after its normal Close/X click had no observable effect, so the later Create Video control was unreachable and the remaining journey was skipped. Add a site-agnostic fallback scoped strictly to the active visible dialog: explicit Close/Cancel/Dismiss/X first, then one unique top-right icon. If a background control is blocked, close the modal and retry from the changed state; never close a modal after a failed Save/Create inside it.
- Bug reporting is too conservative. Report every primary FAIL/NEEDS REVIEW and any passing step with concrete fresh console/page/network/visible error evidence, while excluding synthetic downstream “skipped because upstream” duplicates. Keep run-level success/blocker policy separate so a broadly reported diagnostic does not make an otherwise useful terminal run red.
- Every in-app report must contain concise Console/Exception and Network sections, a short issue/cause/impact summary, and a final `Where:` line naming the page and workflow. Persist `in-app-bug-reporting.json` even when zero reports are found or reporting is unavailable.
- Validate with focused unit tests, the full unit/build gate, and directed live Koyal tests that open/close a scene modal, retry a previously blocked control, and submit a real encountered issue through Report a Bug. Do not commit or push without explicit user permission.

Code/local-live validated 2026-08-03: the real Koyal Scene 1 card required AutoQA's lost-click DOM activation fallback, then the real non-semantic fixed overlay was closed by the new verified modal recovery (`modalBefore:true`, `dismissed:true`, `modalAfter:false`, Edit Scenes still usable). A real scene error (`prompts data is not generated please try again later` plus `Cannot destructure property 'narrative' ... as it is null`) was submitted through the in-app Report-a-Bug UI with Console/Exceptions, Network, impact, and final Where line; result 1/1 submitted with no reporting failure. Full gate: 338/338 tests, clean TypeScript build, clean diff check. Hosted GitHub VM execution remains pending because the user explicitly prohibited commit/push without permission; uncommitted code cannot run in Actions.

### ✅ HIGH — `#functional-run-exit-and-hosted-bug-reporting` — terminal success exits cleanly; only genuine blockers fail

- Raw milestone assertion disagreements remain fully visible in reports, but no longer force a production command to exit 1 by themselves.
- A run exits successfully when it verifies its requested terminal artifact, or when it finishes without a concrete product/infrastructure/forward-progress blocker. A real product error, unrecovered browser/auth/processing failure, disabled forward path, or upstream break that skips the remaining journey still fails when no terminal artifact was verified.
- Terminal proof is grounded only in observed URL/result/page evidence, never the repeated manual prompt, and a persisted intermediate Character/Asset/Outfit cannot masquerade as a final artifact.
- Koyal in-app reporting now opens the icon-only fixed Report-a-Bug launcher deterministically, verifies the actual form before filling, submits once, and retains bounded visual/LLM fallback. Reporting never repeats the failed product operation or changes its verdict.
- Validated against the exact hosted reports: Audio 22/9 becomes functional SUCCESS because its final playable/downloadable video was independently verified; Script 5/27 remains functional FAILURE because it never reached a terminal artifact and its blocked position caused 25 downstream skips.
- Live Koyal verification passed: authenticated, opened the real modal, filled a clearly labeled pipeline-test report, submitted once, and observed the success confirmation. Full gate: 330/330 unit tests, TypeScript build, and diff check pass.

### 🟡 HIGH — `#stale-position-recipe-directed-remap` — stale recovery recipes must self-heal instead of skipping the remaining flow

- Native macOS hosted Audio validation reached a playable final video without a daemon wedge, but a later position rebuild failed on the stale combined control label `EditRevertAnimated` and recorded 20 downstream milestones as skipped.
- Position recovery must retain the deterministic prefix that still works, invoke narrowly directed LLM exploration only for the stale suffix, confirm the next milestone's mapped guard state, and persist a complete entry-to-target recovery recipe for the following replay-validation run.
- Validate with focused unit tests, the full unit/build gate, then sequential hosted VM runs: exploratory remapping first and deterministic replay validation second.

### 🟡 HIGH — `#supervisor-hindrance-cleanup-confirmation` — character/asset cleanup approval must survive site confirmation — CODE + UNIT VALIDATED 2026-07-31

- The production supervisor approved a browser-grounded `Remove character` action for an empty slot blocking Next, but independently denied the site's immediately following generic `Remove` confirmation. The denial became user authority, so Explorer correctly refused to retry it and the Audio journey could not self-heal past the disabled forward control.
- The supervisor now authorizes character/asset deletion only when current browser evidence identifies a concrete workflow hindrance (empty, duplicate, stale, corrupt, wrong, blocking, or preventing progress), carries that narrow authority through the immediate generic confirmation, and still denies unrelated project/account/payment deletion or removal of a required finalized entity.
- Manual v2 task prompts now explicitly require character creation/upload to originate in the owning Character section/slot and reusable asset creation to originate in the standalone Asset section/library. A migrated character display counts only with same-run provenance from the Character section.
- Focused policy/prompt tests and the complete 309/309 unit suite pass; TypeScript build and diff check are clean. A fresh live Audio validation remains before marking this complete.

### 🟡 HIGH — `#manual-operational-edit-contract` — generic edits need concrete input but error-based verdicts — FALSE-NEGATIVE FIX + UNIT VALIDATED 2026-07-31

- Manual v1/v2 previously accepted vague instructions such as “make a realistic edit,” which gave the agent no stable input contract and made visual/persistence judgment subjective.
- Every edit task now preserves an exact user value or chooses one simple concrete delta supported by the visible control. It must record the exact text/option/color/object/lighting/framing/camera request, submit once, and wait for processing to settle.
- The verdict intentionally tests operational health rather than artistic compliance or persistence: changed appearance/text is not required. The edit passes when it genuinely submits, processing settles, the surface remains usable, and the fresh milestone capture contains no page exception, console error, unexpected 5xx/backend failure, or visible/modal application error. A no-op click, active spinner, timeout, broken result, or concrete error fails.
- Koyal/Chrome can return cumulative errors even after the signal channel is cleared. Operational audits now compare the before/after signal bundles as multisets, ignore only narrowly identified WaveSurfer/PostHog teardown noise after a proven input+submit action, and can accept that action when Explorer later loops on `done`. Newly appearing product exceptions, visible/modal errors, and 5xx responses still fail. Focused boundary tests plus the complete 314/314 suite and TypeScript build pass.
- The prompt contract is injected into both newly compiled and previously saved manual flows without duplicate text. Manual contract version 28 prevents unsafe reuse of older learned manual recipes. Focused tests and the complete 309/309 unit suite pass; TypeScript build and diff check are clean. Live Audio validation remains.

### ✅ HIGH — `#in-app-product-bug-reporting` — report verified bugs through Koyal, not Slack — LIVE VALIDATED 2026-07-31

- Stop sending AutoQA product-bug reports to Slack.
- When AutoQA verifies a product-side bug, open the mapped in-app **Report a Bug** modal and submit it there.
- The report must explicitly say it was filed by **AutoQA**, state the concrete observed error, give AutoQA's concise likely cause, and explain exactly what workflow or downstream capability the bug blocks.
- Attach/link the strongest available page, console, network, modal, and correlated backend evidence that the in-app form supports; never include secrets or unrelated reproduction noise.
- A reporting failure must not change the QA verdict or cause the tested product action to be repeated.
- Validate with the final-video edit failure (`video is not edited please try again later!!`) and a synthetic verified product-error case.

The Koyal reporter now runs after verdict finalization, opens only the in-app Report-a-Bug modal, fills an exact redacted AutoQA description deterministically, submits once, polls for the asynchronous confirmation, and stores the submitted text in `in-app-bug-reporting.json`. Manual audits softened to NEEDS REVIEW remain reportable when concrete visible/network product evidence exists; unrelated historical SPA console/page errors are filtered out. Live Audio validation authenticated, opened the real `/finalvideo` modal, submitted the `video is not edited please try again later` bug, saw confirmation, and recorded the exact description. Reporting failure was separately live-proven safe. Unit/build gate is part of the current 283/283 suite.

Hosted icon-click regression fixed 2026-08-02: when a trusted click on Koyal's unlabeled floating bug icon reports success but never reaches the live page, the reporter now identifies the unique fixed bug launcher in the DOM, activates it in-page, and requires the Report-a-Bug heading, description field, and Submit Report control before continuing. A clearly labeled live pipeline-test submission passed end to end.

### ✅ HIGH — `#production-prompt-supervisor-llm` — unattended, realistic answers for production runs — LIVE VALIDATED 2026-07-31

- Add a top-level production supervisor LLM that consumes AutoQA's structured question context and answers field values, upload requests, classifications, guard questions, and flow approvals without a person present.
- Answers must be realistic, semantically appropriate to the field, unique when the site requires uniqueness, and consistent across producer/consumer tasks; it must not invent credentials, secrets, or unavailable files.
- Keep a human override/inbox path, explicit safety boundaries for destructive actions, bounded retries, and an auditable decision log.
- Run it as the production companion for scheduled morning/evening QA so AutoQA remains unattended without weakening deterministic replay.
- Validate duplicate-name recovery, mismatched suggestions (for example a car suggested for a location), file-type selection, verified product-error classification, and safe refusal of unrelated destructive actions.

The opt-in supervisor preserves the human inbox override window, refuses secrets/unallowlisted destructive actions/nonexistent files, chooses upload files by semantic media type, rotates realistic constrained names after rejection, and logs its decision source. The Audio live run proved duplicate-name recovery, PNG-vs-audio selection, car-vs-location correction, character/look/theme/transcript values, safe destructive refusal, and product-error classification while reaching a new playable/downloadable final video without manual steering. Narrow empty-placeholder cleanup requires browser-agent evidence. Unit/build gate is part of the current 283/283 suite.

### 🟡 HIGH — `#script-audio-replay-validation-2026-07-30` — finish both long manual contracts and replay them

Execution order requested by the user:

- [x] Run the complete Script contract: 24 PASS / 1 FAIL / 5 NEEDS REVIEW; a new 9-second final video was rendered, played, and exposed Download/Export controls.
- [x] Run the complete Audio contract and verify its terminal artifact: 26 PASS / 1 FAIL / 3 NEEDS REVIEW; a playable/downloadable 2-second final video was verified.
- [x] Run Script replay validation: 20 PASS / 1 FAIL / 9 NEEDS REVIEW; base 10-second terminal video passed, Koyal final-video edit error blocked post-render functions, and the flow was demoted.
- [x] Run Audio replay validation: 17 PASS / 1 FAIL / 12 NEEDS REVIEW; a multi-hour user-requested pause expired auth, recovery lost project context (`projectId is not allowed to be empty`), terminal positioning failed, and the flow was demoted. This is not a clean uninterrupted replay judgment.
- [x] Rerun Audio replay uninterrupted: 22 PASS / 1 FAIL / 7 NEEDS REVIEW; a fresh playable/downloadable terminal video was verified. Four post-render tasks were blocked by the reproducible Koyal error `video is not edited please try again later!!`; excluding those product-blocked tasks yields 22/26 = 84.6% automation coverage, so the Audio flow was restored to replay-validation pending the product-failure lifecycle fix.
- [x] Validate the product-failure lifecycle with the production supervisor: 20 PASS / 1 FAIL / 9 NEEDS REVIEW, 184 LLM calls; a new Audio final video rendered, played, and exposed Download/Export. The flow remained `replay-validation` instead of being demoted because concrete product-blocked checks are excluded from recipe-health qualification, while automation gaps remain visible and prevent deterministic promotion.
- [x] Report both learning/replay results, all remaining failed or needs-review milestones, lifecycle state, LLM usage, and whether either flow was demoted.
- [ ] Commit and push only if both replay validations succeed and remain qualified; otherwise ask the user before committing or pushing.

Script items carried into replay: three-character post-dialog visual proof; exact producer→consumer identity for the scene asset (items 7 and 15); Reshoot through Scene Edit → Change Scene; Add Reference post-processing/playability proof; aggregate final proof. The terminal video itself was independently verified.

### ✅ HIGH — `#replay-validation-85-percent-gate` — strong terminal runs no longer require perfect first-pass evidence — UPDATED TO 80% PASS + UNIT VALIDATED 2026-07-30

Learning flows now enter replay validation when **at least 80% of milestones actually PASS** and a new terminal artifact is independently verified. NEEDS REVIEW no longer inflates the percentage. Missing, failed, or uncertain recipes remain visible and are repaired during replay validation instead of keeping an otherwise successful end-to-end run in learning forever. Deterministic promotion remains strict: every milestone must pass through replay, every recipe must exist, and terminal evidence must be verified. Manual task-graph flows may use an earlier explicit final-video proof even when the last aggregate audit fails because of unrelated incomplete checks. The completed Script v2 run qualified at exactly 24/30 PASS (80%) with a playable/downloadable video and moved to replay-validation. Gate: 264/264 unit tests, focused boundary tests, TypeScript build, and diff check pass.

### ✅ HIGH — `#manual-shared-safety-invariants-v27` — page similarity, ambiguous icons, and replay recovery could violate a manual request — FIXED + UNIT VALIDATED 2026-07-29

The 8-run v1/v2 matrix exposed four shared manual-mode gaps. Location creation targeted the similarly named library although the create control exists in a mapped wizard; an unlabeled Report-a-Bug button was repeatedly guessed as Add Location; a completed delete-10 task ran a generic back/forward probe and attempted destructive replay; and Anthropic 529 overloads were mixed into ordinary task failure/retry. Contract v27 centralizes the fix for both engines without changing crawler/deep-walk/non-manual behavior: capability semantics outrank page-name similarity, stateful targets inherit the shortest mapped journey prefix that previously reached them, explicit create→delete lifecycle clauses split across builder/library states in v2, every focused manual flow carries `manualContract` and therefore skips generic probes, read-only proofs mechanically reject mutation actions, destructive tasks never cache replay recipes, same-state click attempts are bounded, and unlabeled manual clicks require explicit screenshot grounding rather than positional guesses. Provider overload/network failures become infrastructure-blocked needs-review and do not trigger another exploratory attempt. A focused live v23 run then proved one remaining route defect: a mutually exclusive Concept-Driven choice skipped later mapped Character Style and Locations states. v24 carried the remaining mapped state sequence into every sanitized checkpoint and live-proved the corrected Character-Driven route, labeled creation, one-shot processing, and persisted edit. Its final proof then caught that the contextual deletion clause had been compiled as policy instead of work. v25 kept explicit delete/remove clauses executable and linked “same/newly created” entities to their producer task. A focused cleanup then found read-only proof wrongly demanded a page transition; v26 allowed current-state `done` only for manual verify milestones while keeping their mutation block active. The first comprehensive Script v2 run then proved a destination-only journey checkpoint could overshoot a state already reached by its owning task; v27 zero-actions that checkpoint only when its exact mapped destination is current, leaving the normal deterministic/vision verification active. Gate: focused and full suites plus build must be rerun; final Script/Audio validation remains.

### ✅ HIGH — `#manual-focused-journey-boundary-v21` — focused requests inherited unrelated full wizards and opposite-action recipes — SUPERSEDED BY v27; FULL UNIT VALIDATED 2026-07-29

A focused uploaded-character test matched the complete Audio-to-video walk because shared “upload/file/character” terms outweighed the requested feature boundary. It created and finalized the requested character but continued through Theme, Style, Locations, scene generation, and rendering. Manual contract v21 now keeps a mapped prefix only through the first requested feature checkpoint for non-end-to-end requests, schedules the independent acceptance audits there, and verifies the focused persisted result instead of demanding a terminal video. Both legacy v1 and task-graph v2 use the same bounded source journey. Flow matching also requires explicit requested capabilities (upload, delete/remove, render) and will not reuse a create recipe for deletion merely because both target Characters. Destructive “Go to Projects” requests prefer the primary Projects surface over qualified alternatives such as Collaborated Projects. Ordinary crawler, deep-walk, and non-manual flow execution are unchanged. Full gate: 241/241 tests, TypeScript build, and diff check pass.

### ✅ HIGH — `#slack-product-bug-format-v2` — Slack reports now correlate concise front-end and backend evidence — FIXED + UNIT VALIDATED 2026-07-29

Product-bug Slack messages now contain Platform, the linked page where the bug appeared, the concrete front-end error log, the closest matching backend record fetched from the configured Koyal admin error-log endpoint, and exactly three short What/Why/Impact lines. Inputs and reproduction dumps were removed. Admin credentials and secret-looking backend fields are redacted and never posted; backend lookup failure cannot fail the QA run. Configure `KOYAL_ADMIN_TOKEN` (and optionally `KOYAL_ADMIN_ERROR_LOGS_URL`) at runtime. The mocked integration test verifies endpoint authentication, matching, formatting, redaction, and webhook delivery.

### 🟡 HIGH — `#manual-artifact-scope-and-mutation-identity` — Manual v1/v2 conflated distinct Add controls and cross-section uploads — CHARACTER BLOCKER LIVE-FIXED; TERMINAL REVALIDATION BLOCKED BY CREDITS

Two fresh long Audio contracts stopped on Story Type with only one finalized AI avatar. The manual explorer chose a visually separate assets/animals uploader as the uploaded-character method because text-only accessibility refs lost section ownership, then treated `Add Character`, `Add Assets`, and the existing-library `Add (1)` confirmation as the same broad `add` mutation. A later Save/upload processing barrier was incorrectly attributed to the earlier Add Character action, so the first real `Add (1)` was suppressed as already executed. Manual-only hardening now attaches the screenshot to every decision, uses exact mutation labels within a manual goal, refuses to retain a mutation click as evidence/recipe when it produces no observable state change, and requires the v2 character audit to return from any picker/modal and visibly verify the owning character surface. Live v2 proof passed the former blocker: AI avatar finalized, image uploaded/saved, existing-library character selected/confirmed, and the wizard advanced through Theme, Style, Locations, Assets, and all scene functions to Final Video. Koyal then returned `Insufficient credits to process this task`, so a new playable terminal artifact could not be produced.

That proof run exposed three further manual-only contract gaps, now code/unit fixed in contract v20: selecting a built-in sample cannot satisfy “upload the file I provide”; producer/consumer tasks carry stable artifact identity and require the same library name or exact upload path; and a journey checkpoint overlapping a task-owned one-shot mutation (observed: Create Video) becomes destination-only verification rather than clicking twice. Normal crawl, deep-walk, replay, and non-manual Explorer behavior remain unchanged. 237/237 tests, TypeScript build, and diff check pass; fresh v20 v2/v1 live validation remains.

### 🟡 HIGH — `#manual-detailed-contract-enforcement` — mapped checkpoints skipped explicit sub-actions — TASK-GRAPH ENGINE BUILT, LIVE REVALIDATION NEXT 2026-07-28

A comprehensive beta.koyal.ai Script `--manual` run rendered and verified a new playable final video, but reaching each mapped wizard checkpoint allowed the explorer to omit voice/emotion/dialogue edits, reusable asset creation, location CRUD, scene-edit functions, and post-render functions. It also honored the saved `/upload` entry instead of the explicitly requested Dashboard → New Project path. The legacy acceptance-contract patches proved individual ideas but remained expensive and brittle (295 calls / 7.66M input tokens in one run). A new opt-in `--manual-v2` engine now compiles the request once into a dependency-aware task graph: mapped journey order controls page sequencing, only real producer→consumer relationships become dependencies, policy clauses are constraints rather than actions, the LLM sees only the active task, fresh-entry proof is positioned immediately after the verified entry action, mutations are not repeated by navigation checkpoints, and pre-render/render/post-render tasks have explicit boundaries. It preserves successful action/recipe evidence, prevents task-skipping fast-forward while retaining legitimate Create Video→Final Video fast-forward, and makes unresolved v2 audits fail final proof.

The first live stress run exposed structural gaps, now fixed in the next contract version: an evidence-only task may finish only when the requested state is visibly proven, character creation must use the control inside the character slot rather than the separate assets/animals uploader, primary wizard states may not be opened directly until the current run has naturally visited them, and every journey transition must prove it reached its mapped destination. A missed destination gets one bounded forward recovery and then fails/halts downstream execution rather than letting a KB or human ambiguity decision erase it. End-to-end creation flows now begin with a dedicated fresh-entry checkpoint from the best mapped authenticated landing page instead of direct-opening a stale wizard entry; the original first journey action remains a separate milestone. Three-character acceptance requires one finalized AI avatar, a real image upload (a prior PDF/audio upload cannot count), one existing-library character that was actually committed, no empty extra slot, and an enabled forward control. If an explorer says `done` but the independent audit still finds one missing sub-action, manual-v2 gets one bounded repair attempt for that exact gap while completed mutation labels remain blocked. Raw application contract errors such as `Missing storyInstructions, storyElement for edit-story` now stop the active task as a product failure instead of being retried or locally filled over. Final proof deterministically aggregates all individual task verdicts and verifies only the terminal artifact/global constraints from the final screen, so early evidence cannot disappear from the LLM context.

The first old-process live stress run finished in 29 minutes with 6 PASS / 1 FAIL / 16 NEEDS REVIEW. It reached Edit Scenes, where the real Koyal error `Failed to fetch JSON from S3` / an HTML response parsed as JSON blocked scene work and rendering; no final video was produced, and downstream tasks were correctly skipped rather than falsely passed. The real Beta sitemap dry-plan orders upload → three characters → dialogue edits → theme → outfit/style → location → reusable asset → scene edits/add asset → one render → final tools. 223/223 tests, TypeScript build, and `git diff --check` pass; one clean v16 Beta run and replay validation remain.

### RUN POLICY — cap beta/XP/FilmArena exhaustive runs at 10 deep walks

Every future exhaustive run for `beta.koyal.ai`, `xp.koyal.ai`, or `filmarena.ai` must pass `--deep-flows 10`. Never run these sites with an uncapped dynamic deep-walk queue. This preserves broad coverage while preventing persistent wizard controls from expanding into an excessive number of equivalent walks.

### ✅ HIGH — `#deep-walk-alias-dedup` — persistent wizard navigation generated duplicate Audio walks/flows — FIXED + PRESERVED-STATE VALIDATED 2026-07-27

The dynamic queue treated repeated wizard controls such as “Upload file” and “Go back to upload audio” as new creation entries from every source state. It therefore repeated the same Audio branch and proposed multiple equivalent flows. Persistent wizard navigation is now retained in pages/edges but excluded as a separate deep-walk entry; equivalent terminal trails are fingerprinted by route plus meaningful workflow choices, preserving all walk evidence while consolidating only proposed alias flows. Focused tests preserve distinct Script/Audio modes and same-page Add/Regenerate mutations. Preserved beta validation consolidated 9 proposals to 6 while retaining all 16 pages, 41 edges, and 13 walk records with no dangling flow references.

### ✅ ENHANCEMENT — `#manual-sitemap-test-mode` — natural-language focused testing from an existing map — FIXED + LIVE LEARN/REPLAY VALIDATED 2026-07-27

`run --manual "<request>"` now selects the narrowest real page or exactly matching flow from the saved sitemap, rejects invented IDs, intentionally enters the mapped feature, runs only a bounded feature-scoped test, verifies visible/persistent completion, and stores the action recipe for later replay validation. It asks through the normal human channels for realistic values/files and cannot be combined with `--wipeout`. Full suite: 174/174 + clean build. Live beta proof: a Locations-library `Airport` search learned with 1 PASS / 0 FAIL / 0 NEEDS REVIEW, then the identical request replayed its two-step recipe with zero Explorer fallback and promoted deterministic.

### 👀 `#koyal-locations-list-edit-noop` — visible mapped Edit button produced no state change

The first live manual-mode validation targeted the saved Locations library and clicked the visible `Edit Modern Home Office` control repeatedly, then its owner card. The UI remained on the list; no page error, console error, or backend failure was captured. AutoQA correctly refused to claim success and left the flow exploratory/needs-review. This is a Koyal product-or-click-delivery ambiguity, not evidence that manual targeting failed; keep as watch-only until reproduced manually or with concrete runtime evidence.

### 🟡 HIGH — `#exploratory-post-verification-continuation` — unfinished verified state did not call the LLM again — CODE + UNIT FIXED, FIRST CURRENT-FLOW LIVE VALIDATION COMPLETE 2026-07-24; FOCUSED REVALIDATION PENDING

Flow execution previously chose replay/LLM fallback before verification. If the browser actions reported success but the later DOM/screenshot verification showed the milestone was still unfinished, AutoQA finalized NEEDS REVIEW/FAIL without another LLM call. Exploratory goals were also overly local, so an omitted prerequisite could block the whole end-to-end purpose. Every exploratory milestone now receives the full directed flow mission plus remaining checkpoints; its wording is a guide rather than a hard wall, and it may complete visible safe prerequisites while staying in scope. After any replay or exploratory attempt, an objectively unfinished verification result without a blocking product error triggers one bounded LLM continuation from the exact current state, followed by full deterministic and vision re-verification. The first no-wipe current-flow live run proved the core behavior: Audio solved the omitted “Minimum 1 character required” prerequisite and reached a playable `/finalvideo` artifact with Download and Premiere export controls; Script recovered an upload/input miss, selected Concept Driven, generated/confirmed background music, and reached Theme; Asset and Outfit creations persisted. That run also exposed three narrowing needs now code-fixed: the full mission is context while the current checkpoint remains the recipe boundary, verify milestones may not start new mutations, and successful mutation labels carry into continuation as a hard denylist. Generic visual uncertainty from a disappeared completed form no longer demotes/re-explores a healthy replay, while objective unfinished evidence still does. Run the focused revalidation after the updated unit/build gate.

### ✅ HIGH — `#state-cycle-safe-forward-recovery` — loop guard exited beside an enabled Next button — FIXED + UNIT-VALIDATED 2026-07-24

Fresh beta.koyal.ai Script exploration reached Edit Script and successfully exercised dialogue regeneration, but then repeated sidebar/unlabelled actions until the state-cycle guard fired even though one enabled `Next` button was visible. The guard's visual arbitration merely told the LLM it *may* choose a safe forward click, without grounding the exact control strongly enough. The guard now forbids repetitive/unsafe actions and explicitly identifies the one unique enabled `Next`/`Continue`/`Proceed` control for the LLM to choose. The executor still acts only on the LLM's decision—there is no deterministic click fallback. Disabled, mutating, modal, sidebar and ambiguous forward candidates remain untouched. Regression coverage proves the arbitration prompt selects and advances through a Script-like `Next`.

### 🟡 HIGH — `#walked-video-rebuild-2026-07-24` — old partial Koyal walk omits finalization/render actions; CODE + UNIT FIXED, FRESH WIPEOUT PENDING

The saved `walked-projects-list-create-yournext-video` flow was compiled before terminal processing steps were preserved. Its old seven-milestone recipe ends at Locations even though the underlying walk reached Edit Scenes, Create Video, rendering, and a playable/downloadable video. It also describes Character Driven as “fill the description” without requiring the generated character to be finalized. Generic compiler fix: processing-classified walk steps remain actionable when they contain non-wait actions, and every terminal walk gets an explicit zero-action terminal-artifact verification milestone. A fresh wipeout must rebuild the flow and prove its full recipe replay; the stale flow cannot be promoted.

### ✅ HIGH — `#forward-control-settle-and-disabled-bypass` — Next click landed but AutoQA escaped through wizard sidebar — FIXED + FOCUSED LIVE-VALIDATED 2026-07-24

Three focused beta.koyal.ai replays isolated the upload failure. The changing-credit Standard plan radio is now grounded through its DOM accessible name and volatile numeric labels replay safely (`Standard519` vs prior `Standard553`). Koyal then accepted the Next click asynchronously; AutoQA previously asked the LLM again too early, and it clicked the Story Type progress/sidebar label, which redirected to Dashboard. Generic fix: never execute visibly disabled refs, never use wizard breadcrumbs/sidebar labels to bypass disabled forward controls, poll a real Next/Proceed click for the delayed SPA route, and re-check one-screen boundaries at every loop start. Focused live proof: the same m2 selected Standard, clicked Next, reached `/selectStoryType`, passed its milestone, and all four Story Type probes ran; 161/161 unit tests and TypeScript build pass.

### 🟡 HIGH — `#dynamic-deep-walk-branch-queue` — newly discovered Script/Audio fork choices were deferred to a later crawl — CODE FIXED, WIPEOUT PENDING

The crawler previously snapshotted deep-walk entries once before any walk. A video walk could discover `/upload` plus Script/Audio choices, but those new branch entries were invisible until a later exploration, so an exhaustive run could finish without walking Audio. The queue now rescans sitemap entries after every walk, shares attempted-choice memory, and continues until the explicit cap or no unattempted non-terminal entry remains. Explicit CLI/environment deep-walk caps still win; uncapped exhaustive mode explores every discovered entry. Unit tests and build pass; full Koyal wipeout must prove both branches are queued in one cold run.

### 🟡 HIGH — `#deterministic-creation-replay-state-loss` — creation recipes lost transition/final-artifact proof — CHARACTER + ASSET + OUTFIT + REGENERATE LIVE-VALIDATED 2026-07-23; FULL VIDEO STILL PENDING

Fresh beta.koyal.ai wipeout originally proved Character, Asset, and Outfit in exploration but could not promote their recipes. Generic fixes now cover every root cause found: processing barriers survive compilation and use the current 20-minute policy (including old saved recipes); deterministic fills use real browser input events instead of DOM-only values that React can display without accepting; mid-processing fallback cannot overwrite a complete recipe with a suffix; run-specific artifact identity is passed to the dedicated persistence oracle; real report IDs (`flow-id:m2`) normalize correctly for lifecycle qualification; vision-only uncertainty retains replay-validation; strict “one screen” boundaries stop an entry milestone from consuming the next form; and field preflight neither selects the wrong fill nor asks twice. **Live proof:** Character, Asset, Outfit and contextual Asset Regenerate have each learned, replayed with zero Explorer fallback, and promoted deterministic; Asset and Outfit also replayed again from deterministic state with fresh values. Keep 🟡 only until a full video reaches terminal render and its complete recipe replay receives the same proof.

### ✅ HIGH — `#sigint-browser-session-leak` — interrupted runs leak their Chrome daemon/profile — FIXED + LIVE-VALIDATED 2026-07-22

Nine interrupted beta.koyal.ai exhaustive runs left nine exact `autoqa-beta-exhaustive-postfix-*` agent-browser sessions alive. Their Chrome processes/profiles eventually reduced free disk space to 150 MB, causing a later otherwise-clean walk to fail while atomically saving `sitemap.json` with `ENOSPC`; closing only those nine named sessions restored 5.8 GB. Root cause: command-level `finally` cleanup works on normal completion, but `src/cli.ts` had no SIGINT/SIGTERM handler and immediately exited, bypassing teardown. A first uniquely named smoke run proved an additional wrapper edge: Ctrl-C through `npm`/`tsx` can terminate the foreground process group before Node receives its signal callback. Fixed generically with two layers: every session created by this process is registered for ordinary/signal teardown, and a detached exact-session watchdog closes only that session when its owner PID disappears—even after SIGKILL or wrapper-level termination. Neither layer broad-kills other AutoQA runs. Live validation: interrupted uniquely named `npm run qa` smoke session `autoqa-signal-watchdog-smoke-example.com`; after two seconds `agent-browser session list --json` returned an empty list. All 97 unit tests and TypeScript build pass.

### ✅ HIGH — `#expensive-async-resubmission` — failed persistence verification can relaunch the same multi-minute job — FIXED + LIVE-VALIDATED 2026-07-23

Live beta.koyal.ai exhaustive run 2026-07-22: `Change Location` generated for many minutes, then the requested description was no longer present. Explorer refilled the text and was about to submit `Change Location` again. Fixed generically: once a successful click is followed by a deterministic processing barrier, that exact control is one-shot for the remainder of the goal; ordinary Save remains repeatable after a genuinely new edit. Live Asset and Outfit learning/replay cycles on 2026-07-23 each submitted Generate/Try/Finalize exactly once and polled through multi-minute work, including timeout/fallback cases, with no duplicate artifact mutation.

### ✅ HIGH — `#post-processing-transient-mutation` — spinner-free transitional frame can trigger the wrong next mutation — FIXED + LIVE-VALIDATED 2026-07-23

Live beta.koyal.ai exhaustive run 2026-07-22: Rosalie's avatar generation spinner disappeared one render before `Review and finalize` mounted. AutoQA immediately saw the old `Add Character` control, clicked it, created an empty Character 2 slot, then could not advance despite Rosalie being complete. Vision correctly diagnosed the empty slot and refused terminal success, but the video walk was already contaminated. Fixed generically: after deterministic processing clears (or vision affirms async completion), AutoQA holds one short render-stabilization turn, re-snapshots, and prioritizes one unique enabled post-processing finalizer before permitting Add/New/Create recovery. Live audio-flow validation finalized Dorian and the focused deep walk finalized Lucian without creating an empty second slot.

### ✅ HIGH — `#validation-field-misattribution` — a later field's constraint invalidates an earlier saved value — FIXED + LIVE-VALIDATED 2026-07-23

Koyal keeps the accepted description textarea visible when the later Asset Name field introduces a letters/spaces constraint. Recovery now attributes validation to the distinct unfilled constrained field in both accessibility and screenshot paths. A single boolean was also replaced by a bounded set of distinct validation states, so an early format constraint cannot consume recovery for a later duplicate-name rejection. Live revalidation across repeated Asset creations advanced directly from the accepted description to a separate human name question every time; the description was never invalidated or re-asked as rejected.

### ✅ HIGH — `#duplicate-mutating-entry-fallback` — fallback entry exploration can submit Regenerate repeatedly — FIXED + LIVE-VALIDATED 2026-07-23

Live beta.koyal.ai exhaustive run 2026-07-22: deterministic entry navigation could not activate `REGENERATE (Boxing Animals)`, so the three-step LLM entry fallback clicked it successfully and waited for processing. When the control became enabled again, the fallback clicked the identical Regenerate twice more and then falsely reported the entry unreachable. Root cause: execution-level mutation suppression only knew labels passed in by the outer deep walker; mutations successfully fired inside the entry Explorer were not checked against that Explorer's own action history. Fixed generically: successful mutation labels are remembered within each mutation segment; a repeated identical mutation with no intervening successful action is suppressed, and contextual repeated controls are replayed inside their owning card. Live Wayfinder Compass validation submitted exactly once, learned the contextual recipe, replayed it with zero LLM calls, and promoted deterministic.

### 🟡 HIGH — `#deep-walk-branch-boundary` — one branch milestone can act inside the next wizard state — CODE FIXED; LIVE RERUN IN PROGRESS 2026-07-23

Focused Script/Audio fork validation exposed two generic mapping gaps. First, a verified button-entry path can resume an older draft instead of returning to the expected fork; the deep walker now verifies the landed page and uses a direct wizard URL only as bounded recovery. Second, an action that navigated while deterministic processing was active bypassed the ordinary one-screen return check, so the upload milestone selected a Story Type before handing control back. The boundary is now rechecked after processing. Walk trails are included in the flow-proposal summary as authoritative branch provenance, preventing script-only controls from being asserted on an audio path. Unit coverage and build are clean; final live rerun remains.

### 🟡 HIGH — `#explorer-state-cycle-budget-burn` — alternating controls on the same recurring state bypass the repeat guard — CODE FIXED 2026-07-22, LIVE REVALIDATION PENDING

Live beta.koyal.ai exhaustive run 2026-07-22: Edit Scenes exposed three unlabeled scene controls, one of which opened the unrelated Report a Bug modal. Explorer alternated scene controls, selection mode, modal close, and wizard navigation for all 40 steps, then deep-walker immediately started another 40-step Explorer on the identical state. The existing guard only recognized the exact same `action|ref|value` three times consecutively, so a short multi-action state cycle could consume the whole LLM budget without progress. Reproduced in two separate walks. Fixed generically: normalized URL + full-page state visits are counted across varied actions; on the fourth occurrence AutoQA performs one screenshot/full-page arbitration. First clean live rerun exposed changing generated-image URLs/cache tokens; URL/hash/opaque-token noise is now removed. Second clean rerun proved Explorer stopped at exactly four recurrences, then exposed a handoff hole; a contained state-cycle now ends/recovers the walk after visual assessment unless vision proves persistence or processing. Third clean rerun live-validated that full handoff (walk moved Back and queue continued), but arbitration itself correctly saw an attached PDF and enabled Next and still aborted because only `done` was accepted. One bounded recovery click is now allowed only for an exact non-mutating Next/Continue/Proceed label, through the normal guard; mutations, fills, uploads, Close/Cancel and arbitrary controls remain forbidden, and a second recurrence ends the walk. Regression tests cover the base/modal cycle, changing generated-media URLs, mutation-aware handoff, safe Next recovery, and recovery-label exclusions. Keep 🟡 until final live revalidation.

### 🟡 HIGH — `#vision-invalid-value-recovery` — a vision-confirmed field validation error must invalidate the saved value and retry once — CODE FIXED 2026-07-22, LIVE REVALIDATION PENDING

Live beta.koyal.ai exhaustive run 2026-07-22: vision correctly read “This Avatar name is already in use. Please choose a different name,” but deep-walker ended the walk. A later walk silently reused the persisted `Jason` value, spent 23 more LLM steps selecting empty avatar variants, and hit the identical blocker again. Fixed generically: a disabled completion control plus explicit value-rejection text (`already in use/taken`, `not allowed`, constrained/invalid value) invalidates only the most recent matching non-secret saved fill, asks through the normal human channel for a different value, stores it under the original intent key, refills the live field once when it remains addressable, and continues. Generic “required/missing data” and credentials are excluded; recovery is bounded once per goal. Tests cover store invalidation/replacement, direct live-field refill, phrase detection, and exclusion of “Missing required data.” 88/88 unit tests and TypeScript build pass. Keep 🟡 until live revalidation.

### ✅ HIGH — `#post-mutation-processing-replay` — vision-confirmed processing must poll the submitted artifact, never create it again — FIXED + LIVE-VALIDATED 2026-07-23

After Finalize, terminal vision may see a saved card that still has a PROCESSING badge. Typed persistence states now enter click-free polling; submitted mutation controls stay one-shot. Final hardening on 2026-07-23 prevents a generic visual “complete” assessment from overriding a visible pending badge or disabled Regenerate control. Live Asset validation first caught and refused Ranger Binoculars while still processing, then a fixed run waited until Harbor Barometer became usable; two deterministic replays subsequently waited through both processing phases and vision-verified Blue Storm Lantern and Harbor Desk Lamp with no resubmission. Outfit received the same zero-click wait behavior and promoted deterministic.

### ✅ HIGH — `#processing-timeout-state-loop` — processing timeout becomes a bug and leaves the stuck state — FIXED + LIVE-VALIDATED 2026-07-22

Original failure: `/scriptEdit` visibly remained on “Taking longer than expected. Server may be busy,” but the walker repeatedly started another LLM loop instead of leaving the state. Fixed policy: the banner is active processing; exceeding `AUTOQA_PROCESSING_WAIT_MS` records structured `processing-timeout` evidence, attempts bounded Back then Forward recovery, ends the current walk as an error, and continues the queue. Live validation in the 2026-07-22 beta wipeout run: walk 1 recorded a 1200s timeout, moved back to `/selectStyle`, ended as error, and immediately started walk 2 (`NEW CHARACTER`).

### ✅ HIGH — `#vision-stuck-trigger-too-narrow` — targeted vision affirmation for validation and false processing waits — FIXED 2026-07-22

Live on beta.koyal.ai 2026-07-21: character finalization stayed disabled because the visible UI said the same name was not allowed, but AutoQA never asked vision to diagnose it. The page kept changing slightly (generate, review, regenerate, avatar choices, Use Existing), so the current "same unchanged state" trigger kept resetting. A later beta run exposed the companion failure: the Locations editor visibly showed a completed “Cozy Corner Café” image with Edit/Revert/Regenerate controls, while deterministic processing logic stayed silent for more than 20 minutes and never invoked vision. Fixed: a disabled required completion control plus concrete validation text now triggers a screenshot-assisted full-state decision immediately; every Explorer/deep-walker processing wait asks a dedicated visual state verifier after three 5-second polls. That verifier distinguishes `active`, async-operation `complete`, visibly `blocked`, and `uncertain`; `complete` only releases the wait and never proves the whole flow. Active/uncertain states retain the generous deterministic budget. Build + 64/64 unit tests pass, including an integration-style fake-browser regression proving a false processing state releases after exactly three polls, the narrow validation trigger, and a completed-location-shaped processing assessment. A direct external-model replay of the user screenshot was intentionally not run without separate privacy approval.

### ✅ HIGH — `#stateful-route-match` — similar theme screens are mistaken for each other — FIXED 2026-07-21

Space, Titanic, and Bollywood reuse nearly identical wizard screens. During the 2026-07-21 XP wipeout run, AutoQA correctly reached `/bollywood/export` but labeled it as Titanic's Export page; Space replay also mislabeled `/space/characters` as Titanic's Characters page, then falsely passed Space Premiere/quiz/navigation milestones while actually testing `/titanic/premiere`. Root cause: broad LLM detection fragments such as `/characters`, `/premiere`, and `/export` were trusted even when the page's exact saved route disagreed. Fixed: an exact learned `urlPattern` is authoritative, broad `urlIncludes` is only a fallback when no route pattern exists, and a uniquely owned exact stateful route resolves even after its original Create control disappears. Two regression tests cover the exact character/export failures; build + 52/52 tests pass. Focused live XP validation: Space/Titanic Characters and Premiere each resolved to their correct theme IDs, while the unseen Bollywood Premiere stayed unmapped instead of being stolen by Titanic. The isolated validation Chrome closed cleanly.

### ⭐ Must-do ranking (does a RUN break/stall/mislead without this fix?)

Ranked by "will a run fail or produce invalid results without it," not by general value.

**🔴 MUST status — no confirmed run-breaking implementation bug remains open; completed fixes and one standing watch-item:**
1. ✅ **`#pageerror-object-serialization` — DONE + LIVE-VALIDATED 2026-07-21.** `pageErrorFromUnknown()` now preserves message-less agent-browser error objects as safe JSON instead of `[object Object]`; `collectProductBugs()` groups the same normalized error signature into one report with an occurrence/flow count. Unit tests + build pass. Partial exhaustive xp.koyal.ai validation captured 227 real exceptions as structured `InvalidStateError` payloads with zero placeholders; reconstructing the reporter over the saved partial run collapsed 21 failed-step occurrences across 10 flows into one bug block.
2. ✅ **`#initial-login-retry` — DONE 2026-07-20.** The initial login now retries 3× with backoff before continuing unauthenticated (`commands/explore.ts` → `attemptInitialAuth`, exported + unit-tested). Was: ONE transient error (Anthropic 529) on the first login call stranded the whole explore unauthenticated (koyal run #1 = 7/5/2). Verified by `npm run test:unit` (succeed-first / retry-twice-then-succeed / exhaust-then-continue).
3. ✅ **`#login-submit-spam-+-no-auth-response-check` — DONE 2026-07-21 (`da104af`).** Login exploration submits once and waits for the matching auth response. A 401/403/429/5xx stops and reports the real refusal; a 2xx proceeds to session verification; only “no auth request dispatched” permits one fresh retry. Unit tests cover endpoint selection, pending requests, status classification, and no-resubmit decisions.
4. 👀 **`#auth-gate-convergence` — WATCH-ITEM WITH TESTS; not a failing task.** `looksLikeAuthGate` is centralized, the DOM password-input check reaches all current call sites, and characterization tests lock the historical cases. Add a new test and targeted fix only if a genuinely new failure mode appears.

**🟠 SAFETY / structural — a run "completes" but can do real-world harm or is guaranteed to false-fail:**
1. **`#off-origin-replay-guard`** — no off-origin guard on the replay/test path → a flow can drive to a real payment/off-site page (GreenKart → live Teachable checkout, real order id, zero prompt). Spends money / leaves real state.
2. **`#credential-field-not-detected-sensitive`** (SECURITY, found 2026-07-21 filmarena login) — a login field whose accessible name doesn't resolve can bypass the sensitive-field detector and persist credentials through the ordinary field-value path. Network-capture redaction is fixed; this field-routing gap remains open.
3. **`#logout-guard-override`** — `LOGOUT_RE` is unconditionally auto-denied → any milestone whose goal is "click Logout" is structurally unpassable forever (guaranteed false-fail on logout flows).

**🟡 QUALITY / TRUST — runs complete; verdicts or coverage are weaker (not run-blockers):**
- **Koyal creation/video cluster — `#koyal-fresh-project` + `#koyal-wizard-landmark` + `#upload-reloop`. DOWNGRADED from 🔴 2026-07-20 (evidence).** The 2026-07-20 koyal run's *test-phase* video flows entered cleanly via "New Project" (B) and reached `/editscene` — the cluster no longer BLOCKS runs. The symptoms (6× "no working file input", entry-not-reachable aborts) still recur during *exploration walks*, so it degrades exploration efficiency but doesn't block; the real video blocker is S3 (external). Tractable follow-ups if it resurfaces: re-verify the `#upload-reloop` rule against a live snapshot, make B's fresh-start proactive for creation entries. `#koyal-wizard-landmark` (sidebar over-matches as a landmark) is genuinely hard — don't rush it.
- ✅ `#generation-wait-loop` fixed; ✅ `#theme-wizard-page-identity` fixed and focused-live-validated; `#constrained-recovery-values` is partially fixed (realistic-value policy/defaults shipped, arbitrary visible-constraint validation remains). Details below.
- ✅ `#unproven-walk-flows` fixed 2026-07-21: deep walks ending `no-progress`/`step-cap` can no longer become replayable flows; existing affected beta.koyal.ai flows were quarantined without wiping the crawl/map. Creation flows now require submit/generate/finalize plus visual persistence.
- ✅ **`#flow-lifecycle-proof` fixed 2026-07-21:** human-selected/partial proposals are now explicitly `exploratory`, bypass saved milestone recipes, and use LLM exploration until one complete run passes every milestone, records every recipe, and visually/deterministically proves the terminal or persistent artifact. A second full recipe-validation run is required before promotion to `deterministic`; any LLM fallback demotes/holds the flow for re-validation. Deep exploration retries every non-terminal walk (not only `aborted`), exhaustive mode schedules every discovered creation entry unless an explicit `--deep-flows` cap is supplied, stateful sidebar landmarks cannot absorb different wizard URLs, and vision may conservatively prove terminal persistence when SPA classification is ambiguous. Automation inability to locate/activate a control remains needs-review/exploratory—not a product FAIL without concrete page/console/network/persistence evidence.
  - **Completion hardening:** processing/terminal defaults are now 20 minutes and deep walks default to 60 states. Runtime page/console/5xx signals are recorded as bug evidence but no longer terminate a still-functional UI; the path ends only when the error visibly blocks progress, processing never resolves, or bounded recovery fails. Stable no-progress invokes screenshot-first LLM recovery to locate the actual enabled control/modal/validation/required field, and unfinished entries remain queued. Explicit creation-label fallback schedules NEW CHARACTER, ADD ASSET, CREATE OUTFIT, Script/Audio, Generate/Regenerate, Render and Upload surfaces even if the classifier mislabeled their category. Edit prompts require Apply/Regenerate/Save plus post-refresh persistence, not merely typed text.
- `#14-blankpage` (Explorer recovery can still reopen onto auth), `#16-live` (validate #17), `#prompt-dialog-value`.

**⚪ ENHANCEMENT / EFFICIENCY — nice-to-have, runs work fine:** ✅ `#6 / #12` vision oracle shipped and subsequently expanded with processing/validation affirmation; `#statement-kb-batching` (fewer classify prompts), `#flow-runner-anomaly-tags` (richer reports), the rest of `#long-tail`.

---

### ✅ #pageerror-object-serialization — fixed and live-validated
✅ **DONE + LIVE-VALIDATED 2026-07-21.** `core/verification.ts` now exports/uses `pageErrorFromUnknown()`: direct messages stay direct, message-less objects serialize safely (circular/bigint-safe), and empty objects get a descriptive non-placeholder fallback. `AgentBrowser.errorsJson()` now honestly types raw errors as `unknown[]`. `core/slack-bugs.ts` normalizes error signatures (query strings, UUIDs/long ids, stack line/column noise), groups repeats, and adds `N occurrences across M flows` to the existing four-field Bug block. Tests: `src/core/verification.test.ts`, `src/core/slack-bugs.test.ts`; `npm run test:unit` = 19/19 and `npm run build` clean. Live partial exhaustive xp.koyal.ai run (`2026-07-21T17-39-10-412Z`, user-stopped): 227 real page-error occurrences serialized as `{"column":0,"line":0,"text":"InvalidStateError: Transition was aborted because of invalid state","url":null}`, zero `[object Object]`; reconstructing `collectProductBugs()` over its 110 saved steps collapsed 21 failed-step occurrences across 10 flows into ONE report block. Slack/final site-summary did not execute because the user stopped the process with SIGINT, but both downstream surfaces consume this same validated collector.

### ✅ #generation-wait-loop — fixed; active generation now uses deterministic waits plus vision affirmation
✅ **DONE (CODE + UNIT VALIDATED) 2026-07-21; avatar wait live-validated, new render prose matcher awaits a fresh process.** The inline-processing recognizer now treats an accessibility `button/link "GENERATING"` (and equivalent processing verbs) marked disabled/busy as active processing, so it enters the configured deterministic processing budget. The XP wipeout run successfully waited ~90s for Jason's avatar, finalized it, and reached Storyline. The same run exposed a second phrasing gap on Premiere: the visible status said "YOUR FILM IS RENDERING" / "NOW IN PRODUCTION", but the already-running process did not recognize it and wasted LLM actions against locked customization/nav controls until the render finished. Those explicit active-render phrases are now recognized; static "Video Rendering Settings" remains excluded. Tests lock all cases. If ordinary repeated `wait` decisions still reach the loop guard, the explorer sends a short-lived screenshot plus full snapshot to the LLM; a visually confirmed wait suppresses that abort.

Live-reproduced twice on xp.koyal.ai 2026-07-21. Character creation reached a disabled `GENERATING` button; the LLM correctly chose `wait` three times, but the ordinary explorer loop detector treated `wait||` repetition as stuck and aborted before avatar completion. `AUTOQA_PROCESSING_WAIT_MS=1200000` was configured, but this button-state signal did not enter the protected deterministic processing-wait path. The shipped behavior now lets confirmed processing override the repeat guard while still requiring generated-choice/finalize persistence before completion.

### ✅ #theme-wizard-page-identity — fixed and focused-live-validated
✅ **FIXED 2026-07-21.** Root cause had two layers: stateful landmark-overlap merging did not require URL agreement, and the earlier fix still trusted an identical LLM-generated page id before comparing routes. `mergePage()` now requires route agreement and derives a route-qualified id on textual-id collision. Regression tests cover both cases; focused XP validation correctly separated Space and Titanic Characters/Premiere, while an unseen Bollywood Premiere remained unmapped instead of being stolen by Titanic.

Live-reproduced throughout the xp.koyal.ai 2026-07-21 partial run. The sitemap's single `wizard-characters` page identity allowed Space-specific walked flows (`CREATE COMMANDER`, `CREATE ENGINEER`, `CREATE PILOT`) to start on `/titanic/characters`, where those controls cannot exist. The route-qualified identity and authoritative exact-route matching above close that failure mode.

### 🟡 #constrained-recovery-values — human-controlled fills shipped; arbitrary visible constraints remain
🟡 **CORE USER-CONTROLLED INPUT PATH FIXED 2026-07-21.** Every non-secret free-text fill now crosses one ask-once resolver: LLM exploration/recovery, deep walks, deterministic recipe replay, and edit probes. It shows a field-specific suggestion but has no default and will not submit anything until the human explicitly answers; answers persist per site/page/field in `field-values.json` and recipe values are updated to match. Existing `seedValue`/old recipe junk (including the live `dog`/`autoqa-walk` case) is only a proposal and cannot bypass the resolver on a new process. Explorer/deep-walker prompts also preserve human seeds and forbid fictional/themed/joke/QA-looking person values. Unit tests cover explicit-answer persistence/reuse and suggestions. Still open in this broader task: automatically validate a human answer against arbitrary visible field constraints, and prevent no-progress walks from producing duplicate creation milestones after the UI advances.

**Live follow-ups fixed:** the first resolver version replaced what was typed but built the LLM goal with the old marker first, so the LLM repeatedly "corrected" valid human text and never clicked Generate/Finalize. Quoted field hints are now resolved before goal construction; unquoted/dynamic fills add an explicit authoritative-human-value note to step history. Creation milestones reject stale recipes without a completion action and cannot auto-pass unless visual verification is `clear` on a persisted final state. Saved-recipe uploads now ask the human for a path instead of uploading silently. Duplicate/existing-item dialogs must choose the safe replace/create-new path for creation goals; "Use existing" cannot prove generation. Walk compilation now keeps the entry action and only the final effective attempt from each consecutive unchanged page, preventing one stuck rendering page from becoming five duplicate replay milestones (unit-tested).

Live-reproduced on xp.koyal.ai 2026-07-21. A valid human seed `TitanicNova` was later replaced after probe/repositioning by `Caledon Hockley`; the screenshot visibly showed "Only letters are allowed. No spaces, numbers, or special characters," so creation correctly refused. Another proposed flow hard-coded `CommanderZephyr123`, also invalid. Existing literal-value adaptation does not cover every value source. Apply visible constraints to human seeds, LLM recovery fills, replayed recipes, edit probes, and hard-coded flow literals. Prefer natural persisted content (user preference): e.g. `Jason` plus a realistic character description, not random QA markers. Also prevent no-progress walks from producing repeated `CREATE CHARACTER` milestones after the UI has advanced to `FINALIZE CHARACTER`/`REGENERATE`.

---

### #16-live — validate task #17 on a real login-gated site
`loginFailureDowngrade` (task #17, shipped 2026-07-16, commit `c13c7e4`) is only validated by code-review + saucedemo no-regression — a clean-auth koyal run never proposes a login-shaped milestone, so the actual downgrade-on-silent-failure branch has never fired live. Run against lambdatest / expandtesting / webdriveruniversity (all have real login-gated flows in history) and confirm a genuinely-failed login gets `needs-review`, not a false `pass`.
*(Old prose called this "task #16" on 2026-07-17 — collides with the already-shipped Slack-integration #16 from 2026-07-16. Disambiguated here as #16-live.)*

### #upload-reloop — explorer loops re-uploading despite a completed upload (15dfd41 fix not firing)
Live finding, koyal deep run 2026-07-17, user screenshot as primary evidence: the script wizard's "Upload file" step correctly showed test-script-5-second.pdf attached + Next ENABLED (upload succeeded), but the explorer kept re-asking for a file ("retry attaching via file input") instead of clicking Next. Root cause is the documented 0.00-MB-retry bug (tiny 1.6KB PDF → Koyal honestly renders "0.00 MB" → explorer misreads as "nothing attached"), which commit `15dfd41` (2026-07-13) was *supposed* to fix ("trust filename + enabled advance control over an ambiguous size once a prior upload is in step history") — so either the fix isn't firing on this path or the trigger condition is narrower than the live case. Re-verify `15dfd41`'s rule against the actual koyal upload snapshot. Vision (#6/#12) would also close it as a fallback but the deterministic rule should work first.
*(Was conflated with vision as "#14-vision" in a prior tasks.md draft; the crashed session's tracker kept them separate — this is the concrete bug, #6/#12 is the vision architecture.)*

### ✅ #6 / #12 — screenshot verification as a second signal — shipped
✅ **DONE 2026-07-21; processing/validation affirmation expanded 2026-07-22.** `LlmClient.complete()` supports native image inputs. Every real flow milestone sends its saved screenshot to a conservative visual assessment that may downgrade a pass to `needs-review`, but cannot create a hard fail or upgrade a deterministic failure. Explorer/deep-walker also use targeted screenshot affirmation for stuck controls, visible validation, and prolonged processing. No coordinate-based visual execution was added. Current build + 64/64 tests pass.

**Follow-up hardening from the beta.koyal.ai live trace:** the screenshot layer correctly saw `Generating avatar...`, but KB re-evaluation erased its `needs-review` and restored PASS. Fixed: a visual concern is now sticky until an explicit human verdict, and Explorer suppresses `done` whenever the full page still shows generation/processing, polling within the configured budget. The centralized human field resolver brings the total suite to 28/28.

The hard guardrail remains: vision is corroboration, not an autonomous pass/fail oracle. `#upload-reloop` remains separately open because its deterministic upload-state rule still failed live even though vision can now help recovery.

### #14-blankpage — blank-page recovery doesn't verify it landed on the *authenticated* page
🟡 **PARTIALLY FIXED.** Deep walks now check for a real auth gate on every state, re-authenticate through `ensureAuth`, and abort honestly if the gate remains. `core/explorer.ts` still reopens `lastRealUrl` after `about:blank` without immediately checking whether that recovery landed on a login gate; flow-runner checks often catch it later, but the generic Explorer path remains the open part.
**Local characterization 2026-07-22:** a fake protected page detached to `about:blank`, recovery reopened onto `/login`, and generic Explorer continued from that login state and returned `authentication required`; it did not invoke re-authentication. Open status confirmed.

### 👀 #auth-gate-convergence — standing watch-item, not a discrete open fix
🧪 **Characterization tests added 2026-07-20** (`src/agent/auth-gate.test.ts`, run via `npm run test:unit`) lock in the current hardened behavior of `looksLikeAuthGate` + `isLoginShapedGoal` so the next edit can't silently regress a case some past batch fixed. Deliberately NOT reworked — it's already one centralized DOM-based function with a heading-count guard, and there's no failing case to validate a rework against. **When a new failure mode appears: add a case to that test file, then fix that one case.**
Second-highest-value recurring bug source in this project's history (`normalizePath`/`matchPage` is #1). Patched in nearly every batch since ParaBank (missing accessible-name password field → expandtesting demo-page-name false-positive → automationintesting unconditional-Logout-label trust → lambdatest cross-flow contamination → webdriveruniversity heading-count false-positive + a duplicate bypass → automationintesting's bare-"login"-in-a-verify-goal misroute) with a new failure mode nearly every time. If a future site's flow keeps re-authenticating unnecessarily or silently skips a real login milestone, suspect `looksLikeAuthGate`/`hasVisiblePasswordInput`/`isLoginShapedGoal` first. Consider a structural rework rather than another one-off patch next time it recurs.

### #off-origin-replay-guard — `Explorer.achieveGoal()` has no off-origin guard during milestone execution or flow replay
Real safety gap, found 2026-07-15 on globalsqa (ad interstitial led a click off-site twice, caught only reactively) and GreenKart (a "JOIN NOW" button drove a real 3-hop redirect all the way to a live Teachable checkout page with a real order ID and **zero guard prompt raised** — `guard.ts`'s destructive-keyword floor doesn't cover join/enroll/subscribe, AND the off-origin guard that exists only lives in `crawler.ts`/`deep-walker.ts`'s exploration-time edge mapping, never in the replay/test path). Fix: port the same `isOffOrigin` guard `deep-walker.ts` already has into `Explorer.achieveGoal()`'s milestone/replay path. Until fixed, treat any payment/subscription-adjacent flow on a new site with suspicion — de-approve if in doubt (already done for GreenKart's `subscription-plan-selection`).
**Local characterization 2026-07-22:** a fake Explorer click changed `https://app.example.test/start` to `https://evil.example/checkout`; Explorer accepted `done` and returned success on the foreign origin. Open status confirmed without contacting an external site.

### #credential-field-not-detected-sensitive — unlabeled login fields can bypass protected storage
Open security issue, confirmed on filmarena.ai 2026-07-21. When an email/password input resolves only to a bare ref such as `e8`/`e12`, label-based sensitivity detection can fail and route the value through ordinary persisted field answers. Network evidence redaction is already fixed; remaining fix: mark every fill performed inside the explicit auth credential goal as sensitive regardless of accessible label, with DOM `type=password` as an additional signal.
**Local characterization 2026-07-22:** `isSensitiveFieldLabel('e12')` returned false, and a mocked auth fill with a non-matching proposed value reached `onFillRequested` with `context.sensitive === false`; the eventual step log was redacted, but the resolver had already been invoked through the non-secret path. Open status confirmed.

### #logout-guard-override — `guard.ts`'s `LOGOUT_RE` has no override path at all
Found 2026-07-15. Every other destructive-action keyword can be allowlisted `always`/`never`; a Logout click is auto-denied unconditionally. Makes any flow whose explicit milestone goal is "click Logout" structurally unpassable, permanently, on any site. Distinct from the already-fixed (2026-07-16) "logout hidden in a dropdown" discovery problem.
**Local characterization 2026-07-22:** `Guard.confirmClick('Logout')` returned false with zero human prompts even when the saved allowlist contained `always`; a normal destructive Delete control did use the prompt. Open status confirmed.

### 🟡 #koyal-fresh-project — reactive fresh-start shipped; proactive recipe-first entry remains
**Explicit required behavior (user-directed 2026-07-21): Koyal creation flows should prefer a verified button-click entry path (for example `/dashboard > New project > Start with Script/Audio`) and use direct wizard URLs only for intentional resume/recovery.** Direct `/upload` navigation is not a clean entry because Koyal can resume the account's unfinished draft, which caused the live beta.koyal.ai test to leave one wizard, inherit stale state, and misposition the next flow. Implement this as a replayable entry recipe, not a Koyal-only hardcoded click sequence; retain direct URLs for stateless pages and verified recovery.

✅ **Applied to current beta.koyal.ai state 2026-07-21:** both Script and Audio approved flows now enter at `/dashboard` and click New Project before selecting their mode. Script now continues past Edit scenes through Create Video/render and a playable/downloadable final-artifact check; Audio likewise continues through Create Video/render. General flow-proposal instructions now forbid ending video coverage at Edit scenes.

🟡 **Downgraded from 🔴 2026-07-20** — the 2026-07-20 koyal run's test-phase video flows entered cleanly via "New Project" (B) and reached `/editscene`; the cluster (`#koyal-fresh-project` + `#koyal-wizard-landmark` + `#upload-reloop`) now degrades *exploration* (symptoms still recur there) but no longer *blocks* runs. Real video blocker is S3 (external). See the Must-do ranking → 🟡.
The agnostic `learnedFreshStart` implementation (`591f400`) supports multi-step values such as `/dashboard > New project`, self-verifies the expected entry, and current beta Script/Audio state uses button-click entry. Still open: make that verified entry recipe proactive for newly discovered creation flows instead of learning it only after a resumed-draft mismatch.

### ✅ #initial-login-retry — fixed 2026-07-20
✅ **DONE 2026-07-20** — `commands/explore.ts` now retries via `attemptInitialAuth` (3× linear backoff, exported + unit-tested in `src/commands/explore.test.ts`) before continuing unauthenticated. Run `npm run test:unit`.
Found 2026-07-16. `commands/explore.ts` wrapped the pre-crawl `ensureAuthenticated()` in a try/catch that logged a warning and continued UNAUTHENTICATED on ANY failure, including a transient one (observed: an Anthropic 529). Starved the whole crawl/flow-proposal off a near-empty map (koyal run #1 = 7/5/2).

### #statement-kb-batching — statement-KB flags every item on a listing page as its own classify question
Confirmed inefficient (not incorrect) on 3+ sites (GreenKart's ~25-item catalog, lambdatest's product listings, webdriveruniversity's ~30-widget page). Always correctly classified noise so far, but a real human-in-the-loop efficiency problem. Worth batching same-shaped repeated candidates into one round-trip instead of one per item.

### #flow-runner-anomaly-tags — structured per-milestone anomaly tagging
Design discussed 2026-07-15, not built. Add a tag (`overachievement`/`session-leak`/`off-site`/`upstream-break`/`repositioned`/`crash-recovered`) separate from the pass/fail/needs-review/skipped verdict, so a report shows *why* a milestone is unusual instead of requiring someone to read six step-summaries by hand. Also needs an "is this milestone's goal already satisfied by current state" pre-check for the LLM-overachievement case (distinct from the existing fast-forward-to-a-later-page mechanism, which doesn't apply when overachievement lands *past* the last milestone).

### #koyal-wizard-landmark — Koyal's own wizard sidebar over-matches as a page landmark
Live-reproduced 2026-07-15: the `wizard-upload-file` classifier landmark (`snapshotAnyOf: ['Upload file', 'Story Type', 'Review transcript', 'Edit scenes']`) matches the wizard's persistent sidebar, visible on every step — can't distinguish "on the upload step" from "anywhere in the wizard." Drove ~12 doomed upload attempts in one run. Same "chrome landmarks must never identify a state" disease as `normalizePath`'s collisions; needs a Koyal-specific landmark fix (probably: require a step-specific element, not just a sidebar item, in the detection signature).

### #prompt-dialog-value — native `prompt()` dialogs with a required specific typed value are only partially supported
Found 2026-07-13 on webdriveruniversity. `resolveBlockingDialog` resolves the block correctly but always accepts with an empty value, so a milestone whose verification depends on the exact typed text appearing would false-fail. Needs the pending dialog's message surfaced to the LLM's own decision loop — bigger than a one-off patch.

### ✅ #f13fbbb-regression — completed by subsequent cross-site suites
The walk-choice-memory and distinct-mode proposal changes were followed by exhaustive saucedemo/Koyal validation and later multi-site runs across OrangeHRM, ParaBank, automationexercise, demoqa, and others. No regression attributable to these two mechanisms surfaced; later failures were independently root-caused. This historical regression task is closed.

### ✅ #explorer-fail-signal — closed after current-code audit 2026-07-22
`explorerFailureDowngrade` turns a clean deterministic pass into `needs-review` whenever exploration itself failed; `loginFailureDowngrade` covers the `explored === null` authentication branch; creation milestones additionally require completion actions and visual persistence. KB re-evaluation cannot erase any of those downgrades without new success evidence. Replay-only milestones are governed by the separate two-pass flow-lifecycle proof. No remaining instance of the originally reported “Explorer failed but milestone silently PASSed” path was found.

### #long-tail — accumulated low-priority / diagnosed-not-fixed items
Open: (1) **demoqa `widgets-datepicker-tabs:m2`** fails, never root-caused (2026-07-09). (2) **The 2026-07-14 idempotent-skip verification fix still lacks a direct Koyal test-phase trigger validation.** (3) SPA junk-URL pollution beyond the shipped soft-404 handling.

✅ Removed stale recycle item: `AgentBrowser.recycle()` now resolves the session PID from disk first, verifies ownership with the session socket, and refuses broad killing by default. The old all-session fallback requires the explicit unsafe override `AUTOQA_ALLOW_BROAD_RECYCLE_KILL=true`.

---

## Recently completed / shipped (context, not action items)

- **Login response convergence (`da104af`, 2026-07-21).** Login submit is sent once, awaited, and classified by its actual HTTP response; refusals are reported without resubmission, eliminating the self-induced 429 loop.
- **Session-scoped daemon recycle hardening (`942a4de`).** PID-file-first resolution plus socket ownership checks replaced the automatic broad-kill fallback; broad killing is now an explicit unsafe opt-in only.
- **2026-07-20 batch — merged and pushed (`591f400`, followed by `cd610ab`).** `#15`, noise auto-classification, agnostic `learnedFreshStart`, creation-input prompts, processing-wait no-mis-nav, per-site summaries, and LLM cost tracking are all committed. Live Koyal/filmarena evidence and the still-external Koyal S3 failure are recorded below.
- **#15 — `parseJsonFromLlm` crash-the-whole-flow bug.** Fixed 2026-07-17 in worktree branch `worktree-fix-json-parse-crash`, commit `07d33f0`, draft PR [#2](https://github.com/2006-sk/Koyal-browser/pull/2) — **merged to `autoqa` 2026-07-20 (`5943727`)**. `client.ts`: extracts every brace-balanced candidate, prefers the LAST one that parses (recovers self-correction replies, skips a stray earlier brace). `explorer.ts`: `decideNextAction` retries once on parse/call failure, degrades to one contained `fail` step instead of an uncaught throw; `LlmBudgetExceededError` still propagates. xhigh code review (`wf_48ed55b0-0d5`) found 6 real defects in the first version (credential-redaction bypass, the retry's own unguarded 2nd LLM call, stale-object extraction, wrong-brace anchoring, an idempotent-skip regex collision, silent budget double-consumption) — all fixed in the same commit. 2 lower-severity findings (unify with `proposeFlows`' own retry pattern; retry-stacking with the LLM client's internal retries) left as follow-ups, not fixed. Validated: unit tests for every extraction scenario + mocked-LLM redaction/budget tests, plus a live saucedemo run where a genuine transient "fetch failed" hit both the attempt and the retry and degraded cleanly to one contained FAIL instead of crashing the flow.
- **#11 — flow-runner no longer silently drops milestones after a mid-flow FAIL.** Shipped + live-validated both branches 2026-07-16/17. Commit `c13c7e4`.
- **#17 — login-shaped-milestone false-PASS downgrade.** Shipped 2026-07-16, commit `c13c7e4`. Code-reviewed + no-regression only — see #16-live above for the still-needed live trigger.
- **Two-step logout (opener > logout) + auto-discovery.** Shipped 2026-07-16, commit `8d99b06`. Not yet re-confirmed on OrangeHRM's collapsed-dropdown case specifically.
- **Slack product-bug reporter.** Shipped 2026-07-16, commit `8d99b06`. Live-validated (posted only the 2 real S3 bugs from a 14-fail run, correctly excluded the rest).
- **Walk choice-memory + flow-proposal mode enumeration** (the "only tests the default mode" fix). Shipped 2026-07-16, validated live on filmarena.ai and (mode-coverage generalization) on koyal's 2026-07-17 run (a distinct Audio flow got proposed for the first time).
- **Duplicate-successHint dedup / soft-404 filtering / probe-breaks-position repositioning / Koyal draft "fresh entry" prompt** (5-file diff: `crawler.ts`, `flow-runner.ts`, `page-classifier.ts`, `probes.ts`, `sitemap.ts`). Validated 2026-07-17 on fresh-wipe saucedemo (47/3/1) + koyal (28/13/6) runs — no regressions, repositioning confirmed (no probe failure cascaded).
- **Poisoned koyal flow cleanup.** The 2026-07-15 session-loss cascade wrote Sign-Up/OTP milestones into `walked-projects-list-create-yournext-video` in koyal's `sitemap.json`. Resolved: the 2026-07-17 clean-slate deep run wiped + regenerated the sitemap — verified 2026-07-17 that flow now has 5 clean milestones, no poison. (Was an open item in the crashed session's tracker; confirmed moot.)

## Known, live, external — not autoqa bugs (context only)

- **koyal-s3-scene-generation-fetch** (Critical) — Koyal backend returns an HTML error page (`<!DOCTYPE…`) where JSON was expected during scene generation ("Failed to fetch JSON from S3: SyntaxError"). Documented in `autoqa/KNOWN_KOYAL_BUGS.md`. Reproduced 2026-07-09, 07-13, 07-16, and **re-confirmed live 2026-07-20** — that run reached `/editscene` for the first time and **localized it to scene-IMAGE generation**: scene *descriptions* (text) generate fine, but the scene image/data S3 fetch fails, so no images → **no video can render**. 3 bugs auto-posted to Slack that run. Still Koyal-side (production issue, flagged to the user who is Koyal staff); no autoqa code change can render a video until it's fixed.
- **webdriveruniversity's Login Portal** genuinely cannot succeed — jQuery loaded over `http://` on an HTTPS page is blocked as mixed content, so the page's own `preventDefault` script never attaches and a native form submit always wins, reloading with an empty query string. Real site bug, not autoqa's.
- ParaBank backend has genuine intermittent concurrency/flakiness on login (same credentials succeed most of the time, fail occasionally even in single-session isolation) — not fixable client-side.
- practice.expandtesting.com's Register button is genuinely, repeatedly obstructed by a shifting ad iframe — real site accessibility issue.
# HIGH — Product failures must not demote healthy replay recipes

- Track automation/replay health separately from the product verdict.
- If a deterministic recipe successfully reaches the feature, submits the intended action, waits, and captures a verified product-side error, report the product test as FAIL but keep the recipe qualified.
- Demote to exploratory only for automation-side replay/navigation/action/verification failures or genuinely ambiguous evidence.
- Exclude verified product-blocked downstream tasks from the replay-qualification denominator so one product defect does not turn a working recipe exploratory.
- Validate this against the Script final-video edit failure (`video is not edited please try again later!!`) and an equivalent synthetic product-error case.
