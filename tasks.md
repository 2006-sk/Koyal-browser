# autoqa task tracker

Persistent, cross-session task list for the autoqa project. **Read this first, before CLAUDE.md's prose**, at the start of any session — Claude Code's built-in TaskCreate/TaskList tool is scoped to one session and invisible across jobs, so anything tracked only there is lost the moment that session ends or crashes. This file is the single source of truth for open work; update it directly (check items off, add new ones, don't let CLAUDE.md's prose accumulate more ad-hoc "task #N" mentions — link back here instead).

IDs here are unique by construction. CLAUDE.md's own historical numbering is **not** reliable — #14 and #16 were each independently reused for two unrelated things by different sessions before this file existed. Where a task below descends from one of those prose mentions, the old number is noted for continuity, but the ID in this file is canonical.

Full narrative/evidence for anything here lives in CLAUDE.md (search by date or task ID) — this file is deliberately terse; don't duplicate the write-up, just enough to act on and a pointer.

---

## Current status — priority order

Status key: unmarked heading = open; 🟡 = partially fixed; 👀 = watch-only; ✅ = completed.

### 🟡 HIGH — `#processing-timeout-state-loop` — processing timeout must become a bug and leave the stuck state — CODE FIXED 2026-07-22, LIVE REVALIDATION PENDING

Live beta.koyal.ai wipeout run: `/scriptEdit` visibly remained on “Taking longer than expected. Server may be busy” for 5+ minutes, but after each Explorer failure the deep walker reclassified the identical page as a fresh state and started another 40-step LLM loop. It repeatedly toggled Character Voices, clicked stale refs that opened Report a Bug, and consumed budget instead of waiting once or moving on. Fixed policy: the busy/longer-than-expected banner is an active processing signal; exceeding `AUTOQA_PROCESSING_WAIT_MS` records structured `processing-timeout` evidence, attempts bounded Back then Forward recovery, ends the current walk as an error, and lets the crawler proceed to the next queued entry. A non-timeout Explorer failure that remains on the same stable state—including screenshot-first recovery—receives the same bounded navigation recovery and cannot restart as a new state. TypeScript build + 74/74 unit tests pass; keep 🟡 until a live run proves the crawler advances to the next walk after a real timeout.

### ✅ HIGH — `#vision-stuck-trigger-too-narrow` — targeted vision affirmation for validation and false processing waits — FIXED 2026-07-22

Live on beta.koyal.ai 2026-07-21: character finalization stayed disabled because the visible UI said the same name was not allowed, but AutoQA never asked vision to diagnose it. The page kept changing slightly (generate, review, regenerate, avatar choices, Use Existing), so the current "same unchanged state" trigger kept resetting. A later beta run exposed the companion failure: the Locations editor visibly showed a completed “Cozy Corner Café” image with Edit/Revert/Regenerate controls, while deterministic processing logic stayed silent for more than 20 minutes and never invoked vision. Fixed: a disabled required completion control plus concrete validation text now triggers a screenshot-assisted full-state decision immediately; every Explorer/deep-walker processing wait asks a dedicated visual state verifier after three 5-second polls. That verifier distinguishes `active`, async-operation `complete`, visibly `blocked`, and `uncertain`; `complete` only releases the wait and never proves the whole flow. Active/uncertain states retain the generous deterministic budget. Build + 64/64 unit tests pass, including an integration-style fake-browser regression proving a false processing state releases after exactly three polls, the narrow validation trigger, and a completed-location-shaped processing assessment. A direct external-model replay of the user screenshot was intentionally not run without separate privacy approval.

### ✅ HIGH — `#stateful-route-match` — similar theme screens are mistaken for each other — FIXED 2026-07-21

Space, Titanic, and Bollywood reuse nearly identical wizard screens. During the 2026-07-21 XP wipeout run, AutoQA correctly reached `/bollywood/export` but labeled it as Titanic's Export page; Space replay also mislabeled `/space/characters` as Titanic's Characters page, then falsely passed Space Premiere/quiz/navigation milestones while actually testing `/titanic/premiere`. Root cause: broad LLM detection fragments such as `/characters`, `/premiere`, and `/export` were trusted even when the page's exact saved route disagreed. Fixed: an exact learned `urlPattern` is authoritative, broad `urlIncludes` is only a fallback when no route pattern exists, and a uniquely owned exact stateful route resolves even after its original Create control disappears. Two regression tests cover the exact character/export failures; build + 52/52 tests pass. Focused live XP validation: Space/Titanic Characters and Premiere each resolved to their correct theme IDs, while the unseen Bollywood Premiere stayed unmapped instead of being stolen by Titanic. The isolated validation Chrome closed cleanly.

### ⭐ Must-do ranking (does a RUN break/stall/mislead without this fix?)

Ranked by "will a run fail or produce invalid results without it," not by general value.

**🔴 MUST status — no confirmed run-breaking implementation bug remains open; completed fixes and one standing watch-item:**
1. ✅ **`#pageerror-object-serialization` — DONE + LIVE-VALIDATED 2026-07-21.** `pageErrorFromUnknown()` now preserves message-less agent-browser error objects as safe JSON instead of `[object Object]`; `collectProductBugs()` groups the same normalized error signature into one report with an occurrence/flow count. Unit tests + build pass. Partial exhaustive xp.koyal.ai validation captured 227 real exceptions as structured `InvalidStateError` payloads with zero placeholders; reconstructing the reporter over the saved partial run collapsed 21 failed-step occurrences across 10 flows into one bug block.
2. ✅ **`#initial-login-retry` — DONE 2026-07-20.** The initial login now retries 3× with backoff before continuing unauthenticated (`commands/explore.ts` → `attemptInitialAuth`, exported + unit-tested). Was: ONE transient error (Anthropic 529) on the first login call stranded the whole explore unauthenticated (koyal run #1 = 7/5/2). Verified by `npm run test:unit` (succeed-first / retry-twice-then-succeed / exhaust-then-continue).
3. ✅ **`#login-submit-spam-+-no-auth-response-check` — DONE 2026-07-21 (`da104af`).** Login exploration submits once and waits for the matching auth response. A 401/403/429/5xx stops and reports the real refusal; a 2xx proceeds to session verification; only “no auth request dispatched” permits one fresh retry. Unit tests cover endpoint selection, pending requests, status classification, and no-resubmit decisions.
4. 👀 **`#auth-gate-convergence` — WATCH-ITEM WITH TESTS; not a failing task.** `looksLikeAuthGate` is centralized, the DOM password-input check reaches all current call sites, and characterization tests lock the historical cases. Add a new test and targeted fix only if a genuinely new failure mode appears.

**🟠 SAFETY / structural — a run "completes" but can do real-world harm or is guaranteed to false-fail:**
1. **`#off-origin-replay-guard`** — no off-origin guard on the replay/test path → a flow can drive to a real payment/off-site page (GreenKart → live Teachable checkout, real order id, zero prompt). Spends money / leaves real state.
2. **`#credential-field-not-detected-sensitive`** (SECURITY, found 2026-07-21 filmarena login) — a login field whose accessible name doesn't resolve can bypass the sensitive-field detector and persist credentials through the ordinary field-value path. Network-capture redaction is fixed; this field-routing gap remains open.
3. **`#logout-guard-override`** — `LOGOUT_RE` is unconditionally auto-denied → any milestone whose goal is "click Logout" is structurally unpassable forever (guaranteed false-fail on logout flows).

**🟡 QUALITY / TRUST — runs complete; verdicts or coverage are weaker (not run-blockers):**
- **Koyal creation/video cluster — `#koyal-fresh-project` + `#koyal-wizard-landmark` + `#upload-reloop`. DOWNGRADED from 🔴 2026-07-20 (evidence).** The 2026-07-20 koyal run's *test-phase* video flows entered cleanly via "New Project" (B) and reached `/editscene` — the cluster no longer BLOCKS runs. The symptoms (6× "no working file input", entry-not-reachable aborts) still recur during *exploration walks*, so it degrades exploration efficiency but doesn't block; the real video blocker is S3 (external). Tractable follow-ups if it resurfaces: re-verify the `#upload-reloop` rule against a live snapshot, make B's fresh-start proactive for creation entries. `#koyal-wizard-landmark` (sidebar over-matches as a landmark) is genuinely hard — don't rush it.
- ✅ `#generation-wait-loop` fixed; ✅ `#theme-wizard-page-identity` fixed and focused-live-validated; `#constrained-recovery-values` is partially fixed (realistic-value policy/defaults shipped, arbitrary visible-constraint validation remains). Details below.
- ✅ `#unproven-walk-flows` fixed 2026-07-21: deep walks ending `no-progress`/`step-cap` can no longer become replayable flows; existing affected beta.koyal.ai flows were quarantined without wiping the crawl/map. Creation flows now require submit/generate/finalize plus visual persistence.
- ✅ **`#flow-lifecycle-proof` fixed 2026-07-21:** human-selected/partial proposals are now explicitly `exploratory`, bypass saved milestone recipes, and use LLM exploration until one complete run passes every milestone, records every recipe, and visually/deterministically proves the terminal or persistent artifact. A second full recipe-validation run is required before promotion to `deterministic`; any LLM fallback demotes/holds the flow for re-validation. Deep exploration retries every non-terminal walk (not only `aborted`), exhaustive mode schedules every discovered creation entry unless an explicit `--deep-flows` cap is supplied, stateful sidebar landmarks cannot absorb different wizard URLs, and vision may conservatively prove terminal persistence when SPA classification is ambiguous. Automation inability to locate/activate a control remains needs-review/exploratory—not a product FAIL without concrete page/console/network/persistence evidence.
  - **Completion hardening:** processing/terminal defaults are now 20 minutes and deep walks default to 60 states. Runtime page/console/5xx signals are recorded as bug evidence but no longer terminate a still-functional UI; the path ends only when the error visibly blocks progress, processing never resolves, or bounded recovery fails. Stable no-progress invokes screenshot-first LLM recovery to locate the actual enabled control/modal/validation/required field, and unfinished entries remain queued. Explicit creation-label fallback schedules NEW CHARACTER, ADD ASSET, CREATE OUTFIT, Script/Audio, Generate/Regenerate, Render and Upload surfaces even if the classifier mislabeled their category. Edit prompts require Apply/Regenerate/Save plus post-refresh persistence, not merely typed text.
- `#14-blankpage` (Explorer recovery can still reopen onto auth), `#16-live` (validate #17), `#prompt-dialog-value`.

**⚪ ENHANCEMENT / EFFICIENCY — nice-to-have, runs work fine:** ✅ `#6 / #12` vision oracle shipped and subsequently expanded with processing/validation affirmation; `#statement-kb-batching` (fewer classify prompts), `#flow-runner-anomaly-tags` (richer reports), the rest of `#long-tail`.

---

### ✅ #pageerror-object-serialization — fixed and live-validated
✅ **DONE + LIVE-VALIDATED 2026-07-21.** `core/verification.ts` now exports/uses `pageErrorFromUnknown()`: direct messages stay direct, message-less objects serialize safely (circular/bigint-safe), and empty objects get a descriptive non-placeholder fallback. `AgentBrowser.errorsJson()` now honestly types raw errors as `unknown[]`. `core/slack-bugs.ts` normalizes error signatures (query strings, UUIDs/long ids, stack line/column noise), groups repeats, and adds `N occurrences across M flows` to the existing four-field Bug block. Tests: `src/core/verification.test.ts`, `src/core/slack-bugs.test.ts`; `npm run test:unit` = 19/19 and `npm run build` clean. Live partial exhaustive xp.koyal.ai run (`2026-07-21T17-39-10-412Z`, user-stopped): 227 real page-error occurrences serialized as `{"column":0,"line":0,"text":"InvalidStateError: Transition was aborted because of invalid state","url":null}`, zero `[object Object]`; reconstructing `collectProductBugs()` over its 110 saved steps collapsed 21 failed-step occurrences across 10 flows into ONE report block. Slack/final site-summary did not execute because the user stopped the process with SIGINT, but both downstream surfaces consume this same validated collector.

### ✅ #generation-wait-loop — fixed; active generation now uses deterministic waits plus vision affirmation
✅ **DONE (CODE + UNIT VALIDATED) 2026-07-21; avatar wait live-validated, new render prose matcher awaits a fresh process.** The inline-processing recognizer now treats an accessibility `button/link "GENERATING"` (and equivalent processing verbs) marked disabled/busy as active processing, so it enters the configured deterministic processing budget. The XP wipeout run successfully waited ~90s for Jason's avatar, finalized it, and reached Storyline. The same run exposed a second phrasing gap on Premiere: the visible status said "YOUR FILM IS RENDERING" / "NOW IN PRODUCTION", but the already-running process did not recognize it and wasted LLM actions against locked customization/nav controls until the render finished. Those explicit active-render phrases are now recognized; static "Video Rendering Settings" remains excluded. Tests lock all cases. If ordinary repeated `wait` decisions still reach the loop guard, the explorer sends a short-lived screenshot plus full snapshot to the LLM; a visually confirmed wait suppresses that abort.

Live-reproduced twice on xp.koyal.ai 2026-07-21. Character creation reached a disabled `GENERATING` button; the LLM correctly chose `wait` three times, but the ordinary explorer loop detector treated `wait||` repetition as stuck and aborted before avatar completion. `AUTOQA_PROCESSING_WAIT_MS=1200000` was configured, but this button-state signal did not enter the protected deterministic processing-wait path. The shipped behavior now lets confirmed processing override the repeat guard while still requiring generated-choice/finalize persistence before completion.

### ✅ #theme-wizard-page-identity — fixed and focused-live-validated
✅ **FIXED 2026-07-21.** Root cause had two layers: stateful landmark-overlap merging did not require URL agreement, and the earlier fix still trusted an identical LLM-generated page id before comparing routes. `mergePage()` now requires route agreement and derives a route-qualified id on textual-id collision. Regression tests cover both cases; focused XP validation correctly separated Space and Titanic Characters/Premiere, while an unseen Bollywood Premiere remained unmapped instead of being stolen by Titanic.

Live-reproduced throughout the xp.koyal.ai 2026-07-21 partial run. The sitemap's single `wizard-characters` page identity allowed Space-specific walked flows (`CREATE COMMANDER`, `CREATE ENGINEER`, `CREATE PILOT`) to start on `/titanic/characters`, where those controls cannot exist. The route-qualified identity and authoritative exact-route matching above close that failure mode.

### 🟡 #constrained-recovery-values — human-controlled fills shipped; arbitrary visible constraints remain
🟡 **CORE USER-CONTROLLED INPUT PATH FIXED 2026-07-21.** Every non-secret free-text fill now crosses one ask-once resolver: LLM exploration/recovery, deep walks, deterministic recipe replay, and edit probes. It shows a field-specific suggestion but has no default and will not submit anything until the human explicitly answers; answers persist per site/page/field in `field-values.json` and recipe values are updated to match. Existing `seedValue`/old recipe junk (including the live `dog`/`autoqa-walk` case) is only a proposal and cannot bypass the resolver on a new process. Explorer/deep-walker prompts also preserve human seeds and forbid fictional/themed/joke/QA-looking person values. Unit tests cover explicit-answer persistence/reuse and suggestions. Still open in this broader task: automatically validate a human answer against arbitrary visible field constraints, and prevent no-progress walks from producing duplicate creation milestones after the UI advances.

**Live follow-ups fixed:** the first resolver version replaced what was typed but built the LLM goal with the old marker first, so the LLM repeatedly "corrected" valid human text and never clicked Generate/Finalize. Quoted field hints are now resolved before goal construction; unquoted/dynamic fills add an explicit authoritative-human-value note to step history. Creation milestones reject stale recipes without a completion action and cannot auto-pass unless visual verification is `clear` on a persisted final state. Saved-recipe uploads now ask the human for a path instead of uploading silently. Duplicate/existing-item dialogs must choose the safe replace/create-new path for creation goals; "Use existing" cannot prove generation. Walk compilation now keeps the entry action and only the final effective attempt from each consecutive unchanged page, preventing one stuck rendering page from becoming five duplicate replay milestones (unit-tested).

Live-reproduced on xp.koyal.ai 2026-07-21. A valid human seed `TitanicNova` was later replaced after probe/repositioning by `Caledon Hockley`; the screenshot visibly showed "Only letters are allowed. No spaces, numbers, or special characters," so creation correctly refused. Another proposed flow hard-coded `CommanderZephyr123`, also invalid. Existing literal-value adaptation does not cover every value source. Apply visible constraints to human seeds, LLM recovery fills, replayed recipes, edit probes, and hard-coded flow literals. Prefer natural persisted content (user preference): e.g. `Jason` plus a realistic character description, not random QA markers. Also prevent no-progress walks from producing repeated `CREATE CHARACTER` milestones after the UI has advanced to `FINALIZE CHARACTER`/`REGENERATE`.

---

### #16-live — validate task #17 on a real login-gated site
`loginFailureDowngrade` (task #17, shipped 2026-07-16, commit `c13c7e4`) is only validated by code-review + saucedemo no-regression — a clean-auth koyal run never proposes a login-shaped milestone, so the actual downgrade-on-silent-failure branch has never fired live. Run against lambdatest / expandtesting / webdriveruniversity (all have real login-gated flows in history) and confirm a genuinely-failed login gets `needs-review`, not a false `pass`.
*(Old prose called this "task #16" on 2026-07-17 — collides with the already-shipped Slack-integration #16 from 2026-07-16. Disambiguated here as #16-live.)*

### #upload-reloop — explorer loops re-uploading despite a completed upload (15dfd41 fix not firing)
Live finding, koyal deep run 2026-07-17, user screenshot as primary evidence: the script wizard's "Upload file" step correctly showed test-script-5-second.pdf attached + Next ENABLED (upload succeeded), but the explorer kept re-asking for a file ("retry attaching via file input") instead of clicking Next. Root cause is the documented 0.00-MB-retry bug (tiny 1.6KB PDF → Koyal honestly renders "0.00 MB" → explorer misreads as "nothing attached"), which commit `15dfd41` (2026-07-13) was *supposed* to fix ("trust filename + enabled advance control over an ambiguous size once a prior upload is in step history") — so either the fix isn't firing on this path or the trigger condition is narrower than the live case. Re-verify `15dfd41`'s rule against the actual koyal upload snapshot. Vision (#6/#12) would also close it as a fallback but the deterministic rule should work first.
*(Was conflated with vision as "#14-vision" in a prior tasks.md draft; the crashed session's tracker kept them separate — this is the concrete bug, #6/#12 is the vision architecture.)*

### ✅ #6 / #12 — screenshot verification as a second signal — shipped
✅ **DONE 2026-07-21; processing/validation affirmation expanded 2026-07-22.** `LlmClient.complete()` supports native image inputs. Every real flow milestone sends its saved screenshot to a conservative visual assessment that may downgrade a pass to `needs-review`, but cannot create a hard fail or upgrade a deterministic failure. Explorer/deep-walker also use targeted screenshot affirmation for stuck controls, visible validation, and prolonged processing. No coordinate-based visual execution was added. Current build + 64/64 tests pass.

**Follow-up hardening from the beta.koyal.ai live trace:** the screenshot layer correctly saw `Generating avatar...`, but KB re-evaluation erased its `needs-review` and restored PASS. Fixed: a visual concern is now sticky until an explicit human verdict, and Explorer suppresses `done` whenever the full page still shows generation/processing, polling within the configured budget. The centralized human field resolver brings the total suite to 28/28.

The hard guardrail remains: vision is corroboration, not an autonomous pass/fail oracle. `#upload-reloop` remains separately open because its deterministic upload-state rule still failed live even though vision can now help recovery.

### #14-blankpage — blank-page recovery doesn't verify it landed on the *authenticated* page
🟡 **PARTIALLY FIXED.** Deep walks now check for a real auth gate on every state, re-authenticate through `ensureAuth`, and abort honestly if the gate remains. `core/explorer.ts` still reopens `lastRealUrl` after `about:blank` without immediately checking whether that recovery landed on a login gate; flow-runner checks often catch it later, but the generic Explorer path remains the open part.
**Local characterization 2026-07-22:** a fake protected page detached to `about:blank`, recovery reopened onto `/login`, and generic Explorer continued from that login state and returned `authentication required`; it did not invoke re-authentication. Open status confirmed.

### 👀 #auth-gate-convergence — standing watch-item, not a discrete open fix
🧪 **Characterization tests added 2026-07-20** (`src/agent/auth-gate.test.ts`, run via `npm run test:unit`) lock in the current hardened behavior of `looksLikeAuthGate` + `isLoginShapedGoal` so the next edit can't silently regress a case some past batch fixed. Deliberately NOT reworked — it's already one centralized DOM-based function with a heading-count guard, and there's no failing case to validate a rework against. **When a new failure mode appears: add a case to that test file, then fix that one case.**
Second-highest-value recurring bug source in this project's history (`normalizePath`/`matchPage` is #1). Patched in nearly every batch since ParaBank (missing accessible-name password field → expandtesting demo-page-name false-positive → automationintesting unconditional-Logout-label trust → lambdatest cross-flow contamination → webdriveruniversity heading-count false-positive + a duplicate bypass → automationintesting's bare-"login"-in-a-verify-goal misroute) with a new failure mode nearly every time. If a future site's flow keeps re-authenticating unnecessarily or silently skips a real login milestone, suspect `looksLikeAuthGate`/`hasVisiblePasswordInput`/`isLoginShapedGoal` first. Consider a structural rework rather than another one-off patch next time it recurs.

### #off-origin-replay-guard — `Explorer.achieveGoal()` has no off-origin guard during milestone execution or flow replay
Real safety gap, found 2026-07-15 on globalsqa (ad interstitial led a click off-site twice, caught only reactively) and GreenKart (a "JOIN NOW" button drove a real 3-hop redirect all the way to a live Teachable checkout page with a real order ID and **zero guard prompt raised** — `guard.ts`'s destructive-keyword floor doesn't cover join/enroll/subscribe, AND the off-origin guard that exists only lives in `crawler.ts`/`deep-walker.ts`'s exploration-time edge mapping, never in the replay/test path). Fix: port the same `isOffOrigin` guard `deep-walker.ts` already has into `Explorer.achieveGoal()`'s milestone/replay path. Until fixed, treat any payment/subscription-adjacent flow on a new site with suspicion — de-approve if in doubt (already done for GreenKart's `subscription-plan-selection`).
**Local characterization 2026-07-22:** a fake Explorer click changed `https://app.example.test/start` to `https://evil.example/checkout`; Explorer accepted `done` and returned success on the foreign origin. Open status confirmed without contacting an external site.

### #credential-field-not-detected-sensitive — unlabeled login fields can bypass protected storage
Open security issue, confirmed on filmarena.ai 2026-07-21. When an email/password input resolves only to a bare ref such as `e8`/`e12`, label-based sensitivity detection can fail and route the value through ordinary persisted field answers. Network evidence redaction is already fixed; remaining fix: mark every fill performed inside the explicit auth credential goal as sensitive regardless of accessible label, with DOM `type=password` as an additional signal.
**Local characterization 2026-07-22:** `isSensitiveFieldLabel('e12')` returned false, and a mocked auth fill with a non-matching proposed value reached `onFillRequested` with `context.sensitive === false`; the eventual step log was redacted, but the resolver had already been invoked through the non-secret path. Open status confirmed.

### #logout-guard-override — `guard.ts`'s `LOGOUT_RE` has no override path at all
Found 2026-07-15. Every other destructive-action keyword can be allowlisted `always`/`never`; a Logout click is auto-denied unconditionally. Makes any flow whose explicit milestone goal is "click Logout" structurally unpassable, permanently, on any site. Distinct from the already-fixed (2026-07-16) "logout hidden in a dropdown" discovery problem.
**Local characterization 2026-07-22:** `Guard.confirmClick('Logout')` returned false with zero human prompts even when the saved allowlist contained `always`; a normal destructive Delete control did use the prompt. Open status confirmed.

### 🟡 #koyal-fresh-project — reactive fresh-start shipped; proactive recipe-first entry remains
**Explicit required behavior (user-directed 2026-07-21): Koyal creation flows should prefer a verified button-click entry path (for example `/dashboard > New project > Start with Script/Audio`) and use direct wizard URLs only for intentional resume/recovery.** Direct `/upload` navigation is not a clean entry because Koyal can resume the account's unfinished draft, which caused the live beta.koyal.ai test to leave one wizard, inherit stale state, and misposition the next flow. Implement this as a replayable entry recipe, not a Koyal-only hardcoded click sequence; retain direct URLs for stateless pages and verified recovery.

✅ **Applied to current beta.koyal.ai state 2026-07-21:** both Script and Audio approved flows now enter at `/dashboard` and click New Project before selecting their mode. Script now continues past Edit scenes through Create Video/render and a playable/downloadable final-artifact check; Audio likewise continues through Create Video/render. General flow-proposal instructions now forbid ending video coverage at Edit scenes.

🟡 **Downgraded from 🔴 2026-07-20** — the 2026-07-20 koyal run's test-phase video flows entered cleanly via "New Project" (B) and reached `/editscene`; the cluster (`#koyal-fresh-project` + `#koyal-wizard-landmark` + `#upload-reloop`) now degrades *exploration* (symptoms still recur there) but no longer *blocks* runs. Real video blocker is S3 (external). See the Must-do ranking → 🟡.
The agnostic `learnedFreshStart` implementation (`591f400`) supports multi-step values such as `/dashboard > New project`, self-verifies the expected entry, and current beta Script/Audio state uses button-click entry. Still open: make that verified entry recipe proactive for newly discovered creation flows instead of learning it only after a resumed-draft mismatch.

### ✅ #initial-login-retry — fixed 2026-07-20
✅ **DONE 2026-07-20** — `commands/explore.ts` now retries via `attemptInitialAuth` (3× linear backoff, exported + unit-tested in `src/commands/explore.test.ts`) before continuing unauthenticated. Run `npm run test:unit`.
Found 2026-07-16. `commands/explore.ts` wrapped the pre-crawl `ensureAuthenticated()` in a try/catch that logged a warning and continued UNAUTHENTICATED on ANY failure, including a transient one (observed: an Anthropic 529). Starved the whole crawl/flow-proposal off a near-empty map (koyal run #1 = 7/5/2).

### #statement-kb-batching — statement-KB flags every item on a listing page as its own classify question
Confirmed inefficient (not incorrect) on 3+ sites (GreenKart's ~25-item catalog, lambdatest's product listings, webdriveruniversity's ~30-widget page). Always correctly classified noise so far, but a real human-in-the-loop efficiency problem. Worth batching same-shaped repeated candidates into one round-trip instead of one per item.

### #flow-runner-anomaly-tags — structured per-milestone anomaly tagging
Design discussed 2026-07-15, not built. Add a tag (`overachievement`/`session-leak`/`off-site`/`upstream-break`/`repositioned`/`crash-recovered`) separate from the pass/fail/needs-review/skipped verdict, so a report shows *why* a milestone is unusual instead of requiring someone to read six step-summaries by hand. Also needs an "is this milestone's goal already satisfied by current state" pre-check for the LLM-overachievement case (distinct from the existing fast-forward-to-a-later-page mechanism, which doesn't apply when overachievement lands *past* the last milestone).

### #koyal-wizard-landmark — Koyal's own wizard sidebar over-matches as a page landmark
Live-reproduced 2026-07-15: the `wizard-upload-file` classifier landmark (`snapshotAnyOf: ['Upload file', 'Story Type', 'Review transcript', 'Edit scenes']`) matches the wizard's persistent sidebar, visible on every step — can't distinguish "on the upload step" from "anywhere in the wizard." Drove ~12 doomed upload attempts in one run. Same "chrome landmarks must never identify a state" disease as `normalizePath`'s collisions; needs a Koyal-specific landmark fix (probably: require a step-specific element, not just a sidebar item, in the detection signature).

### #prompt-dialog-value — native `prompt()` dialogs with a required specific typed value are only partially supported
Found 2026-07-13 on webdriveruniversity. `resolveBlockingDialog` resolves the block correctly but always accepts with an empty value, so a milestone whose verification depends on the exact typed text appearing would false-fail. Needs the pending dialog's message surfaced to the LLM's own decision loop — bigger than a one-off patch.

### ✅ #f13fbbb-regression — completed by subsequent cross-site suites
The walk-choice-memory and distinct-mode proposal changes were followed by exhaustive saucedemo/Koyal validation and later multi-site runs across OrangeHRM, ParaBank, automationexercise, demoqa, and others. No regression attributable to these two mechanisms surfaced; later failures were independently root-caused. This historical regression task is closed.

### ✅ #explorer-fail-signal — closed after current-code audit 2026-07-22
`explorerFailureDowngrade` turns a clean deterministic pass into `needs-review` whenever exploration itself failed; `loginFailureDowngrade` covers the `explored === null` authentication branch; creation milestones additionally require completion actions and visual persistence. KB re-evaluation cannot erase any of those downgrades without new success evidence. Replay-only milestones are governed by the separate two-pass flow-lifecycle proof. No remaining instance of the originally reported “Explorer failed but milestone silently PASSed” path was found.

### #long-tail — accumulated low-priority / diagnosed-not-fixed items
Open: (1) **demoqa `widgets-datepicker-tabs:m2`** fails, never root-caused (2026-07-09). (2) **The 2026-07-14 idempotent-skip verification fix still lacks a direct Koyal test-phase trigger validation.** (3) SPA junk-URL pollution beyond the shipped soft-404 handling.

✅ Removed stale recycle item: `AgentBrowser.recycle()` now resolves the session PID from disk first, verifies ownership with the session socket, and refuses broad killing by default. The old all-session fallback requires the explicit unsafe override `AUTOQA_ALLOW_BROAD_RECYCLE_KILL=true`.

---

## Recently completed / shipped (context, not action items)

- **Login response convergence (`da104af`, 2026-07-21).** Login submit is sent once, awaited, and classified by its actual HTTP response; refusals are reported without resubmission, eliminating the self-induced 429 loop.
- **Session-scoped daemon recycle hardening (`942a4de`).** PID-file-first resolution plus socket ownership checks replaced the automatic broad-kill fallback; broad killing is now an explicit unsafe opt-in only.
- **2026-07-20 batch — merged and pushed (`591f400`, followed by `cd610ab`).** `#15`, noise auto-classification, agnostic `learnedFreshStart`, creation-input prompts, processing-wait no-mis-nav, per-site summaries, and LLM cost tracking are all committed. Live Koyal/filmarena evidence and the still-external Koyal S3 failure are recorded below.
- **#15 — `parseJsonFromLlm` crash-the-whole-flow bug.** Fixed 2026-07-17 in worktree branch `worktree-fix-json-parse-crash`, commit `07d33f0`, draft PR [#2](https://github.com/2006-sk/Koyal-browser/pull/2) — **merged to `autoqa` 2026-07-20 (`5943727`)**. `client.ts`: extracts every brace-balanced candidate, prefers the LAST one that parses (recovers self-correction replies, skips a stray earlier brace). `explorer.ts`: `decideNextAction` retries once on parse/call failure, degrades to one contained `fail` step instead of an uncaught throw; `LlmBudgetExceededError` still propagates. xhigh code review (`wf_48ed55b0-0d5`) found 6 real defects in the first version (credential-redaction bypass, the retry's own unguarded 2nd LLM call, stale-object extraction, wrong-brace anchoring, an idempotent-skip regex collision, silent budget double-consumption) — all fixed in the same commit. 2 lower-severity findings (unify with `proposeFlows`' own retry pattern; retry-stacking with the LLM client's internal retries) left as follow-ups, not fixed. Validated: unit tests for every extraction scenario + mocked-LLM redaction/budget tests, plus a live saucedemo run where a genuine transient "fetch failed" hit both the attempt and the retry and degraded cleanly to one contained FAIL instead of crashing the flow.
- **#11 — flow-runner no longer silently drops milestones after a mid-flow FAIL.** Shipped + live-validated both branches 2026-07-16/17. Commit `c13c7e4`.
- **#17 — login-shaped-milestone false-PASS downgrade.** Shipped 2026-07-16, commit `c13c7e4`. Code-reviewed + no-regression only — see #16-live above for the still-needed live trigger.
- **Two-step logout (opener > logout) + auto-discovery.** Shipped 2026-07-16, commit `8d99b06`. Not yet re-confirmed on OrangeHRM's collapsed-dropdown case specifically.
- **Slack product-bug reporter.** Shipped 2026-07-16, commit `8d99b06`. Live-validated (posted only the 2 real S3 bugs from a 14-fail run, correctly excluded the rest).
- **Walk choice-memory + flow-proposal mode enumeration** (the "only tests the default mode" fix). Shipped 2026-07-16, validated live on filmarena.ai and (mode-coverage generalization) on koyal's 2026-07-17 run (a distinct Audio flow got proposed for the first time).
- **Duplicate-successHint dedup / soft-404 filtering / probe-breaks-position repositioning / Koyal draft "fresh entry" prompt** (5-file diff: `crawler.ts`, `flow-runner.ts`, `page-classifier.ts`, `probes.ts`, `sitemap.ts`). Validated 2026-07-17 on fresh-wipe saucedemo (47/3/1) + koyal (28/13/6) runs — no regressions, repositioning confirmed (no probe failure cascaded).
- **Poisoned koyal flow cleanup.** The 2026-07-15 session-loss cascade wrote Sign-Up/OTP milestones into `walked-projects-list-create-yournext-video` in koyal's `sitemap.json`. Resolved: the 2026-07-17 clean-slate deep run wiped + regenerated the sitemap — verified 2026-07-17 that flow now has 5 clean milestones, no poison. (Was an open item in the crashed session's tracker; confirmed moot.)

## Known, live, external — not autoqa bugs (context only)

- **koyal-s3-scene-generation-fetch** (Critical) — Koyal backend returns an HTML error page (`<!DOCTYPE…`) where JSON was expected during scene generation ("Failed to fetch JSON from S3: SyntaxError"). Documented in `autoqa/KNOWN_KOYAL_BUGS.md`. Reproduced 2026-07-09, 07-13, 07-16, and **re-confirmed live 2026-07-20** — that run reached `/editscene` for the first time and **localized it to scene-IMAGE generation**: scene *descriptions* (text) generate fine, but the scene image/data S3 fetch fails, so no images → **no video can render**. 3 bugs auto-posted to Slack that run. Still Koyal-side (production issue, flagged to the user who is Koyal staff); no autoqa code change can render a video until it's fixed.
- **webdriveruniversity's Login Portal** genuinely cannot succeed — jQuery loaded over `http://` on an HTTPS page is blocked as mixed content, so the page's own `preventDefault` script never attaches and a native form submit always wins, reloading with an empty query string. Real site bug, not autoqa's.
- ParaBank backend has genuine intermittent concurrency/flakiness on login (same credentials succeed most of the time, fail occasionally even in single-session isolation) — not fixable client-side.
- practice.expandtesting.com's Register button is genuinely, repeatedly obstructed by a shifting ad iframe — real site accessibility issue.
