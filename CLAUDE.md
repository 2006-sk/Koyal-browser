# happyflow — Koyal QA workspace + autoqa (autonomous site-agnostic QA agent)

This file is the session brain-dump: everything a fresh session needs to be as effective as the one that built this. Last updated 2026-07-17 (external #11/#17 live validation on a 10-flow koyal deep-run found a new HIGH bug — a `parseJsonFromLlm` crash, task #15, NOT yet fixed — see the 2026-07-17 dated section below).

**Open-task tracker lives in [`tasks.md`](tasks.md), not in this prose.** Check it first at the start of any session — this doc's own ad-hoc "task #N" numbering has already collided twice (#14 and #16 were each independently reused for unrelated things by different sessions), and Claude Code's built-in task tool is session-scoped and invisible across jobs, which is how a "record it in the tracker" got lost when a session crashed on 2026-07-17. `tasks.md` is the single source of truth for open work going forward; update it directly.

## What this repo is

Browser-QA workspace for **beta.koyal.ai** (AI filmmaking app: turns uploaded scripts/audio into videos). Layout quirk: the git root is `happyflow/`, with `login/` at top level and a NESTED `happyflow/` subdir holding `audio/`, `script/`, exploration reports, and test media.

| Path | What | Status |
|---|---|---|
| `autoqa/` | **The main project**: generic autonomous QA agent CLI | Active; branch `autoqa` (commits 8b6ca3c → a8de818 pushed to github.com/2006-sk/Koyal-browser) |
| `login/` | Legacy hand-built auth QA harness (Phase 1) | Passing, superseded by autoqa |
| `happyflow/audio/` | Legacy audio-wizard harness | Passing; scenario consolidation uncommitted |
| `happyflow/script/` | Legacy script-wizard harness | Never fully passed; superseded |
| `happyflow/*.md` | HAPPY_FLOW_MAP / FULL_EXPLORATION_REPORT / PATH_PROBE_REPORT | Manual exploration ground truth |
| `happyflow/test-*` | Test media: `test-script-5-second.pdf` (known-good script), `test-narration-short.wav/.mp3` (audio), heist TXT (known-BROKEN on Koyal) | fixtures |

**Git state:** work happens on branch `autoqa` (created from `cursor/rename-logjn-to-login`). Do NOT switch back to `cursor/rename-logjn-to-login` casually — `autoqa/` files are tracked on `autoqa` but absent there, so checkout deletes them from disk. Credentials/API keys live in `login/.env` (KOYAL_TEST_EMAIL/PASSWORD, ANTHROPIC_API_KEY) — autoqa's config auto-falls-back to it.

## autoqa — what it is

Give it a URL; it logs in (asking the human in the CLI only when needed), **deep-explores** (crawls + actually enters creation flows: uploads CLI-asked files, completes required modals, waits out multi-minute processing), maps every state, auto-generates testable flows with replayable recipes, then **tests** them with QA probes. Learns page outcome statements from the human ONCE (success/failure/noise), forever. agent-browser CLI is the ONLY thing touching the browser; the LLM (Anthropic, `claude-sonnet-4-6` default) only picks which ref to click.

Core loop everywhere: **replay recipe (0 LLM) → if broken, LLM-explore (self-heals recipe) → verify deterministically (console/network/DOM) → triage unknown statements with human → persist**.

### Run it
```bash
cd autoqa
npm run qa -- run --url https://beta.koyal.ai            # explore-if-needed → test (thorough probes default)
npx tsx src/cli.ts explore --url ... --deep-flows 2 --budget 150
npx tsx src/cli.ts test --url ... --flow <id> --quick
npx tsx src/cli.ts review --url ...                       # browse/fix statements, flows, walks, recipes, allowlist
npx tsx src/cli.ts reset --url ... --all                  # wipe a site's memory
```
Flags: `--deep-flows N` `--no-deep` `--quick` `--budget N` (hard LLM-call cap) `--upload-file <p>` (force one file for all uploads) `--fresh` `--headless` `--max-pages/steps`.
NOTE: `run` skips exploration if the sitemap has pages AND approved flows — use explicit `explore` to force re-exploration (don't rely on `run` after partial state exists).

### Module map (src/)
- `config.ts` — env (AUTOQA_*), falls back to `../login/.env`; `applyCliOverrides`; deep/probes/uploadFileOverride blocks.
- `core/agent-browser.ts` — sync spawnSync wrapper per action; sessions named `autoqa-<hostname>`. FACTS: 30s default subprocess timeout (never issue one long wait — loop 5s waits); `evalScript` RETURNS stdout (eval prints the JS return value); accessibility snapshots DO NOT contain hrefs; `clickButtonByText` returns honest boolean via eval stdout.
- `core/explorer.ts` — LLM loop, one JSON action/step (click/fill/wait/upload/done/fail); label capture (`resolvedLabel/Role` from `[ref=eN]` lines) makes recipes replayable; guard hook before clicks; upload hook asks CLI for a path; loop detection; snapshot truncation is HEAD+TAIL (60/40 — Next buttons live at the end); upload selector chain incl. DOM-scanned inputs + dropzone-ref arming.
- `core/verification.ts` — deterministic verdicts (pass/fail/needs-review) from page errors, console (allowlist), blank screen, ugly-error regexes, url/snapshot includes/excludes (RegExp-capable), 5xx counts. `core/llm/client.ts` — Anthropic + OpenAI paths BOTH retry 3× w/ backoff; static callCount + budget (throws LlmBudgetExceededError).
- `core/nav.ts` — resilient click ladder (interactive-snapshot ref → full → role find → DOM text); `core/edits.ts` — fillFieldByHint ladder + snapshot echo verify, randomEditMarker.
- `agent/sitemap.ts` — **identity rules (hard-won)**: plain pages are URL-identified ONLY (PASS 2 snapshot-only matching reserved for wizard/modal/processing/terminal/error kinds — else one chrome landmark like a site header absorbs every page); PASS 0 = landmark-first for stateful kinds (same-URL wizard states); mergePage dedupes by landmark overlap same-kind. WalkTrail/WalkStep carry full `actions` sequences + verified landmarks + processingMs.
- `agent/deep-walker.ts` — the flow walker: deterministic entry click (label known) → per state: matchPage else classifyPage (landmarks filtered against PREV snapshot — wizard sidebars list every step name on every screen); kind-based handling (processing = 5s polls + screenshots + one human-approved extension; inline processing detected by spinner regex, waited once per state; error → bounded recovery; terminal → done); `via` chain re-entry for fork branches (direct URLs resume drafts); `flowFromTrail` (hints = NEXT DIFFERENT state's verified landmark; processing folds into maxWaitMs×1.5; goals include idempotency clause "if already done, just advance"); `recordWalkRecipes` (full action sequences).
- `agent/crawler.ts` — BFS **seeded with the post-auth landing page** (sites without auth redirects hide the app behind the login URL); links from live DOM eval (not snapshot); click-probes nav then unknown categories; walk entries = create/upload interactives + checkout-ish submits, wizard-step pages allowed as entry sources (with via); inventory diff → "NEW since last explore"; flows approval via CLI.
- `agent/flow-runner.ts` — per milestone: guardPhase check (poll 30s, then **replayUpTo** = rebuild position by replaying prior milestone recipes — entry alone strands you); fast-forward when a draft resumes at a LATER milestone's page; recipe replay → explorer fallback; markers only on `edit` milestones; missed successHint alone (no marker) downgrades fail→needs-review→human; KB statements auto-resolve verdicts (flip logic); probes after each passing milestone (`probe:*` steps never abort; count as needs-review in flow verdict).
- `agent/probes.ts` — optionMatrix (cached groups, settle canonical, back out if navigation), backForward (marker/landmark survives), abandonResume (leave + re-open URL), editSweep (markers echo), rapidToggle. Deterministic; capped 3/milestone.
- `agent/statements.ts` — ask-once KB: deterministic candidate extraction (new outcome-looking lines + console errors), normalize/mask → regex pattern, human classifies s/f/n, feeds expectations (failures→snapshotExcludes etc.).
- `agent/guard.ts` — destructive keyword floor (delete|pay|checkout|invite|revoke|logout...) + classifier tags; yes/no/always/never → allowlist; logout auto-denied.
- `agent/auth.ts` — stateLoad → **poll** waitForAuthenticated (SPAs hydrate slowly; probe a requiresAuth page, not the origin) → recipe replay → LLM login → OTP via CLI channel.
- `agent/interact.ts` — CLI questions: TTY readline OR **inbox file channel** (`.autoqa-state/<host>/inbox/QUESTION.txt` → write `answer.txt`); `AUTOQA_PROMPT_TIMEOUT_MS` controls wait; unanswered classify questions defer (re-ask next run).

### Per-site state (`autoqa/.autoqa-state/<hostname>/`) — gitignored
`sitemap.json` (pages/kinds/landmarks/optionGroups/edges/flows/walks), `statements.json`, `recipes.json`, `allowlist.json`, `auth-state.json`, `secrets.json` (0600), `screens/`, `walks/` (poll screenshots), `inbox/`. Reports: `autoqa/reports/<hostname>/<runId>/` — same evidence format as legacy harnesses.

### Driving it detached (how this session operated)
Launch runs with `nohup ... &` + a watcher script answering the inbox (see `/Users/apple/.claude/jobs/*/tmp/watcher2.sh` pattern): parse the QUOTED message out of classify questions (naive globbing matches the word "success" in the answer menu — that bug once stored "Epic sadface: Username is required" as success); answer files contextually (pdf question → test-script-5-second.pdf, audio → test-narration-short.wav); "still processing" → wait; approve flows → all; guard → case-by-case (`always` for saucedemo Checkout, `no` for Koyal destructive). Kill wedged browser daemons with `pkill -f agent-browser` if `open` times out.

## Hard-won lessons (the recurring villain: URLs ≠ app state)
1. **BFS maps addresses; apps are state machines.** Everything action-gated needs the deep walker; the walker is FED by the crawl, so a starved crawl starves everything.
2. **Chrome landmarks** (site headers, wizard sidebar step names) must never identify a state — filter classified landmarks against the previous snapshot; plain pages match by URL only.
3. **Drafts/state leak between runs**: Koyal's "Create Your Next Video" resumes the last draft; saucedemo keeps cart items. Cures: fast-forward to later milestones, idempotent goals, via-chain fresh entry. Remaining idea: fresh-project-per-run entry strategy.
4. **Inline processing** (spinner on the same URL) ≠ processing page — detect via text regex, wait once per state.
5. One walker "step" may span several screens (LLM overachieves) — record FULL action sequences for recipes.

## Koyal product knowledge (verified by runs)
Login page defaults to Sign Up — click "Log In" toggle, submit is "Start Creating"; auth = cookies, sessions expire fast. Script path is **PDF-only** (TXT broken: "No dialogue found"); script-edit does ~50-110s inline character/voice generation; scenes ~70-125s; final render ~10min (landmark "Generating Video...Est."); credit-upsell modal appears at style step (dismiss ✕); upload fork at `/upload` is NOT resumable (probes proved it) and resumes drafts instead of showing the fork; wizard sidebar lists all step names on every screen. Audio path: fork → plan Continue → story type → /lyricedit transcript → theme → style.

## Validation results
- **2026-07-07 night, both sites wiped (`reset --all`) and re-run from zero with two new fixes (below):**
  - **saucedemo.com**: full cycle (login learned → crawl → 2 walks to "Thank you for your order!" → 7 flows tested): **43 PASS / 12 FAIL / 9 NEEDS-REVIEW, 0 aborts, 128 LLM calls**. Walked flows 100% green incl. probes. Legit site finding (re-confirmed): browser back→forward at checkout-overview strands you on checkout-step-one with progress lost; most remaining FAILs are collateral of that probe (runner doesn't re-position after a probe breaks state — known improvement). login-authentication:m1 FAIL is cosmetic (auth module silently restores session; milestone successHint expects to *see* the login form).
  - **beta.koyal.ai**: full cycle from zero (login learned incl. Sign-Up→Log In toggle, crawl 2 pages/7 edges, 2 walks incl. script wizard with PDF upload + "Initializing script engine" inline-processing waits, 7/7 flows approved+tested): **47 PASS / 7 FAIL / 3 NEEDS-REVIEW, 129 LLM calls**. wizard-step-navigation + audio flows fully green with edit-sweep probes. FAILs: sidebar-nav-flow m2–m4 are FALSE positives — the flow proposer copied the same successHint ("Upload Content") onto every sidebar destination (pages actually loaded: /assets, /locations-list); plus back/forward probe quirks at script-wizard m2/m4 (same SPA-history class as yesterday). Final ~10-min render NOT exercised this run; MP3 parity still untested.
- Earlier same-day benchmark (pre-wipe): saucedemo 7 PASS / 0 FAIL / 9 LLM calls — that run tested only ONE walked flow on warm recipes.

## 2026-07-09 — fixes batch + 6-site validation + start of the DEEP-QA rework
**Purpose clarified (drives everything below):** autoqa is Koyal's refactor-resilience QA — every time Koyal rebuilds the site, re-map + re-test that the *platform functions* (create characters, walk scenes, edit along the path, back/forth robustness) with NO re-scripting. It is NOT judging video/image *quality*. Human-in-the-loop is BY DESIGN (no success/failure rulebook given — the agent must ask when unsure + confirm milestones, and learn permanently). See plan `~/.claude/plans/ok-what-a-ll-glimmering-teacup.md`.

**Fixes shipped this session (all build clean; NOT yet committed):**
- `agent/flow-runner.ts` A1: reposition (`replayUpTo`) after a probe breaks state + drifts. A3: login-shaped milestones drop the successHint check when auth already succeeded. Nav/state-loss probe fails (back-forward/abandon-resume) are now FIRST-CLASS flow FAILs (user wants them reported, not buried).
- `agent/page-classifier.ts` A2: clears duplicate successHints within a flow (LLM copy-paste). `agent/deep-walker.ts` B1/B2/B3: shared `actionableSteps()` (recipes can't misalign to wrong milestone), login-wall re-auth+abort at entry, `about:blank` recovery. `agent/crawler.ts` A4: soft-404 + duplicate-content skip; aborted walks retried.
- `core/explorer.ts` B1: deterministic inline-processing wait (regex tightened to progress-indicators only, try/catch, no LLM steps burned) — THIS got Koyal's script path to a rendered video. `setRedactions()` masks creds from logs/step-history.
- `core/agent-browser.ts`: `evalScript` 30s timeout. `core/verification.ts`: `maxUnexpectedNetwork5xx:0` no longer disables the check; failed error/console captures count as ambiguous not pass; bounded confirmation-hold catches async failures right after an initial pass.
- `agent/interact.ts`: `askChoice` bounded retries (no infinite-loop hang on invalid detached answers — the watcher-'always' bug).
- `agent/sitemap.ts`: `mergePage` no longer collapses distinct PLAIN pages that share one chrome landmark (fixes automationexercise 4-page collapse); landmark-overlap merging reserved for stateful kinds.

**6-site validation (clean-slate, fresh explore+test, no replay):**
- **saucedemo** 72/8/4 (full 11-flow map; 8 fails all real checkout back/forward state-loss). **beta.koyal.ai** 70/33/10 across 15 flows — **caught the REAL S3 scene-gen product bug** (5 console-error fails: "Failed to fetch JSON from S3: SyntaxError"), and the **script path reached `wizard-final-video` end-to-end**. Most other fails = successHint mismatch noise + probe state-loss.
- **automationexercise.com** 12/11/2 (only 2 real milestone fails; crawl shallow at 4 pages → over-merge, now fixed). **demoqa.com** 9/5/4 but crawl COLLAPSED to 1 page — its nav is JS-routed `div` cards with no `<a href>`, invisible to the crawler's anchor-only link extraction (deferred fix, task #26). **opensource-demo.orangehrmlive.com** 72/11/2 (real login wall `Admin`/`admin123`, 11 flows — strongest generalization: enterprise SPA + genuine auth). **parabank.parasoft.com** 21/3/1 (MPA, 9 pages, clean; BUT landing login form not detected as auth-gate → banking behind login never reached; `;jsessionid=` path param is another MPA edge).

**5-agent code audit run this session** (findings feed the rework): verification blind to delayed async failures (partly fixed); capture-failures-as-pass (fixed); statement KB `scope:'global'` + unanchored `.{0,60}?` patterns flip verdicts site-wide (Phase 3); guard bypasses (nav auto-accepts native confirm dialogs; crawler probes `unknown` unguarded; English-only keyword floor); `normalizePath` drops query+hash → hash-routed SPAs collapse; auth trusts stale/soft-expired sessions + `reset --auth` doesn't clear the live daemon.

**Honest capability read:** ~35-40% of a skilled human QA today (strong at autonomous setup/smoke/regression + real crash/console bug catching; weak at deep coverage, verdict trustworthiness, creative-surface testing). 90% is not a near-term target; the rework aims ~55-65%.

**DEEP-QA rework status (`AUTOQA_EXHAUSTIVE=true` gates new behavior, default off for safety):**
- Phase 0 (trust) ✓, Phase 1 over-merge ✓.
- Phase 2 ✓ (committed ab8ef58): exhaustive mode uncaps probes + nav-breakage first-class, AND `advanceGoal` now instructs the walker to CREATE/EDIT/select real content (create a character, edit a scene) not just click through; flow proposer asks for creation/edit milestones.
- Phase 3 ✓: human success/failure classifications now PAGE-scoped (were global — a phrase could flip verdicts site-wide); noise stays global. test-broke-vs-product-broke already handled by the hint-only→needs-review softening.
- Phase 4 ✓: crawler reports pages re-visited but no longer matched as possibly removed/changed + flags flows referencing them.
- JS-nav discovery ✓ (#26): crawler probes anchor-less clickables (cards/role=button) via findClickableCandidates + clickByText.

**Overnight rework validation (Claude acted as the real human-in-the-loop, NO watcher — answered every classify/upload/approve prompt with judgment):**
- **saucedemo exhaustive** (`AUTOQA_EXHAUSTIVE=true`, fresh): 51 PASS / 8 FAIL / 4 NEEDS-REVIEW — **0 milestone failures, 0 false capture-ambiguities**; the 8 fails are all the real checkout back/forward state-loss, now correctly FIRST-CLASS (Phase 0/1/2/3 confirmed non-regressing + working).
- **koyal exhaustive** (fresh, quick): the `create-yournext-video` flow passed m1/m3/m4/m5 and the deep walk REACHED THE CHARACTER-SELECTION STEP ("Create New Character" / "Choose your characters") — Phase 2 deep traversal proven to reach the creative surface. Verdicts clean (0 fail on completed flow).
- **A6 (MP3 parity): BLOCKED, not by format.** Koyal's audio-upload dropzone doesn't expose a file input the harness can find ("upload harness could not find a file input") — neither MP3 nor WAV attaches. A6 can't be judged until dropzone-input discovery is fixed (real autoqa gap, NOT a Koyal format bug).
- **Robustness finding → FIXED (task #27, commit 571ec2a): the agent-browser daemon WEDGES on Koyal's heavy wizard** (autoplay `<video#vid>` + multi-min processing + websockets saturate the CDP connection) after ~1 flow — every subsequent flow was aborting with `agent-browser timed out after 30000ms: open/get url`. Root cause diagnosed: persistent named daemon reused across CLI calls (killing tsx ≠ killing daemon; new runs reconnect to the wedged one); graceful `close` hangs on a dead CDP pipe so killed runs LEAK Chrome trees (19 agent-browser + 15 Chrome piled up overnight → swap thrash amplifies). **Fix:** `AgentBrowser.recycle()` force-kills the daemon+Chrome tree; `run()` auto-retries idempotent read/nav cmds once on timeout + tracks `consecutiveTimeouts`; flow-runner recycles + re-auths when a flow aborts on timeout so ONE wedge no longer cascades; cursor overlay off in exhaustive mode to cut per-action CDP traffic.
  - **VALIDATED (2026-07-09 ~03:00, Claude as human-in-the-loop, AUTOQA_EXHAUSTIVE):** koyal **41 PASS / 4 FAIL / 1 NEEDS-REVIEW** — the daemon still wedged once but recycle SELF-HEALED it and all 6 flows completed (vs 0/0/0 and 4/0/0 pre-fix). `video-wizard-from-script` 9P, `video-wizard-from-audio` 6P (**A6: MP3 uploaded + audio flow advanced 6 milestones via the "Choose files" input — MP3 parity works when a real file picker is present**; the drag-drop-only entry still blocks upload = task #28). 4 fails = 2 edit-marker/edit-sweep misses (test-side) + 2 back/forward nav state-loss (the real target bug class, now first-class). Note: env still memory-pressured (swap thrash from the night); a machine/Chrome restart further reduces wedge frequency but recycle makes it non-fatal.

## 2026-07-09 continued — D1-D7 live-sweep-driven fixes, a genuine reporting bug, and an environmental wall
User asked for "deep testing, fix all issues, perfect product" with full autonomy to run overnight. Re-ran saucedemo/automationexercise/demoqa/OrangeHRM/ParaBank fresh (`npm run qa -- run`) with Claude as the actual human-in-the-loop (no watcher), which surfaced 7 NEW concrete issues beyond the earlier rework, all fixed same session (commits `4880848`, `077bac6`, `c527340`, `1d2443d`):

- **D1** (flow-runner.ts): search-shaped `edit` milestones no longer get the random verification marker injected as the search query — a random string as a search term guarantees zero results, so "verify search results appear" always false-failed. Now instructs the explorer to use a real generic term.
- **D2** (flow-runner.ts + sitemap.ts `learnedLogoutControl`): session leak between flows in the SAME run — a flow needing an unauthenticated entry (register/invalid-login) kept landing already-logged-in from an earlier flow's auth. Generalized the ask-once pattern to SITE level: one question ("what's the Logout control?"), persisted once, reused by every flow that hits this, instead of asking (or worse, silently failing) per flow.
- **D3** (deep-walker.ts + page-classifier.ts): successHint mis-grounding, two real root causes — (a) `flowFromTrail`'s target-landmark lookahead was unbounded, so a milestone whose action didn't immediately navigate (same-page toast/modal) could walk PAST the next milestone and steal ITS landmark (observed: an "Add to cart" milestone asserting "ENTER ACCOUNT INFORMATION", a Signup page's text) — now bounded to the next milestone's own boundary; (b) the duplicate-hint dedup in `proposeFlows` was per-flow, now GLOBAL across all flows proposed in one call (catches a hint reused across unrelated flows, not just within one).
- **D4** (page-classifier.ts, real root cause found — task #26's JS-nav framing was the wrong diagnosis): demoqa's crawl collapse was NOT missing `<a href>` links (verified they exist and work fine via direct DOM inspection) — it was `matchPage` PASS 2 wrongly treating the LLM's classification `urlIncludes: "demoqa.com"` (the bare hostname, not a distinctive path fragment) as matching every URL on the domain, combined with landmarks that are actually persistent sidebar nav items. `classifyPage` now rejects any `urlIncludes` contained in the page's own hostname. Confirmed via live re-crawl: 1 page → 2 pages. **Follow-up filed (#37, unresolved):** even with this fix, a NARROWER bug remains — demoqa's crawl still converges multiple distinct nav destinations (Elements/Forms/Widgets/etc.) onto one page node after several probes, even though manually replaying the identical click+return-home sequence works perfectly in isolation. Root mechanism not found; needs step-by-step live instrumentation, not more theorizing.
- **D5** (crawler.ts): a real scope leak, introduced by the JS-nav fallback added in the earlier rework not checking navigation origin — a footer link ("OrangeHRM, Inc") led the crawler to `orangehrm.com` (the vendor's marketing site), which got classified, mapped, and turned into a TESTED flow ("marketing-trial-signup") against the wrong domain entirely. Both click-probe loops now check the landed URL's origin and refuse to map/edge off-site navigation.
- **D6** (explorer.ts): investigated the documented Koyal audio-dropzone upload blocker directly (logged into a real Koyal session, inspected the DOM) — the file input (`#file-input`, correct accept types) IS present and reliably uploadable via the existing `input[type=file]` selector once the wizard state is reached; the earlier failure was most likely a React-mount timing race, not a missing input. Hardened `tryUpload` (3 attempts, longer settle waits) and `resolveDropzoneRef` (walks up to the nearest clickable ANCESTOR when the matched dropzone text itself isn't clickable — observed structure: heading nested inside the real clickable wrapper).
- **D7** (agent-browser.ts): `recycle()` previously did a **system-wide** `pkill -f agent-browser` / `Chrome for Testing` on every wedge — killing every OTHER concurrent session too, breaking the established pattern of running multiple sites at once. Now resolves THIS session's own daemon PID via `agent-browser session info --json` (confirmed the CLI exposes this) and kills only that PID's Chrome-child tree, falling back to the broad kill only if a specific PID can't be resolved.

**A genuine, higher-severity bug found via ParaBank returning 0 PASS / 0 FAIL / 0 NEEDS-REVIEW** despite the console log showing several milestones PASS (commit `1d2443d`): `runMilestone`'s POST-verdict bookkeeping (KB statement triage, verdict-flip re-check, human escalation) calls `currentPageId()`/browser methods AFTER the milestone's own verdict is already computed and logged. If the daemon wedges in that window, the exception propagated out of `runMilestone` entirely — and since the step object is only pushed to `scenario.steps` by the CALLER after a normal return, an already-PASSING step vanished from the report with zero trace beyond the console line. This is NOT specific to any one fix tonight — it's a latent bug that could have under-reported results in ANY run, on any site, for as long as this codebase has existed. Fixed: all post-verdict bookkeeping now wrapped in try/catch, keeping the already-computed verdict on failure; `currentPageId()` itself hardened to return `'unknown'` on a browser error instead of throwing (matches how `'unknown'` is already handled everywhere else in the file). Re-ran ParaBank after the fix: 0/0/0 → **5 PASS / 0 FAIL / 0 NEEDS-REVIEW** (3 flows still aborted on daemon timeouts — see environmental finding below — but the ones that ran were now correctly reported).

**Environmental wall hit, not a code problem:** `memory_pressure` showed **8.4 MILLION swapouts** over ~20h uptime (load average 2.17). This explains daemon wedging becoming MORE frequent as the night went on, even on ParaBank — a lightweight server-rendered MPA that should never stress the browser. This is NOT Koyal-specific heavy-page load; it's the host machine itself under sustained memory pressure from many hours of continuous Chrome/agent-browser launching, degrading CDP responsiveness generally. Recycle makes wedges non-fatal, but can't fix the underlying degradation — that needs a machine/Chrome restart, which was NOT taken autonomously (a destructive/system-level action outside this session's remit).

**A self-inflicted lesson, corrected the same session:** a quick bash regex used to auto-answer routine classify prompts during a live regression run matched keywords against the WHOLE question text (including the ambient `page "account-created"` context), not just the quoted message — so on any page whose ID happens to contain a keyword like "created", EVERY message on it got misclassified (12 clearly-noise UI labels on automationexercise.com's account-created page got wrongly written as `success`). Caught mid-run by spot-checking, the live process was killed immediately to stop further corruption, and all 12 entries were hand-corrected in `statements.json` (the 2 genuinely-correct success entries — "Added!", "Your product has been added to cart." — were left alone). Lesson: any future auto-answer heuristic MUST extract and match only the text between the quotes, never the surrounding question framing.

**Honest capability re-read after this batch:** the CODE is meaningfully more trustworthy than at the start of the night — a whole class of "results silently under-reported" is closed, the daemon-wedge recovery no longer has collateral damage across concurrent sessions, and 5 more real, live-discovered bugs are fixed with evidence. But "perfect product" was not, and could not honestly be, reached — the environment itself degraded past the point where more live testing produced trustworthy signal (hence stopping the OrangeHRM/demoqa regression re-runs partway through the plan rather than pushing through for volume), and D4's follow-up + the Koyal audio-dropzone question (D6 is a strong hypothesis, not a proven fix, since the origin no-input event wasn't reproduced live tonight) remain open. Next session should start with a machine/Chrome restart before any further live autoqa runs.

## 2026-07-08 stress + detection benchmarks
- **uitestingplayground.com (click ladder + wait logic).** BLOCKER: the agent-browser–bundled **Chrome for Testing 150.0.7871.24 refuses ALL plain-http nav** with `net::ERR_BLOCKED_BY_CLIENT` (repro'd on neverssl.com/httpforever.com too; https fine). Fix: run against http sites with `AGENT_BROWSER_EXECUTABLE_PATH="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"` (system Chrome loads http; the wrapper passes env through). LIMITATION found: `run`'s crawl COLLAPSED the ~40-page challenge hub into 3 nodes (challenge pages share header/nav landmarks → same-kind landmark-overlap dedup merged them), so auto-flows were shallow nav only (24/1/1). Drove the widgets directly via the `AgentBrowser` class instead: **7/7 challenge behaviors correct** — click registers, dynamic-id (ref not id), load-delay/ajax-15s/client-delay-15s waits, hidden-layers 2nd click honestly REJECTED ("covered by #blueButton" — no false pass), overlapped fill lands. NOTE: the lost-click *fallback* only fires on CfT (which can't load http), so uitp exercised the ladder/waits, not the zombie-target fallback — that path is covered by the saucedemo runs on CfT.
- **saucedemo detection benchmark (differential vs standard_user's 43/12/9 baseline, same map/flows, `reset --auth` between users).** Method: keep standard_user's approved map+recipes, clear session, `test` as the broken user. MUST `pkill -f agent-browser` before switching users — agent-browser reuses the live session daemon and its cookies, so `reset --auth` (disk only) isn't enough; a contaminated perf_glitch run showed "session restored silently" as the prior user until the daemon was killed.
  - **problem_user: 33 PASS / 12 FAIL / 7 NEEDS-REVIEW — detection WORKS.** 3 milestones green for standard_user flipped to FAIL, all at checkout "Your Information"→Continue; "Error: First Name is required" surfaced ×20 and got KB-flagged (bike-light m3 reason literally `Snapshot should not include "error: first name is required"`). Real product bug caught: problem_user can't complete checkout.
  - **performance_glitch_user: 43 PASS / 10 FAIL / 7 NEEDS-REVIEW — wait logic ROBUST, no timing false-fails.** PASS count = standard_user baseline; "Thank you for your order" captured 4× (slow checkout DID complete). All FAILs are the user-independent back/forward SPA collateral, not slowness.

## 2026-07-07 night fixes (uncommitted on `autoqa`)
1. **`core/agent-browser.ts` — verified clicks.** agent-browser 0.31.1 (reinstalled 10:23 that day) + its Chromium 150.0.7871.24 silently LOSES trusted clicks on some pages: CLI reports success but the browser routes input to a stale/zombie page target (`/json` shows a page target frozen at the pre-navigation URL); eval/snapshot always reach the live page; hover + keyboard + JS clicks still work. `click()` now arms a one-shot in-page probe → CLI click → if probe reads 0 on the same un-navigated document, activates the element via `elementFromPoint(...).click()` (focus+activeElement fallback). Log line: `[browser] trusted click on @eN never reached the page`. Costs ~2 extra subprocess calls per click. If clicks ever look dead again: this is the first suspect — diagnose with an in-page capture listener, NOT by trusting CLI exit codes.
2. **`agent/flow-runner.ts` — login milestones route to the auth module.** LLM-proposed flows carry credential-less goals ("Log in with valid credentials") tagged kind:'edit', so the explorer typed the run MARKER into the password field ("Epic sadface: Username and password do not match any user"). `isLoginShapedGoal()` detects positive-path auth milestones → `ensureAuthenticated()` (env/secrets creds, saved session, learned recipe); negative-path goals (invalid/empty creds) still explore. Saucedemo login needs `AUTOQA_EMAIL=standard_user AUTOQA_PASSWORD=secret_sauce` env at launch or askSecret leaks KOYAL_TEST_* into it (env is checked FIRST).

## 2026-07-09 afternoon — credential-priority, click-matching, and report-loss fixes (4-site regression)
User asked to fix all open issues, test repeatedly, and keep it site-agnostic, with full autonomy. Root-caused and fixed 5 real bugs via live instrumentation (temporary debug logging added, verified, then removed each time — not left in the codebase), validated with fresh explore+test cycles on ParaBank, demoqa, and OrangeHRM. All 9 changed files build clean.

- **Task #41 (ParaBank auth-gate) — root cause was TWO bugs, not one.** (a) `looksLikeAuthGate` (page-classifier.ts) required the word "password" to appear in the accessibility-tree snapshot, but ParaBank's login `<input type=password>` has no accessible name (no label/aria-label/placeholder) — the snapshot just showed an unlabeled "textbox," so the gate was never detected and login was silently skipped for this site since it was built. Fixed with a real DOM check: `AgentBrowser.hasVisiblePasswordInput()` queries `input[type=password]` directly, threaded into `looksLikeAuthGate`'s call sites (auth.ts, deep-walker.ts). (b) Far more serious, found only by adding temporary logging of the LLM's actual `decision.value` (the console log never printed it — only the `reason` text, which looked plausible but wasn't the real typed value): `resolveCredentials`/`askSecret` checked **env vars before per-site saved secrets**, and `login/.env`'s `KOYAL_TEST_EMAIL`/`KOYAL_TEST_PASSWORD` are auto-loaded into `process.env` for **every** run regardless of target site — so ParaBank (and any other non-Koyal site) was silently submitting the user's own Koyal credentials on every login attempt, correctly rejected every time. Fixed: `askSecret` now checks saved secrets **first**; `auth.ts` no longer references Koyal-specific env names at all; `config.ts` scopes the `KOYAL_TEST_*` → `AUTOQA_EMAIL`/`AUTOQA_PASSWORD` convenience shim to only fire when the resolved target hostname actually contains "koyal". Also added: one bounded fresh retry on the first login attempt, and made an initial explore-time login failure **non-fatal** (`commands/explore.ts` now catches it, warns, and continues mapping the public surface — login is one gate, not the whole product). Registered a real ParaBank test account to validate end-to-end; final clean run: **15 PASS / 3 FAIL / 0 NEEDS-REVIEW** (from 0/0/0). The 3 fails are genuine, confirmed-via-~10-manual-reproductions **external ParaBank backend flakiness** (same exact credentials succeed most of the time, fail intermittently even in true single-session isolation — not fixable client-side) plus one browser-level `chrome-error://` blip during a probe.
- **Task #37 (demoqa page-convergence, the D4-followup that stayed unsolved for weeks) — root cause finally found via live instrumentation.** demoqa's home page has ONE wrapper `<div>` whose accessibility-tree name is the **concatenation of all 6 category card labels with no separators** ("ElementsFormsAlerts, Frame & WindowsWidgetsInteractionsBook Store Application..."). `Nav.click()`'s label-matching regex was unanchored substring matching, so searching for "Elements" matched this wrapper line FIRST (its text starts with "Elements") instead of the real `link "Elements"` element further down — and since the click resolves to the wrapper's fixed screen coordinate regardless of which label was searched for, **every one of the 6 category probes physically clicked the same spot**, landing on whichever card sat there and collapsing all 6 destinations onto one page. Fixed in `core/nav.ts`: try an exact-quoted-name pattern (`"Elements"`) before falling back to the old loose substring pattern. Verified live: demoqa's crawl went from 1-2 pages (stuck for the entire D4 investigation) to **7 correctly-distinct pages with all 6 edges pointing at their own real destinations**. Full fresh explore+test: **45 PASS / 8 FAIL / 3 NEEDS-REVIEW** — 7/8 fails are correctly-first-classed back-forward/abandon-resume nav-state-loss probes (intended design); 1 genuine milestone fail (`widgets-datepicker-tabs:m2`) left as an honest, unexplored open finding.
- **A real report-loss bug, distinct from the one fixed 2026-07-09 night.** `runFlow`'s top-level `ensureAuthenticated`/`navigateToEntry`/`applyFreshEntryHint` calls, before the milestone loop even starts, are wrapped in ONE try/catch — but on throw, `scenario.steps` was still empty (no milestone had run yet), so the flow vanished from the report with **zero** evidence instead of showing up as an explained failure. If every approved flow on a site happens to need a login that's currently failing (exactly ParaBank's case), every flow hits this same path and the WHOLE test run reports 0/0/0 — indistinguishable from "nothing was tested" even though real work happened. Fixed: the catch block now pushes one synthetic FAIL step ("enter flow (authenticate + navigate to entry)") when `scenario.steps.length === 0`, carrying the real error message, so an entry-level abort is as visible as a milestone that ran and failed.
- **D2's session-leak fix (2026-07-09 night) had a real gap, found chasing OrangeHRM's regression.** `applyFreshEntryHint`'s trigger only fired when the resolved current page id was BOTH non-`'unknown'` AND didn't match the milestone's expected guard phase — missing the case where a same-session redirect lands somewhere not yet classified (`'unknown'`) or where the site's login URL doesn't auto-redirect away from an authenticated session at all. Widened the trigger (now 'unknown' also counts as "not the expected anon page," plus a second signal: if the already-learned logout-control label is literally visible in the current snapshot, that alone triggers the check regardless of page id) and made the logout click itself **self-verifying** — click, re-check the page id, retry once, and if still stuck, log an honest warning instead of silently returning "logged out" on a no-op `optional: true` click. Root cause for OrangeHRM fully isolated this way: its real Logout control is hidden inside a collapsed user-avatar dropdown that needs an extra click to open — the ask-once mechanism only supports a single click target, so this specific site's case stays an honest, clearly-diagnosed open item (the log now explicitly explains why) rather than a silent miss. 3 fresh full re-runs all produced the same real, consistent **11 PASS / 3 FAIL / 2 NEEDS-REVIEW**, confirming D5's off-origin scope guard still holds (0 off-site edges every run) and the fails are legitimate.
- **A secondary bug found and fixed along the way (not root cause, but real):** `AgentBrowser.clickVisible`/`fillVisible` checked the raw, un-adjusted `config.showCursor` instead of the instance's exhaustive-mode-adjusted `this.showCursor` — meaning the documented "cursor overlay off in exhaustive mode to cut per-action CDP traffic" (2026-07-09 night, task #27) never actually applied to the two most-used action paths. Fixed to use `this.showCursor` consistently.
- **Task #28 (Koyal audio dropzone) — re-confirmed working, not re-broken.** Directly reached the real audio-wizard upload step (fresh `/upload` fork chooser → "Start with Audio") and confirmed the hidden `input[type=file]#file-input` (correct accept types) uploads an MP3 successfully via `agent-browser upload`, advancing to the plan-selection modal — matches D6's 2026-07-09-night finding. An attempted live automated re-validation via the existing `video-wizard-from-audio` flow was inconclusive because the SAME manual investigation left a draft on the account that the automated run then resumed into the WRONG (script) wizard — Koyal persists drafts per-**account**, not per-browser-session, so manual investigation and automated runs against the same account collide. This is real but separate from task #28 itself (already known as "draft contention," see below); getting a clean automated re-run needs a genuinely fresh account/draft.

**Honest capability note:** every fix this session came from live, instrumented reproduction (adding temporary debug logging, confirming the exact mechanism, then removing the instrumentation) rather than guessing from log text — the credential-priority bug specifically was invisible in the normal console output (which never printed the actual typed value) and would have stayed misdiagnosed as "the site's own flakiness" indefinitely without checking `decision.value` directly. When a failure looks inexplicable across many reproduction attempts, checking what's ACTUALLY happening at the lowest level (raw values, raw DOM, raw network requests) beats reasoning further from symptoms.

## 2026-07-10 — first non-anti-bot new-site validation: practice.expandtesting.com
User asked to move beyond the 7 already-tested sites and specifically wanted a target WITHOUT anti-bot protection (a direct lesson from ParaBank's Cloudflare interference). Researched ~10 candidates via WebFetch (verified live, no bot-challenge content): practice.expandtesting.com, the-internet.herokuapp.com, rahulshettyacademy.com's QAClickJet, automationintesting.online (Restful Booker Platform), globalsqa.com's XYZ Bank — user picked **practice.expandtesting.com** for its genuine functional depth (React Notes app, e-commerce bookstore, file upload, locators, a Mocha/Chai/Sinon playground, Swagger-documented REST API). Found and fixed **4 more real, generic bugs**, all via the same live-instrumentation discipline (add targeted debug logging, confirm the exact mechanism, remove the instrumentation, ship the real fix) — none of these were previously visible on any of the 7 already-tested sites, meaning **every new site so far has surfaced at least one genuinely new class of bug**, not just re-confirmed old ones.

- **A second auth-gate false-positive, inverse of ParaBank's.** ParaBank's bug (2026-07-09) was a MISSED gate (real password field, no accessible name). This site's homepage literally lists demo/practice pages named "Forgot Password Form," "OTP: One Time Password," "Secure Password Checker" — `looksLikeAuthGate`'s legacy text fallback (`snap.includes('password')`) matched these as if a real login form were present, and `ensureAuthenticated` tried to log in on a page with no such form at all. Root cause: the DOM-based `hasVisiblePasswordInput()` check (ParaBank's fix) was only OR'd in as an *additional* signal alongside the old loose text check, so the flaky path could still fire independently. Fixed in `page-classifier.ts`: when the caller CAN run the DOM check (every current call site can), it is now the *sole, authoritative* signal — the text-based fallback only applies when no DOM check was possible at all.
- **A systemic bug: 3 separate DOM-array-extraction call sites were all silently broken by the same JSON double-encoding mismatch** — found by tracing why a 90-plus-link homepage's crawl kept coming back with 0 free edges despite every manual reproduction of the identical eval script succeeding. `agent-browser eval`'s CLI wraps a returned STRING value in its OWN JSON encoding, so a page script that does `return JSON.stringify(array)` comes back DOUBLE-encoded (`"[\"a\",\"b\"]"`); the existing `stdout.match(/\[.../) → JSON.parse` pattern grabs the substring between the first `[` and last `]` WITHOUT undoing that outer escaping, leaving literal backslashes that are invalid JSON and throw — silently swallowed by the surrounding try/catch as "found nothing." This exact pattern was copy-pasted into THREE places: `crawler.ts`'s `extractSameOriginLinks` (the crawler's primary free-edge discovery — meaning **the crawler has been relying on click-probing alone, capped at `probesPerPage` (6), for its entire existence**; sites with ≤6 relevant nav links never noticed), `explorer.ts`'s `scanFileInputSelectors` (the DOM-scanned part of the upload selector chain — Koyal's file-input discovery "worked" only because ITS OTHER selectors in the chain compensated), and `agent-browser.ts`'s `findClickableCandidates` (task #26's JS-nav discovery fallback, added specifically for demoqa but never actually functional — demoqa's fix this session worked for an unrelated reason, `Nav.click`'s exact-match fix, not this fallback). Fixed once, correctly, with a new shared `parseJsonArrayFromEvalStdout()` helper (`core/agent-browser.ts`) that unwraps the CLI's outer JSON-string encoding before parsing the inner array, falling back to the old regex approach only if that fails; applied to all three call sites. Verified live: this exact site's crawl went from **1 page / 0 edges → 15 pages / 20 edges**, and the deep-walker started reaching real functional surfaces (file upload, web inputs form, locators page) it had never touched before.
- **`isLoginShapedGoal` false-positive: any milestone mentioning "password" alone was misrouted through `ensureAuthenticated`.** A perfectly generic edit milestone — "Enter a value into the Input: Password field" (testing a password-*type* widget on a practice page, unrelated to real auth) — matched the heuristic's `(enter|fill|type|submit) && (username|password|credentials)` check on "password" alone, causing the milestone to navigate away to the login/origin probe URL instead of just filling the field on the page it was already on. Fixed: the fill-based branch now requires BOTH "username" AND "password" together, or the word "credentials" alone (which by itself implies a full login attempt) — never "password" in isolation. Verified live: the milestone now correctly stays on the target page and passes.
- **A 4th false-positive, in the deterministic verification layer itself:** `core/verification.ts`'s "ugly/raw error text visible" check included `/\b500\b.*error/i` — a loose pairing that matches ANY page merely *documenting* a possible 500 response, which is completely normal content for a Swagger/OpenAPI docs page (or any status-codes practice page). This flagged a Notes-API Swagger docs milestone as FAIL purely because the page's own static documentation mentions "500" and "error" nearby. Removed the standalone pattern — a real server crash already trips the more specific `Internal Server Error` / stack-trace patterns already in the list, so this pairing added false positives without meaningfully more true positives. Verified live: the affected milestone flipped FAIL → PASS with zero other regressions (3/3 milestones now pass).

**Final validated numbers for this site:** fresh explore (15 pages, 20 edges, 7 real functional flows incl. register/login, file upload, web-input exercise, item creation, JS-test playground, and a live Swagger "Try it out" API call) + fresh test: **33 PASS / 4 FAIL / 0 NEEDS-REVIEW**. All 4 remaining fails are genuine, individually-confirmed site characteristics, not autoqa bugs: (1) the Register button is genuinely, repeatedly covered by a rotating Google AdSense iframe that shifts refs unpredictably — the explorer tried 8+ times across two full attempts and never landed a clean click, a real accessibility/testability problem with the site's own ad placement; (2) a `#google_vignette` full-page ad interstitial hijacked a back-forward nav probe mid-flow — the SAME first-class nav-state-loss detection this project already treats as a real bug, just triggered by an ad this time instead of app routing; (3) the embedded Mocha/Chai/Sinon test-runner page is heavy enough to wedge the CDP daemon (self-healed via the existing recycle mechanism, correctly reported thanks to 2026-07-09's report-loss fix rather than vanishing); (4) `recycle()`'s session-specific-PID resolution (D7, 2026-07-09) itself failed once when the daemon was wedged badly enough that even `session info --json` couldn't respond in time, falling through to the broad-kill path — harmless this run (no other concurrent sessions to collaterally damage) but a real, not-yet-closed gap in the "wedge so bad even the diagnostic call times out" case.

**Honest capability note, extending 2026-07-09's:** this is now the SECOND consecutive new-site validation where careful live instrumentation (not log-reading, not guessing) surfaced genuinely new bug classes that had been silently degrading results on every prior site without anyone noticing — this time a systemic one (the JSON double-encoding bug affected 3 separate mechanisms and had likely been suppressing crawl depth on every single site tested this entire project). The pattern holds: a fresh, sufficiently-different site is a better bug-finding tool than re-running the same 7 sites again.

## 2026-07-10 evening — parallel 3-site batch validation via worktree-isolated subagents (automationintesting.online, globalsqa XYZ Bank, bstackdemo.com)

User asked for a bigger push: pick 8 more new sites (beyond the 8 already validated across this project's history), test them in parallel, fix any autoqa bugs found live, and validate no regressions on saucedemo + koyal after a full wipe — with explicit approval gates (site list approved before testing, concurrency capped at 2-3, git pushed incrementally, batch 2/3 held for explicit go-ahead before starting). This is the first session to run genuinely PARALLEL site-testing agents (previous sessions ran one site conversation at a time) — using the Agent tool's `isolation: 'worktree'` so each site's testing+fixing happens in its own git worktree/branch, avoiding any risk of concurrent agents clobbering each other's source edits, then hand-reviewed and merged into `autoqa` afterward.

**New candidate sites researched and approved** (excluding all sites already tested in this project's history: saucedemo, koyal, automationexercise, demoqa, orangehrm, parabank, uitestingplayground, expandtesting): automationintesting.online (Restful Booker Platform), globalsqa.com's AngularJS "XYZ Bank" demo, bstackdemo.com (BrowserStack's React shop demo), ecommerce-playground.lambdatest.io, the-internet.herokuapp.com, rahulshettyacademy.com/seleniumPractise (GreenKart), webdriveruniversity.com, testpages.eviltester.com. Rejected `demo.opencart.com` — returned an HTTP 403 to a plain fetch, smelling like bot-blocking despite the site's own claims of being anti-bot-free.

**Batch 1 (3 of 8 sites) — complete.** Each site ran as an independent Agent-tool call with `isolation: 'worktree'`, fully self-sufficient: set up its own `.env`/`node_modules` symlink, ran explore+test, acted as its OWN human-in-the-loop (no watcher script — same discipline as the most-trusted historical runs), and was instructed with this project's full hard-won-lesson set (quoted-message-only classify matching, live-instrumentation-not-log-reading debugging, autoqa-bug vs. site-characteristic distinction) inline in its prompt since it starts with zero context. Final numbers: **automationintesting.online 38 PASS/9 FAIL/3 NEEDS-REVIEW, globalsqa XYZ Bank 34/4/0, bstackdemo.com 22/9/0 — 94 total PASS, 18 bugs found and fixed (6 per site), zero unexplained fails** (every non-pass individually traced to either a first-class nav-state-loss probe, a genuine site quirk, or an honestly-disclosed test-side gap).

18 bugs, by site:
- **automationintesting.online** (commits `4554977`, `00b18bb`): (1) deep-walk entry-finder regex missed booking vocabulary ("Reserve Now") — extended to `reserve|book(ing)?|schedule|enroll` (`crawler.ts`). (2) entry-finder also required a stable, parameter-free `urlPattern`, permanently excluding per-item detail pages (`/reservation/:id`) — added a `PageNode.exampleUrl` fallback, the literal URL that was classified (`sitemap.ts`). (3) `ensureAuthenticated` unconditionally navigates away to a generic probe URL even when ALREADY sitting on a real login gate — silently skipped the admin-login milestone every run while the flow still reported overall PASS; added an `alreadyOnRealLoginGate` check via the existing `isGated()` helper (`auth.ts`) — **not fully closed**, a related fast-forward-reached variant of the same false-PASS risk remains open (see below). (4) a site's `/admin` page renders a "Logout" button UNCONDITIONALLY even when logged out, poisoning the "are we already logged out" signal — gated on a real DOM login-gate check outweighing a merely-present Logout label (`flow-runner.ts`). (5) the generic `Explorer.achieveGoal()` loop had no blank-page recovery (agent-browser's page target can detach to `about:blank` mid-transition) even though `deep-walker.ts` already had this exact recovery — added a bounded (2-attempt) reopen-last-real-url recovery (`core/explorer.ts`); disclosed residual gap: doesn't help if the page is ALREADY blank at the very first snapshot of a fresh call. (6) **the file-based Q&A protocol had a stale-answer race**: `askViaFile()` checked for an existing `answer.txt` only AFTER writing the new `QUESTION.txt`, so a late/duplicate write meant for question N could be silently consumed as question N+1's answer — reproduced live (corrupted `secrets.json` to `{"email":"success","password":"admin"}`); fixed by clearing any leftover `answer.txt` before writing the new question (`agent/interact.ts`). This is systemic — the same race could misroute classify verdicts into the global statements KB — correctly flagged as the session's highest-value fix, evidenced but not live-reproduced (mechanical, doesn't touch verdict logic).
- **globalsqa XYZ Bank** (commit `fb24592`, a hash-routed AngularJS `ngRoute` SPA — CLAUDE.md's own long-flagged risk area): (1) **`normalizePath` only read `pathname`, discarding the hash entirely** — every route (`#/login`, `#/customer`, `#/manager`, …) folded onto one identity; fixed to fold the hash into identity only when it matches the `#/route` convention (excludes plain in-page anchors) (`sitemap.ts`) — live-verified via a throwaway script feeding real URLs through the function directly, then confirmed 3 genuinely distinct pages tracked. (2) **first-navigation URL loss, generic to any site**: on a totally fresh explore, `authProbeUrl()`'s fallback (`state.sitemap.origin`, when no page is known yet) discards the requested `--url`'s path/hash entirely — the very first `browser.open()` of the whole run landed on the bare origin instead of the requested entry point, and the crawler explored an unrelated marketing blog instead of the banking app; fixed by adding `SiteState.startUrl` (the untouched original `--url`) as a fallback (`site-state.ts`, `auth.ts`). (3) **dead retry-allowlist entries caused a fatal crash**: `getUrl()`/`getTitle()` invoke the two-word CLI form (`get url`/`get title`), so `args[0]` is `"get"`, not `"url"`/`"title"` — those two allowlist entries never matched anything, silently excluding the two most-called read commands from the documented auto-retry-once-on-timeout, turning one transient CDP stall into an immediate fatal crash instead of a self-healed retry (`core/agent-browser.ts`). (4) **`applyFreshEntryHint` compared against the wrong "expected page"** — same root-cause CLASS independently found by bstackdemo below, fixed differently; `milestone[0].guardPhases` describes the state AFTER milestone 1 runs, not the entry page itself, so it mismatches on any flow whose milestone 1 is a navigate-type step (6 of 8 flows on this site) — fixed to use `flow.entry.pageId` (documented in `page-classifier.ts`'s own LLM prompt as "page id where the flow starts") as the single source of truth (`flow-runner.ts`). (5) **`isLoginShapedGoal` matched a clicked control's own quoted label** (e.g. `Click 'Customer Login' to reach the customer selection screen.`) — strip quoted spans before checking for the bare "log in"/"sign in" phrase (`flow-runner.ts`); disclosed residual: an UNQUOTED variant of the same label phrasing still slips through (harmless on this ungated site, could misfire on a site with a real gate). (6) `rapidToggleProbe` assumed a 2-member toggle pair is always an in-place UI control, but on this site the pair are real navigation links — added a `stayedOnPage` check mirroring `optionMatrixProbe`'s existing equivalent (`probes.ts`).
- **bstackdemo.com** (commits `ea71378`, `90c0c60`, `334bb76`, `8666eec`, `2986266`): (1) LLM HTTP calls had no request timeout — `proposeFlows()`'s caller comment explicitly promises "a proposal failure must NEVER kill the run," but an un-timed-out `fetch()` never rejects, so that safety catch could never fire; explore hung 57+ minutes at 0% CPU; added `config.llm.requestTimeoutMs` (default 60s) to both provider fetch calls (`config.ts`, `core/llm/client.ts`) — verified against a synthetic black-hole IP, not the original hang condition. (2) clicks blocked by a covering element (agent-browser's own overlap guard, e.g. a react-select placeholder layered over its backing input) had no recovery — the existing `domActivate()` synthetic-click fallback doesn't work for react-select (it toggles on real `mousedown`, not a bare `.click()`); added `trustedClickAtRefPoint()`, a real mouse move+down+up at the ref's box center (`core/agent-browser.ts`) — live-validated twice. (3+4, one commit) **same `applyFreshEntryHint` root cause as globalsqa's #4, independently found, fixed differently** — this version kept a fallback to the old (buggy) `firstGuardPhases` comparison when `flow.entry.pageId` is unset, rather than globalsqa's clean "skip the check entirely" approach; **the merge (below) chose globalsqa's version as the base** and layered this site's OTHER, orthogonal fast-forward fix on top: the fast-forward-to-a-later-milestone logic treated the flow's OWN entry page as a valid jump target, silently PASSing an entire username/password/login-click sequence that never actually ran (`flow-runner.ts`). (5) no explorer action existed for native `<select>` dropdowns — clicking an `<option>` ref fails every time with a CDP box-model error (native select options render outside normal layout); added a new `select` action end-to-end (system prompt, executor, `AgentBrowser.select()`) (`core/explorer.ts`, `core/agent-browser.ts`). (6) the random edit-verification marker was injected into selection-shaped milestones (checkbox/dropdown/radio) that have no free-text field to type it into, guaranteeing a false-fail on the marker-presence check regardless of whether the real action succeeded — added `isSelectionShapedGoal()` as an exemption, mirroring the existing `isSearchShapedGoal`/`isLoginShapedGoal` pattern (`flow-runner.ts`). **Disclosed as NOT fixed, deliberately**: a real, proven-closeable gap where `isLoginShapedGoal` routes any "sign in" milestone through `ensureAuthenticated()`, which no-ops on public-catalog-with-gated-features sites (browsable while logged out) — manually proven the real login IS automatable end-to-end with fixes #2/#5, but left unfixed since the right fix touches `auth.ts`, a path cross-validated across many other sites, and there wasn't time to re-validate that breadth; also a real, generalizable gap where `recordVerifiedStep`'s verdict never consults the explorer's own "I gave up" signal, allowing an explicitly-failed milestone to still PASS by absence of negative signal — deferred as touching `verification.ts` core with all-site blast radius.

**The merge — 5 files had overlapping changes across the 3 branches** (`flow-runner.ts` 3-way; `core/explorer.ts`, `core/agent-browser.ts`, `agent/sitemap.ts`, `agent/auth.ts` each 2-way). Diffed every overlapping file by hand before merging (not blind `git merge`) — 4 of 5 turned out to be purely additive (different functions/lines, confirmed via `git diff --stat` then reading each diff) and auto-merged cleanly. Only `flow-runner.ts`'s `applyFreshEntryHint` was a genuine design conflict (bstackdemo's defensive-fallback-to-old-buggy-logic vs. globalsqa's clean "skip when data missing," per above) — resolved by hand: globalsqa's version as the base (confirmed via `page-classifier.ts` that `entry.pageId` is the documented canonical field, so skipping when genuinely absent is more correct than falling back to logic already proven buggy), with automationintesting's orthogonal `currentlyOnRealLoginGate` guard and bstackdemo's two other orthogonal fixes (`isSelectionShapedGoal`, the fast-forward entry-page check) layered on top. `npx tsc --noEmit` clean, CLI smoke-tested, pushed to `origin/autoqa` at `0177a19`. Worktrees + their branches deleted after merge (they're isolation scaffolding, not meant to persist).

**A real operational lesson, distinct from any autoqa code bug**: two of the three worktree agents hit repeated API connection drops mid-session (infrastructure-side, not autoqa's fault) and were resumed each time — no work was lost (all state checkpointed to disk correctly) — but their FIRST attempt at delivering a final report was silently lost each time; both agents later claimed "already reported this" when asked for status, and BOTH claims were wrong — the content had never actually reached the orchestrating session. Only re-requesting the full report explicitly, and independently cross-checking one against its on-disk `report.md`, recovered the real content. Lesson for future sessions: after any connection-drop-and-resume cycle on a subagent, treat a later "I already told you that" claim as unverified until you either get the content re-sent or check on-disk evidence yourself — don't take the agent's word for what reached you.

**An environmental lesson on load monitoring**: a background monitor watching system load/swap for genuine wedge signals fired repeatedly during this session, and on live investigation, the actual cause was almost never the test agents themselves — it was the user's own unrelated concurrent activity (a regular Chrome window with 20+ processes, Cursor IDE, Granola, a completely separate `qa-audio` ffmpeg-based E2E harness run, and one-off DMG-installer mounts for Cursor/Ollama updates that briefly pegged `diskimages-helper`). One spike hit load average 255 — traced entirely to that combination, not to autoqa. Lesson: a load-average or swap-delta alert tells you THAT something is under strain, never WHO — always check actual top-consumers (`ps aux | sort -rk3`, matching Chrome-for-Testing user-data-dirs against known sessions) before concluding it's your own work, let alone reacting to it. Also found and cleaned up one genuinely orphaned agent-browser "default" session (`PPID=1`, i.e. its controlling process had already exited) unrelated to any of the 3 active sites — confirms `agent-browser session list` is the reliable way to enumerate what's actually yours vs. stray.

**Disk space finding, not yet actioned**: at one point only ~7.3GB free out of 228GB — a tight margin that plausibly contributed to the session's instability independent of memory pressure. ~826MB of old installer DMGs sitting in `~/Downloads` (Cursor, Ollama, Clean-Agent, anytrans, steam) identified as safe, easy reclaimable space once those installs are confirmed done — flagged to the user, not deleted without explicit go-ahead each time asked.

**Status at time of writing**: batch 1 done, merged, pushed. Batches 2 (`ecommerce-playground.lambdatest.io`, `the-internet.herokuapp.com`, `rahulshettyacademy.com/seleniumPractise`) and 3 (`webdriveruniversity.com`, `testpages.eviltester.com`) are researched and approved but explicitly NOT started — user asked to be asked before each. Final validation (full wipe + fresh saucedemo + koyal run) not yet started. A `code-review` workflow (xhigh effort) was launched against the batch-1 merge diff specifically to scrutinize the hand-resolved `flow-runner.ts` conflict — result pending as of this writing, check for it before assuming the merge is fully clean.

## 2026-07-10 night → 2026-07-11 — xhigh code-review of the batch-1 merge (15 fixes) + batch-2 (3 more sites, 13 bugs)

**Part A: an xhigh `/code-review` workflow was run against the batch-1 merge diff (5d63bb2..0177a19)** — 6 finder agents + 1 cleanup sweep found 25 candidates, an independent verifier checked every one, 0 refuted, deduplicated to 15 distinct confirmed/plausible issues. This is the first time this project ran an *adversarial review of its own fixes* rather than only live-testing them — and it worked: several of batch-1's fixes turned out to only partially close the bug they claimed to.

Most severe finding: **`trustedClickAtRefPoint()` (added in batch-1 to click through react-select's own placeholder layer) never checked exit codes or verified which element was actually activated** — meaning it could silently report success after clicking through a genuine blocking overlay (ad iframe, cookie-consent banner, modal) instead of the intended target, exactly masking the class of obstruction this project has repeatedly documented as a real, honest FAIL (practice.expandtesting.com's ad-obstructed Register button). Fixed: exit-code checks added, plus a live DOM check that refuses the coordinate-fallback when the covering element is an `<iframe>` or covers >50%/30% of the viewport (heuristic — a same-widget layer like react-select's placeholder is small and not an iframe; a real overlay usually is one or the other).

Other confirmed issues, several in the "the fix didn't fully close its own bug" category: `isLoginShapedGoal`'s credentials-branch still tested the RAW goal (not the quote-stripped one) despite the fix's own comment claiming otherwise; the same function's quote-stripping regex could mis-pair a stray possessive apostrophe with a later real quote, corrupting unrelated text; `isSelectionShapedGoal` matched the bare word "option" and skipped the verification marker even on compound goals with a genuine free-text field; `normalizePath`'s new hash-route branch skipped the id-masking regexes applied everywhere else, so id-in-hash SPAs still page-exploded; `rapidToggleProbe`'s new "stayed on page" check treated an unclassified `'unknown'` page as "stayed," reproducing the exact false-fail the fix targeted; a `select` explorer action was wired into single-milestone recipe recording but silently dropped by the deep-walker's *separate*, parallel recipe recorder; a pinned per-item crawl entry URL could go stale (item deleted/renumbered) with no fallback; an empty (not just missing) `AUTOQA_LLM_TIMEOUT_MS` env var coerced to `Number('')===0`, aborting every LLM call instantly; the LLM retry loop used the same fixed timeout on all 3 attempts, so a genuinely slow (not stalled) call failed identically every time; a "reserve/schedule/enroll" regex addition was missing the `\b` word-boundary its sibling "book" alternative got in the same line (`"Preserve My Settings"` false-matched); a fast-forward guard over-corrected and could block a *legitimate* later-milestone resume, not just the original false-PASS case; and `applyFreshEntryHint`'s empty-`entry.pageId` early-return silently disabled draft-resume detection entirely, where the old code had at least a weaker fallback. All 15 fixed in commit `c979e4b`.

**Verification lesson, directly actionable for any future fix-a-finding pass:** re-testing the fixes with hastily-borrowed illustrative examples from the review findings themselves produced 2 false alarms — the reviewer's own example for the quote-mis-pairing bug used a QUOTED "Sign In" as the thing that "should" match, which actually conflicts with the *separate*, intentional design (quoted clicked-labels are deliberately excluded, that's the whole point of the original quote-stripping fix). Constructing a cleanly-isolated standalone repro (an unrelated stray apostrophe + a genuinely UNQUOTED login phrase elsewhere in the string) confirmed the actual claimed mechanism was fixed correctly. Lesson: when verifying a fix against a review finding, isolate the ACTUAL confirmed mechanism in your own repro rather than copying the illustrative example verbatim — illustrative examples can themselves conflate two different things.

**Part B: batch 2 (3 more new sites, same worktree-isolated parallel-subagent process as batch 1).** Sites: **the-internet.herokuapp.com** (18 PASS/10 FAIL/2 NEEDS-REVIEW, 6 bugs, commits `19bebf3`+`e12afc3`), **rahulshettyacademy.com's GreenKart** (45 PASS/0 FAIL/0 NEEDS-REVIEW — clean sweep, up from 34/4/0 pre-fix, 2 bugs, 1 commit `4caf437`), **ecommerce-playground.lambdatest.io** (34 PASS/5 FAIL/2 NEEDS-REVIEW reported, but the agent itself caught and disclosed that 1 of the 34 is a KNOWN false-pass its own fix doesn't cover — see below; 3 bugs, 4 commits `bc8c78a`/`9e2f27d`/`8e795ef`/`6588544`). **13 bugs total.** All 3 branches merged into `autoqa` with ZERO conflicts this time (unlike batch-1's 3-way `flow-runner.ts` clash) — every change landed in a different function/section; pushed at `9dc0678`.

Standout bugs:
- **A genuine safety issue, not just a test-quality gap**: `crawler.ts` already has a same-origin guard on its own click-probing, but `deep-walker.ts`'s state classifier had NO equivalent — a deep-walk entry on rahulshettyacademy.com's marketing site followed a multi-hop chain off-site to a REAL third-party checkout page (`sso.teachable.com`) and was about to sit through a 300-second processing wait for someone else's real payment flow before the human-in-the-loop agent caught it mid-run. Fixed by porting the same `isOffOrigin` guard into `deep-walker.ts`'s `identify()`.
- **Another page-identity-collapse bug, same conceptual class as the hash-route one, third variant**: `normalizePath` dropped the QUERY STRING entirely, and legacy front-controller MPAs (OpenCart, phpBB, MediaWiki-style — route through one bare pathname like `/index.php` and encode the real page in `?route=...`) collapsed their ENTIRE multi-page app onto one node (`1 pages, 4 edges` on a real e-commerce catalog/cart/checkout site). Fixed with a `maskQueryValue()` + query-folding block mirroring the existing hash-route handling. Between hash-routing (globalsqa), query-string routing (lambdatest), and trailing-slash inconsistency (the-internet), **every one of the three major "how does a URL encode page identity" schemes has now independently broken `normalizePath`/`matchPage` on a real site** — this function has been the single highest-value bug source across this entire project's history.
- **A whole "trailing-slash" bug family, 4 call sites, same root cause**: the-internet.herokuapp.com 404s on a path missing its required trailing slash, but `normalizePath` strips trailing slashes for identity purposes and several places (deep-walk entry builder, flow-runner's stale-URL fallback, the re-explore queue seeder) reconstructed URLs from the stripped pattern instead of the concrete URL that actually rendered. A deeper sibling: `matchPage`'s plain-page identity is URL-PATTERN-ONLY, never content-verified, so a broken slash-less URL that still "matched" the pattern got trusted and `identifyCurrentPage` blindly overwrote the good `exampleUrl` with the broken one — poisoning the very field the other fixes depend on. All fixed together by preferring `exampleUrl` over pattern-reconstruction everywhere, plus a soft-404 check before trusting a URL match.
- **A silent-PASS bug that survived its own live-discovered follow-up regression, in the same session**: verification only ever consulted deterministic page-health signals, never the explorer's OWN "I gave up" report — a milestone where the explorer exhausted its step budget without confirming the goal still recorded PASS. First fix (commit `9e2f27d`) added a downgrade to `needs-review` when this happens. Live-testing that SAME fix immediately surfaced a second bug: a pre-existing post-verdict "flip-check" re-ran the same already-clean deterministic signals and flipped the fresh `needs-review` right back to `pass`, erasing the fix on the spot (`[flow] verdict flipped needs-review → pass` observed firing right after the downgrade's own log line). Second fix (commit `6588544`, same session) required a genuinely NEW signal, not a bare re-check, to flip back to pass. **Honestly disclosed residual**: this whole downgrade mechanism only covers the direct-explorer code path — login-shaped milestones route through `ensureAuthenticated()` instead, whose failures are only logged, never fed into the same downgrade, so `account-login-gate:m2` on lambdatest still shows a false PASS underneath a failed login attempt. Deliberately NOT fixed this session (diminishing marginal value fixing the same bug class twice in one pass; needs a fresh full re-run to live-verify, which a completed run can't retroactively provide) — flagged with a one-line fix direction for whoever picks this up next.
- **Cross-flow contamination, a new bug CLASS this project hadn't hit before**: on lambdatest, flow #4 (entry = the unauthenticated home page, no login involved at all) prompted for login credentials it didn't need, because the browser was still sitting on OpenCart's checkout "Account" step — left there by the PREVIOUS flow's completed checkout, which legitimately renders an optional login form as one choice alongside guest checkout. `ensureAuthenticated`'s "trust the current page if it looks like a real login gate" shortcut (itself a batch-1 fix, for a *different*, legitimate case — a login-shaped MILESTONE that deliberately navigated to a real gate first) doesn't distinguish "I navigated here on purpose" from "an unrelated prior flow happened to leave this behind." Fixed with a new `trustCurrentGate` option on `ensureAuthenticated`, defaulting to the old (still-correct) behavior for milestone-level calls, explicitly `false` for the generic per-flow-start call.

**A real operational incident this batch, not a code bug**: the-internet.herokuapp.com's own crash-resilience fix (recycling the daemon after a native-dialog wedge on `/javascript_alerts`) triggered `recycle()`'s broad-kill fallback for real — this killed a DIFFERENT, concurrently-running agent's browser daemon (GreenKart's session) as collateral damage. This is the exact "not yet hit" risk flagged after 2026-07-09's D7 fix, now confirmed live for the first time. GreenKart self-healed via its own recycle mechanism and finished cleanly — but this is a real, now-confirmed gap in `recycle()`'s session-specific-PID resolution (falls through to the broad, all-sessions kill when it can't resolve a specific PID fast enough), still not fixed, now with a real reproduction case to work from next time someone picks it up.

**A batch-2-specific process lesson worth remembering**: mid-batch, the orchestrating session (Claude) launched 2 of 3 batch-2 agents on a too-loose reading of a follow-up instruction ("start next open task") that did not actually name or reference the EXPLICIT confirmation gate the user had set for batch 2 specifically, after batch 1's severe resource strain. The safety classifier correctly blocked the 3rd launch attempt on this basis. Lesson for any future multi-step task with a user-set approval gate: a generic "keep going" instruction does not automatically lift a SPECIFIC, previously-stated confirmation requirement — the gate needs to be explicitly and unambiguously addressed before proceeding, not inferred from adjacent context. (The user then explicitly approved continuing, so no real harm resulted this time — but the near-miss is worth the note.)

## 2026-07-13 — batch 3 (webdriveruniversity.com, testpages.eviltester.com, 14 bugs) + a regression-testing gap analysis

**Batch 3, same worktree-isolated parallel-subagent process as batches 1/2.** 2 sites this time (the last of the originally-approved 8-site batch-2/3 list), each agent fully self-briefed (fresh context, no memory of prior batches beyond what was written into its prompt), acting as its own human-in-the-loop end to end.

- **webdriveruniversity.com**: 44 PASS/3 FAIL/1 NEEDS-REVIEW, 8 bugs, 7 commits (`eeb502e`→`0ad05a1`). Its ~30-widget "AI Testing Playground" demo page was an unusually effective bug magnet: (1) `looksLikeAuthGate` false-positived on the page itself (a real `<input type=password>` widget + several "Login"-labelled buttons sitting among ~20 unrelated demo cards aborted 2 full deep-walk entries) — fixed with a heading-count safeguard, empirically calibrated (real gates: 0-2 headings; this false-positive page: 28). (2) **No explorer action existed for keyboard-only submission** (search/todo/tag inputs with no submit button — `fill` can only set a value, never dispatch a keypress) — added a full `press` action end-to-end through both recipe recorders (a prior `select`-action addition was once dropped by wiring only one recorder — this time both were updated together). (3) Native alert/confirm/prompt dialogs were completely unhandled on the core `Explorer.achieveGoal()` loop and unsafely blind-accepted (bypassing the destructive-action guard) on the `Nav` path — added `dialogStatus()` + a shared `resolveBlockingDialog()` gated on the same keyword floor. (4) **A systemic reporting bug closed**: `recordVerifiedStep`'s only caller never threaded `explorerSteps` through, meaning **every `step-summary.md` on every site this project has ever tested has always rendered "No LLM explorer steps recorded,"** regardless of what actually happened — pure plumbing, now fixed. (5) A sibling bug: post-verdict mutations (explorer-failure downgrade, KB flip, human escalation) correctly fed the aggregate report but never patched the already-written per-step file, leaving it permanently stale — added `patchStepSummaryVerdict()`. (6) A demo widget with buttons literally labelled "Success"/"Error"/"Timeout" was having its own button labels misread as reported outcomes by the statement-KB extractor, retroactively flipping an already-passing milestone to fail — fixed by excluding any line carrying a control role. (7) **A second, cruder duplicate of the SAME auth-gate false-positive** was living unpatched in `flow-runner.ts`'s mid-flow retry path (a bare `/log ?in|password/i` regex bypassing the just-fixed `looksLikeAuthGate` entirely) — replaced with the hardened check. (8) **A live-reproduced daemon wedge that caused `recycle()`'s previously-flagged broad-kill fallback to fire for real a 3rd time**, hitting the OTHER concurrently-running batch-3 worktree (eviltester) as collateral damage (self-healed, no lasting harm, but the underlying gap is now reproduced 3 times, still unfixed) — root cause was `Nav.afterClick()` calling `wait()` *before* resolving a blocking dialog, so the resulting exception skipped dialog resolution entirely and wedged every subsequent click attempt; fixed by resolving first and wrapping the wait.
- **testpages.eviltester.com**: 43 PASS/2 FAIL/0 NEEDS-REVIEW, 6 bugs, 6 commits (`70fc0e3`→`d091932`). (1) **A 4th distinct failure mode of the project's highest-value recurring bug** (page-identity collapse, `agent/sitemap.ts`): the site's `/apps/` index page legitimately owns that path as its own distinctive URL fragment, but `matchPage`'s substring-inclusion then matched every deeper sibling app URL under it too (`/apps/triangle`, `/apps/basiccart`, `/apps/button-calculator`, ...) — 6 genuinely distinct micro-apps collapsed onto one node, helped along by a persistent sidebar nav that trivially satisfied the snapshot-corroboration check too. Fixed by rejecting a `urlIncludes` that's just a restatement of the page's own normalized path; crawl went from 7 pages to 20+. (2) The exact same auth-wall false-positive as webdriveruniversity's #(7) above, independently discovered and independently fixed with the identical mechanism (see merge note below). (3) **LLM-proposed flow `entryPageId` isn't validated against its own `entryUrl`** — confirmed live 5 times across 2 runs, the LLM's free-text page-id guess didn't match the concrete URL it was paired with, causing `navigateToEntry` to "correctly" (but wrongly) treat the real target URL as stale and fall back to an unrelated page. Fixed by deterministically correcting `entryPageId` against `entryUrl` right after parsing. (4) A milestone whose goal specifies an exact required literal value (a length-validation micro-app) had the usual random verification marker injected on top, creating a self-contradictory instruction — added `isLiteralValueShapedGoal`, broadened once already after a second LLM phrasing of the same flow slipped past the first, narrower regex. (5) **The explorer's decision loop is structurally blind to non-interactive result text** — it only ever reads `snapshotInteractive()` (a deliberate cost/size tradeoff), so a goal whose success signal is a plain, non-interactive `<p>` (a computed "Equilateral" result) could never be recognized no matter how many retries, aborting as falsely "stuck." Fixed narrowly: one bounded full-snapshot recheck only in the "about to abort as stuck" path, which can only prevent a false abort, never introduce a false pass.

**The merge**: 3 overlapping files (`page-classifier.ts`, `flow-runner.ts`, `core/explorer.ts`). `page-classifier.ts` and `explorer.ts` auto-merged cleanly — both branches touched different, non-overlapping functions in each. `flow-runner.ts` had one genuine conflict, and it's a good sign, not a bad one: **both agents independently found the same root cause and wrote the identical functional fix** (the `looksLikeAuthGate(...)` call replacing the old bare regex) — only their explanatory comments differed, referencing their own site's repro. Hand-merged into one comment crediting both discoveries. `npx tsc --noEmit` clean. Pushed at `a8de818`.

**A regression-testing gap analysis, prompted mid-batch by the user asking "how well was regression testing actually done between batches."** Checked git history directly rather than reasoning from memory: `agent/flow-runner.ts` had taken **14 separate fix commits**, and `agent/sitemap.ts`/`core/explorer.ts`/`agent/deep-walker.ts`/`core/agent-browser.ts` each 3-6, across batch 1, the xhigh code-review, and batch 2 — all touching the exact shared code (crawl identity, auth-gate detection, click execution, verdict computation) that saucedemo's and koyal's original baseline numbers depend on — with **zero executed re-validation against either site since 2026-07-09**, before any of this started. What actually happened between batches was never true regression testing: each fix was live-verified only against the one new site that found it, plus one static (non-executed) adversarial code-review after batch 1. The user pushed back with a fair counter-point — if batch-1 bugs don't reproduce in batch 2/3, isn't that itself a regression signal? Answer, after checking specific mechanisms: **partially, and it's mechanism-dependent, not a blanket guarantee.** `normalizePath` (hash-route → query-string → trailing-slash fixes, batches 1-2) hit zero new variant in batch 3 — looks genuinely converged. `trustedClickAtRefPoint` hasn't resurfaced since the xhigh review hardened it. But **auth-gate detection has been patched in literally every batch** (ParaBank → expandtesting → automationintesting → lambdatest → webdriveruniversity ×2 this batch) and still isn't closed — the same disease, a new symptom nearly every time. And `recycle()`'s broad-kill collateral damage is the clean counter-example: explicitly flagged as a known, deliberately-unfixed gap, it has now fired for real in 2 of the last 2 opportunities where its trigger condition actually repeated (batch 2's the-internet→GreenKart, batch 3's webdriveruniversity→eviltester) — meaning "no recurrence" elsewhere may often just mean the trigger conditions haven't repeated, not that the fix generalized. **Net: raises confidence specifically on generic mechanisms that new sites happen to exercise, says nothing about the original 8 sites' own idiosyncratic flows (Koyal's wizard, saucedemo's checkout state machine, ParaBank's transactions), which no new site reproduces by construction.**

**Decided next step, before any batch 4**: a real regression suite — replay/re-run all ~13 previously-validated sites on top of current merged code, prioritized as saucedemo + koyal first (most detailed historical baselines to diff against — the original "final validation" ask, still gated on explicit go-ahead), then the other 6 original sites. Auth-gate detection and the `recycle()` collateral-damage gap are flagged as the two mechanisms most likely to still be actively broken, not just unconfirmed.

## 2026-07-13 night → 2026-07-14 — regression suite kickoff, a real orchestration incident, and 3 more live-found fixes via an exhaustive-mode saucedemo/koyal re-run

**Started task #9** (the regression suite decided above) via a Workflow script chunking all 14 sites (saucedemo+koyal first, then 4 chunks of 3) with strict no-fixing instructions — this was meant to be a pure observation pass to get a clean regression signal, distinct from every previous batch's find-and-fix mandate.

**A real orchestration bug, not a hypothetical one.** Chunk 1 (saucedemo, koyal) hit a transient connection drop, got auto-resumed by the harness — but the workflow's `parallel()` call treated the resumed agents as settled and advanced to chunk 2 anyway, stacking **5 concurrent sites** (load average 43-50) instead of the intended 2. Caught via direct `ps`/`agent-browser session list` inspection (not from the workflow's own reporting, which had no visibility into this). Fixed by stopping the runaway workflow, killing chunk 2's 3 sites (minimal time lost) and their orphaned agent-browser/Chrome daemons via `agent-browser --session <s> session info --json` → targeted PID kill (not broad pkill), leaving chunk 1 alone. Chrome footprint: 79 processes/5 sites → 25/2. **Switched approach for the rest of the session**: drove saucedemo/koyal directly (Bash + Monitor, no Workflow subagents) rather than risk the same resume/parallel() interaction again — this turned out to be the right call, since it kept me, not a delegated subagent, checking live browser state before every ambiguous verdict for the rest of the night.

**A methodology gap, caught by the user, not by process**: the first saucedemo/koyal run (default mode, no `AUTOQA_EXHAUSTIVE`) produced 28 PASS/8 FAIL/1 NEEDS-REVIEW for koyal — shallow, stopping at "upload script." The user asked directly why it never did a deep walk. Checked the actual command used (verified via the workflow's own transcript, not assumed): plain `npm run qa -- run --url ...`, no exhaustive flag — but the historical baseline being compared against (41 PASS/4 FAIL/1 NEEDS-REVIEW) was captured *with* `AUTOQA_EXHAUSTIVE=true`. Apples-to-oranges, self-inflicted. Re-wiped and re-ran both sites with `AUTOQA_EXHAUSTIVE=true --deep-flows 3`, this time directly supervised end-to-end.

**Live-diagnosed and fixed 3 real bugs during the exhaustive re-run**, all via direct browser inspection before answering ambiguous verdicts (never guessing from the question text alone):

1. **The explorer retried an already-successful upload forever** (`core/explorer.ts`, commit `15dfd41`). A genuinely tiny (1.6KB) test PDF makes Koyal's own file-size display legitimately round to "0.00 MB" — not a cosmetic bug, real math. The explorer had no rule for recognizing that retrying an identical upload can never change that number, so it kept re-issuing "upload" every time it saw the ambiguous size next to the filename, looping 4+ times even with an enabled "Next" button sitting right there. Confirmed the upload had genuinely succeeded by invoking the real button's `.click()` handler directly via `eval()` (the CLI-level click didn't register for *manual* intervention — a separate, already-known click-loss quirk whose fallback lives in the wrapped browser methods, not the raw CLI) — the wizard advanced cleanly once clicked. Added a rule: trust the filename + enabled advance control over an ambiguous size readout once a prior upload attempt is already in the step history.
2. **`replayUpTo` stranded a flow at its own entry on its very first pass** (`agent/flow-runner.ts`, commit `087a384`) — the actual root cause behind 5 separate "explorer exceeded max steps" escalations that had been individually triaged live before the pattern was obvious enough to investigate. `replayUpTo()` always navigates back to a flow's entry, then replays each *prior* milestone's recorded recipe — but silently skips any milestone with no recipe yet. On a freshly-proposed flow's first run, none exist. So the moment a later milestone's `guardPhases` doesn't precisely match the live page classification (a naming/timing mismatch, not a real off-track condition), the off-track branch fires, navigates all the way back to entry, then skips every intermediate step — stranding the browser at square one instead of wherever the previous milestone's own actions had actually, correctly left it. Explains exactly why the explorer's own action logs kept re-deriving login→cart→checkout from scratch before ever attempting the milestone's real goal, reliably exhausting the step budget before reaching it. Fixed with `hasAnyPriorRecipe()`: only call `replayUpTo` in the guard-mismatch branch when at least one earlier milestone actually has something to replay; otherwise log the mismatch and proceed from the current, real position.
3. **Literal test values weren't checked against visible field-format constraints** (`core/explorer.ts`, commit `f48b24a`). Two independently-proposed koyal flows each specified an exact literal character/asset name containing a hyphen (`QA-Char-7788`, `QAChar-Zephyr`, `QAAsset-Prism`) to type verbatim — the field itself visibly says "Only letters are allowed. No spaces, numbers, or special characters," and Koyal correctly rejects all of them; the item never gets created, the milestone genuinely fails. Root cause isn't the generic marker-injection system (already exempted via `isLiteralValueShapedGoal`) — it's the flow-proposal step inventing a plausible-looking test name without ever having interacted with the real form to discover its validation rule. Fixed: for NAME-like fields with an exact literal value in the goal, if the snapshot shows a visible format constraint the value would violate, adapt the value (strip disallowed characters) instead of typing it verbatim and letting the site reject it.

**A 4th bug found and diagnosed, deliberately left unfixed pending the next go-ahead**: verification doesn't respect a *legitimate* explorer skip. When `achieveGoal()` correctly recognizes an edit milestone's target is already done (idempotency: "already done, just advance" — working exactly as designed) and calls `done` without ever typing the injected marker, the deterministic verification layer still checks the final snapshot for that marker and fails the milestone for not finding text that was *never supposed to appear*. Live-reproduced twice (`walked-projects-list-create-your-next-video:m5`, `walked-characters-list-new-character:m2`) in the same koyal run. Distinct from the earlier "marker survives navigation" gap (that one is about the marker being typed then navigated away from; this one is about the marker never being typed at all because skipping was the *correct* behavior).

**Final exhaustive-mode numbers**: **saucedemo 27 PASS/3 FAIL/4 NEEDS-REVIEW** — 3 fails are the known nav-state-loss probe class, all 4 needs-review trace directly to bug #2 above (pre-fix, this run predates the patch). **koyal 45 PASS/9 FAIL/3 NEEDS-REVIEW** — genuinely beats the original 2026-07-09 baseline (41/4/1) on pass count. Every non-pass explained: known probes, the plan-modal step-budget squeeze (a legitimate required "Select Your Plan" modal — Standard/Pro radio + disabled Continue — not previously documented, possibly new UI since the last deep validation), bugs #3 and #4 above, one infra-flake (a daemon wedge mid-run triggered `recycle()`'s broad-kill fallback for a **4th confirmed live occurrence** — no collateral damage this time since saucedemo had already finished), and a real Koyal backend bug reproducing again ("Failed to fetch JSON from S3" / "Failed to fetch data" console errors during scene generation — the same confirmed product bug from 2026-07-09, genuine, not autoqa's fault).

**The audio-path question, finally resolved — and it reverses the shallow run's own finding.** The very first (shallow, non-exhaustive) run's option-matrix probe had reported "Start with Audio... not clickable" on the `/upload` fork chooser. In this exhaustive run, the same probe hit the same fork chooser and reported **"all options selectable"** — both Script and Audio. The "issue" it flagged instead ("an option click navigated away from the step") is a probe-design mismatch, not a functional bug: `optionMatrixProbe` expects togglable filters, but this fork's options are real navigation triggers by design. Read together: the original "not clickable" finding most likely was a flaky, transient repro (a stray click-loss moment), not a persistent product or autoqa bug — never independently reproduced despite direct investigation earlier this session (including a live login + manual DOM inspection that found the button genuinely enabled). Every other deep-walk/flow entry across both runs still chose Script every time (once even hardcoded into a flow's own milestone-1 goal text), so Audio's *deep* pipeline (past the fork) remains unexercised this session — the fork itself being clickable is confirmed, what's beyond it on the Audio side is not.

**Status at end of night**: 3 real bugs fixed + pushed (`15dfd41`, `087a384`, `f48b24a`), 1 more diagnosed and reported but deliberately not fixed (verification-after-legitimate-skip), task #9 (the other 12 sites) not yet started. Next session should decide: fix the 4th bug first, or proceed straight to the 12-site sweep with what's already shipped.

## 2026-07-14 night → 2026-07-15 — the "fix all three" priority list, an xhigh code review, and the 15-site regression suite (IN PROGRESS — check for a completion note before trusting final numbers)

User asked to fix CLAUDE.md's own top-3 prioritized candidates from the prior session's close, in order: (1) `recycle()` broad-kill collateral damage, (2) auth-gate detection structural rework, (3) verification not respecting a legitimate explorer skip. Explicit sequencing: fix + validate all three first, THEN an xhigh code review (gated on explicit go-ahead), THEN the 13-ish-site regression suite (not separately gated — "obviously" the next step). All three phases are now done or in progress; nothing has been committed to git yet (5 files modified, uncommitted on branch `autoqa`, pending user go-ahead).

**Fix #1 — `recycle()` broad-kill collateral damage (`core/agent-browser.ts`).** Root cause: the old fallback (when session-specific PID resolution failed/timed out) killed EVERY agent-browser session on the machine — confirmed live 4 times across project history. Fixed with a session-ownership check that doesn't depend on the wedged daemon's own responsiveness: `resolveDaemonPid()` reads `~/.agent-browser/<session>.pid` first (falling back to `session info --json` only if the file is missing/unreadable), then `pidOwnsThisSession()` cross-checks BOTH that the PID's command line contains "agent-browser" AND that `lsof -p <pid>` shows it holding open the path to `~/.agent-browser/<session>.sock` (each daemon holds its own socket file open — a direct, reliable ownership proof). A second ownership re-check runs immediately before the actual kill (TOCTOU mitigation — the PID could theoretically be recycled by the OS between resolution and kill). The old broad-kill is now OPT-IN ONLY via `AUTOQA_ALLOW_BROAD_RECYCLE_KILL=true`; by default, an unresolvable PID just logs a warning and returns `false` (`crawler.ts`'s catch block now checks this return value and aborts the crawl cleanly instead of looping to a timeout on every remaining page — a code-review follow-up, see below). Live-validated against the exact PID-reuse mechanism the eventual code review flagged as still-vulnerable in the first pass (a scratch script that forged one session's `.pid` file to point at a DIFFERENT real session's live PID — `pidOwnsThisSession` correctly refused to kill it).

**Fix #2 — auth-gate detection structural rework (`agent/auth.ts`, `agent/site-state.ts`, `agent/flow-runner.ts`).** This mechanism had been patched in nearly every batch across project history (ParaBank → expandtesting → automationintesting → lambdatest → webdriveruniversity ×2) without ever converging — always a NEW failure mode, never a fix that generalized. Root cause of the pattern: `ensureAuthenticated()` returning without throwing was being treated as "authenticated," but it actually meant two DIFFERENT things conflated into one signal — "a real login just succeeded" vs. "the generic probe simply found no login gate anywhere" (the bstackdemo.com false-pass). Fixed with a new `SiteState.authenticatedThisRun` flag (in-memory only, never persisted) that `auth.ts` sets ONLY on strong evidence: silent session-restore while already on a confirmed real gate (or a previously-confirmed `requiresAuth` page), successful recipe replay, or a successful full LLM-driven login — explicitly NOT set on "probe found no gate" alone. `flow-runner.ts`'s login-shaped-milestone branch now checks this flag after a first plain `ensureAuthenticated()` call; if false, it calls a NEW `ensureAuthenticated(ctx, { forceAttempt: true })`, which skips the probe/navigation step entirely and goes straight to a REAL credentialed login attempt at the milestone's own current position — reusing the module's own already-correct credential-resolution machinery instead of falling through to the credential-less generic `Explorer.achieveGoal()` (which has no channel to real secrets and can only guess/hallucinate — the exact anti-pattern this whole module exists to prevent).

**Fix #3 — verification doesn't respect a legitimate explorer skip (`agent/flow-runner.ts`).** Walked-flow goals deliberately carry an idempotency clause ("if this action appears already done... skip it and just advance"), but when the explorer legitimately took that path (calling `done` without ever typing the injected random verification marker, because there was nothing to type), the deterministic verification layer still required the marker in the final snapshot and false-failed the milestone. Fixed with `looksLikeIdempotentSkipReason()` — only waives the marker requirement when the explorer's OWN recorded reasoning text shows it actually recognized an already-done state (regex over phrases like "already done/exists/added", "no need to", "nothing left to do"); absence of a fill action ALONE is not sufficient (that could just as easily mean the LLM hallucinated "done" without attempting the edit at all — a distinct failure mode). Paired with `valueLooksLikeMarker()`, which tolerates the explorer's own documented behavior of ADAPTING the literal marker to satisfy a field's format constraint (e.g. stripping digits/hyphens for a letters-only name field) rather than requiring exact string equality.

**xhigh code review of all three fixes** (explicit user go-ahead given before starting) surfaced 9 confirmed/plausible findings, ALL fixed same session: (1) the PID-reuse vulnerability in `pidOwnsThisSession` mentioned above; (2) `authProbeUrl`'s return-type change needed a stray reference fixed (`probeUrl` → `probe.url`) on the retry-after-failed-login path; (3) `crawler.ts` never checked `recycle()`'s return value, looping to a timeout on every remaining page instead of aborting cleanly on an unrecoverable wedge; (4) the marker-verification fix's absence-of-fill-alone heuristic (see Fix #3 above) needed the explorer's-own-reasoning check added, or it would have silently waived verification on genuinely-failed edits; (5) `valueLooksLikeMarker` needed to exist at all, or the fix would have broken the already-shipped, cross-validated literal-value-adaptation behavior; (6-9) smaller robustness/comment-accuracy findings in the same three files. Re-validated live afterward: PID-reuse repro correctly refused, saucedemo clean (4 PASS/1 FAIL, the 1 fail being a known pre-existing quirk), and koyal's re-validation run happened to surface a genuine, live, independent finding — **beta.koyal.ai's backend was actually down** (confirmed via repeated `502` on `/v1/api/user/userLogin` and Socket.IO polling endpoints, reproduced identically across 2 separate runs). Flagged to the user (whose email domain indicates Koyal staff) as a real production issue, unrelated to any of this session's code changes.

**The 15-site regression suite (task from CLAUDE.md's own prior "Decided next step") — IN PROGRESS.** Koyal deferred (backend confirmed still down via a direct live re-check just before starting: fresh login attempt still 502s on `/v1/api/user/userLogin`). For the other ~15 historically-validated sites: sites with existing `.autoqa-state/<hostname>` in this checkout (saucedemo, orangehrm, parabank, uitestingplayground, expandtesting) got a PARTIAL reset (`--sitemap --statements --recipes --auth`, deliberately NOT `--all`, to preserve saved `secrets.json` credentials) — sites from batches 1-3 (automationintesting, globalsqa, bstackdemo, the-internet.herokuapp.com, GreenKart, lambdatest, webdriveruniversity, eviltester) have NO state in this checkout at all (those batches ran in separate git worktrees whose gitignored `.autoqa-state` never persisted here), so they're naturally fresh. Driven via the Agent tool in batches of 3 (NOT the Workflow tool, NOT worktree-isolated) — deliberately avoiding a repeat of the prior session's resume/`parallel()` stacking incident, and worktree isolation is unnecessary here since this is a pure-observation, no-code-editing pass (each agent only touches its own site's state directory). Each agent is briefed fresh (zero context) with the project's full lesson set and told explicitly: bucket every non-pass into (a) known nav-state-loss probe, (b) genuine site characteristic, (c) external flakiness, or (d) possible new regression — and to distrust raw PASS/FAIL count deltas against history, since a fresh explore re-derives flows via LLM and isn't flow-for-flow comparable.

**A real, recurring operational quirk this batch (not an autoqa bug): delegated regression agents kept prematurely stopping mid-run, claiming to be "waiting for an external notification/monitor" that does not exist** — they are themselves responsible for polling their own backgrounded `npm run qa` process and answering its prompts; nothing else drives it. Hit on 3 of 6 agents so far (ParaBank, automationexercise, expandtesting) and once even after the underlying process had ALREADY finished and printed a final report (expandtesting — its `report.md` sat complete on disk for a while before the agent said anything). Each time, resuming the agent with an explicit "there is no external notification, you must poll yourself" instruction got it back on track. Worth stating this explicitly up front in any future delegated-agent briefing that involves a long-running backgrounded process.

**Results so far (6 of 15 sites done, batches 1-2):**
- **saucedemo**: 25 PASS/2 FAIL/0 NEEDS-REVIEW. 1 fail is the known marker-survives-navigation test-side gap. The other is a REAL, newly-surfaced finding — but confirmed via `git diff` to be **pre-existing** code, untouched by any of this session's 3 fixes: saucedemo's root URL ("/") always renders the login form regardless of actual session state (confirmed live: after loading "/", direct navigation to `/inventory.html` still showed the fully-authenticated view), which breaks `ensureAuthenticated`'s `alreadyOnRealLoginGate` optimization — that code path reloads the CURRENT url in place with only a 5s timeout instead of bouncing to a known-authenticated probe URL, and since "/" can never resolve to "not gated," it always concludes "session expired" and forces an unnecessary full fresh re-login. Fired 5+ times in one run, and in one case burned an entire milestone's step budget on redundant re-auth, leaving zero budget to click "Finish." Diagnosed live, NOT fixed (pure-observation pass) — flagged for triage. Likely also explains why this run didn't reproduce the site's one well-known historical bug (checkout back-forward state loss) — the repeated auth churn probably reset the navigation-history stack needed to trigger it.
- **OrangeHRM**: 7 PASS/3 FAIL/0 NEEDS-REVIEW. Real admin login succeeded cleanly on the first attempt via the new auth-gate rework — no retry loop, correct positioning. All 3 fails git-blamed to pre-existing code unchanged since the original 2026-07-07 commit (an `isLoginShapedGoal` false-positive on a navigation-only "Open the login page" goal, cascading into the documented logout-dropdown gap) — confirmed NOT touched by this session's fixes or the code-review follow-ups.
- **uitestingplayground.com**: 28 PASS/0 FAIL/0 NEEDS-REVIEW — clean sweep; even the long-documented crawl-collapse issue didn't reproduce (6 distinct pages mapped cleanly).
- **ParaBank**: 1st attempt: 0 PASS/6 FAIL — but the auth-gate mechanism (`hasVisiblePasswordInput()`-based detection, one of the ORIGINAL two root causes Fix #2 targeted) fired correctly with ZERO missed gates across all 6 flows; the actual blocker was a dead/stale saved test account (100% rejection across ~10 manual reproduction attempts — NOT the site's documented *intermittent* flakiness, which succeeds most of the time). User-authorized credential refresh: registered a fresh account, let the CLI's own `askSecret`/`saveSecrets` flow persist it normally, re-ran. 2nd attempt: **39 PASS/5 FAIL/1 NEEDS-REVIEW** — real banking flows genuinely exercised end-to-end for the first time in a while (account creation, loan application, fund transfer, profile update, transaction search, all with real confirmation text). 4 fails are the standard nav-state-loss probe class, 1 fail is a genuine test-harness field-ref-misalignment gap on a 9-sequential-textbox bill-pay form (unrelated to the 3 target fixes), 1 needs-review is a correctly-auto-denied logout-guard prompt. Also positively reconfirmed: the prior session's `hasAnyPriorRecipe()` fix (087a384) firing correctly, the report-loss fix still holding (no silent 0/0/0 despite 6 entry-level aborts on the first attempt), and the D5 off-origin scope guard still holding.
- **automationexercise.com**: 18 PASS/4 FAIL/0 NEEDS-REVIEW. Crawl found 6 pages/19 edges (meaningfully more than the historical 4-page-collapse baseline — the page-identity fixes still generalize here). Auth-gate rework confirmed working both directions (correct true-negative at the seed URL, correct true-positive + direct-login at the real `/login` gate) plus a full live registration completed end-to-end. 2 fails are known bucket-(b) methodology/mis-grounding characteristics. The other 2 (initially flagged by the agent as "possible regressions") both traced via `git diff` to the SAME pre-existing, completely UNCHANGED code block — the "mid-flow auth wall" recovery (`flow-runner.ts`, ~line 620-640, present before any of this session's fixes): this site's `/login` page shows BOTH signup AND login forms together, so a real visible password field is present even during a pure-signup milestone, and the mid-flow check misreads that as "hit an auth wall," derailing into an unwanted real login attempt (once) and misreading a legitimate negative-path rejection loop as session loss (once). Not a new regression — but a genuine, newly-surfaced instance of the same "auth-gate detection still not converged" disease flagged repeatedly across this project's history. Not fixed (pure-observation pass) — flagged for triage.
- **practice.expandtesting.com**: 37 PASS/7 FAIL/0 NEEDS-REVIEW. The site's own specific historical auth-gate false-positive (demo pages literally named "Forgot Password Form" etc.) did **NOT** resurface — confirmed correctly evaluated as not-gated, including via the new `forceAttempt` path exercised live in the `user-auth-api` flow. 3 fails are known nav-probes, 3 are genuine site characteristics (a flow that never obtains an auth token before a note-CRUD call; a live-code-editor race; a stray in-article ad link to an unrelated third-party page, correctly self-failed rather than followed through). 1 fail is a pre-existing deep-walker recipe-recording gap (records an incomplete field-fill), unrelated to the 3 fixes. **One additional finding, a false PASS (not in the fail count):** `user-auth-api:m3` passed despite never actually verifying its goal ("a token is returned") — a forced-login exception was only logged, never fed into the verdict. This is a live reproduction of an ALREADY-DOCUMENTED, deliberately-deferred gap (login-shaped milestone failures don't feed the same explorer-failure-downgrade mechanism that covers the direct-explorer path — first found on lambdatest) — now specifically confirmed against the NEW `forceAttempt` code path too, since `forceAttempt` is just another caller of the same `ensureAuthenticated()` whose failures were already known to be under-reported. Not fixed this session (still deliberately deferred, same reasoning as before: needs a fresh full re-run to live-verify, which a completed run can't retroactively provide).

**Remaining as of this writing**: 9 sites not yet started (demoqa, automationintesting, globalsqa, bstackdemo, the-internet.herokuapp.com, GreenKart, lambdatest, webdriveruniversity, eviltester), running unattended per explicit user instruction (laptop staying on, ~3 hour window) — batches of 3, same methodology. Koyal to be re-checked near the end and run if recovered. **If you're reading this and no later "regression suite complete" note exists below, the numbers above are partial — check task state / conversation history before treating this as final**, and none of the 5 pre-existing (non-regression) findings above have been fixed yet — they're accumulated observation-pass output awaiting a triage decision.

## 2026-07-15 — regression suite continuation (6→10 of 15 sites touched), 8 more live bugs, the "phantom monitor" agent pattern, koyal's first post-outage run, two architecture decisions queued

**This is the "later note" the previous section told you to check for.** Session opened with the user asking to "restart the tracker." Reality check found: the harness's own task-tracking was empty (the prior session's "running unattended" tracking lived only in that session's conversation state, not anywhere persistent), and NO process was actually running — the 3 sites the prior session had launched (demoqa, automationintesting, globalsqa) had each stalled overnight on an unanswered CLI prompt and then the underlying `npm run qa` processes had died entirely. Rebuilt tracking from scratch this session (10+ tracked tasks), resumed the 3 stalled sites, then continued through 2 more batches of 3, same methodology as batches 1-3 in the project's history: pure-observation regression passes (no code edits) via the Agent tool, one delegated agent per site, each acting as its own human-in-the-loop through the file-based inbox channel.

**Per-site results and live findings (8 real bugs found, all diagnosed-not-fixed per the pure-observation mandate):**

- **demoqa.com**: 17 PASS/9 FAIL reported, but likely 15/11 in truth — a **verdict-flip false-PASS bug** in `flow-runner.ts`'s post-verdict bookkeeping (~line 780-800) flipped at least 2 genuine fails back to PASS after classifying ad-iframe console noise as harmless, even though the explorer's own log for one of them literally said `fail — the current page is alaskaair.com, not demoqa.com`. This week's Fix #3 safeguard against exactly this class only covers the narrower explorer-failure-downgrade path, not a plain signal-driven `fail`. Contributing cause: the site got hit by aggressive ad injection this run (an Alaska Airlines ad hijacked navigation 4 separate times), and there's **no same-origin guard in statement/candidate extraction** — each hijack generated 30-40 noise classify-prompts scanning the off-site page. Also found: inconsistent off-site recognition in the explorer's own goal-completion judgment (caught later occurrences correctly, missed the first one), and `isSearchShapedGoal` over-firing on a legitimate non-random search term that was actually a meaningful substring of a record the same flow had just created. Crawl itself healthy (32 pages/35 edges, beats the historical baseline). No daemon wedges.
- **automationintesting.online**: 24 PASS/4 FAIL. Two new regressions: (1) **`guard.ts`'s `LOGOUT_RE` has no override path at all** — it auto-denies any Logout-labeled click unconditionally, not even via the `always`/`never` allowlist every other destructive action gets, making any flow whose milestone is "click Logout" structurally unpassable, permanently, on any site. (2) A new sub-case of the still-unconverged auth-gate disease: `isLoginShapedGoal`'s regex matched the bare word "login" inside a verify-kind goal phrased as "...displayed **after login**" (describing a completed state, not an instruction to authenticate) — misrouting it into the auth-delegation branch, which then deleted the milestone's own success check; confirmed live the browser was actually sitting on the public landing page, not the admin dashboard, when this passed. Also: a recipe-replay reported "OK (no LLM calls)" while its fill actions silently no-op'd on all 4 fields — only the site's own backend validation caught it. The known fast-forward silent-admin-auth-skip pattern reproduced exactly as documented.
- **globalsqa (XYZ Bank)**: 23 PASS/7 FAIL. Hash-route page identity held up correctly (no regression). 2 of the fails are the site's own ad-interstitial hijacking clicks off-site — traced live through `flow-runner.ts` and confirmed this is **not** a silent-pass bug (a cosmetic write-before-patch console artifact briefly showed `[PASS]`, but the persisted verdict correctly landed on fail). New observation: `Explorer.achieveGoal()` has **no off-origin guard during milestone execution**, unlike the crawler/deep-walker's existing preventive guards — twice this run an ad interstitial led a click off-site before the explorer reactively (not preventively) caught it, burning steps both times. Also re-confirmed the marketing-blog crawl-seed dilution (3 of 6 tested flows were about the wrong app) and a new instance of the already-known "explorer doesn't recognize an already-successful submit and retries" bug class, this time on a select/submit form (created a duplicate bank account) rather than file upload.
- **bstackdemo.com**: 21 PASS/4 FAIL/4 NEEDS-REVIEW. All 4 needs-review root-caused by direct step-summary read rather than left as an opaque bucket: (1) a session-leak between flows in the same run — a walked flow's m2 assumed an unauthenticated entry and the explorer correctly refused to fabricate a login it couldn't perform; (2)+(3) classic LLM-overachievement — one milestone's actions (`full-purchase-flow:m4`) secretly completed the *next* milestone's goal too (filled all shipping fields AND submitted AND reached `/confirmation`), so m5 then honestly self-failed reporting nothing left to do; (4) a flow's own premise was wrong — `checkout-requires-auth:m2` assumed clicking Checkout while unauthenticated sends you to sign-in, but the site actually shows a real shipping form with no auth wall at all, and the explorer correctly reported the mismatch instead of pretending the assumption held.
- **the-internet.herokuapp.com**: 32 PASS/1 FAIL — the cleanest run of the batch. The 1 fail is genuine click-loss flakiness (verified live: only 2 of 3 expected elements existed in the DOM). The real finding: an **uncaught exception in `core/explorer.ts`'s `decideNextAction`** — when the LLM's malformed-JSON fallback parser (`parseJsonFromLlm`'s greedy regex, `core/llm/client.ts:167-178`) itself throws, the exception propagates uncaught all the way through the milestone loop to `runFlow`'s outer catch, silently dropping the rest of that flow's milestones with **zero trace** (no PASS/FAIL/NEEDS-REVIEW at all) — a mid-flow variant of the report-loss bug class already fixed once (2026-07-09 night) for entry-level aborts only; that fix's `scenario.steps.length === 0` guard doesn't cover a crash after some milestones already passed. Trailing-slash page-identity mechanism held up cleanly, off-origin guard held, no daemon wedges.
- **GreenKart (rahulshettyacademy.com)**: paused twice (first at 134/0/0 to make room for koyal, then killed outright per explicit user request), then fully resumed and re-run to completion later the same day. **Final: 158 PASS / 5 FAIL / 3 NEEDS-REVIEW.** The resumed run re-executed all 9 approved flows from scratch (not a continuation from the pause point) and covered new territory the paused run never reached — not a 0→5 regression, new coverage surfacing new findings. **The site's specific historical risk (a multi-hop off-site chain to a real third-party checkout) DID reproduce this time, through a different entry point than before**: `subscription-plan-selection:m3` ("Click JOIN NOW (SILVER)...") drove the browser through `rahulshettyacademy.com/all-access-subscription` → `courses.rahulshettyacademy.com/p/all-access-membership` → a live `checkout.teachable.com` page with a real, addressable order ID — with **zero guard prompt ever raised**. Root cause, precisely diagnosed: (1) `guard.ts`'s destructive-keyword floor (`delete|pay|checkout|invite|revoke|logout`) doesn't include "join"/"enroll"/"subscribe"; (2) more fundamentally, `Explorer.achieveGoal()` during flow-execution/replay has **no origin check at all** — the off-origin guard that exists only lives in `crawler.ts`/`deep-walker.ts` (exploration-time edge mapping), a different code path never invoked when testing an already-approved flow. Mitigating: the explorer stopped at the checkout page without entering any payment data (also Cloudflare-gated), so nothing was actually purchased — but a real order session now exists in Teachable's system. **A second mistake compounded this**: the deterministic layer correctly computed FAIL for this milestone, escalated it as an ambiguous verdict to a human, and the delegated agent driving the run answered "pass" — overriding a correct fail to an incorrect pass, and triggering one additional (benign but avoidable) probe interaction with the live off-site page. The delegated agent disclosed this mistake itself in its final report. **Recommendation: de-approve `subscription-plan-selection` from this site's flow set going forward**; also review `career-accelerator-application` (filled a real lead-capture form with fake values on the academy's real business page, never submitted — same scope problem, lower severity). Two new (d)-bucket autoqa gaps, not site bugs: a fill-vs-select widget-handling gap (`flight-search-form:m2` — plain `fill()` doesn't register as a real selection on autocomplete/select2-style widgets) and an explorer stuck-loop where a real, working sortable column header got ignored in favor of repeatedly re-filling a search box (`offers-table-search-sort:m3`). State preserved on disk (`.autoqa-state/rahulshettyacademy.com/`).
- **ecommerce-playground.lambdatest.io**: 27 PASS/5 FAIL/0 NEEDS-REVIEW. Query-string page-identity mechanism held up cleanly (10 distinct pages, no collapse). Standout: the delegated agent **caught its own false PASS live** — `search-add-to-cart-checkout:m5` was recorded as passing, but the actual Checkout button never advanced the page through 8 retries (confirmed via the real `step-summary.md`: final URL/title never left `checkout/cart`); the verdict got flipped from needs-review to pass because an ambient "Shopping Cart (45.00kg)" page heading — present regardless of whether checkout actually worked — had been classified as `success`. This is a **distinct mechanism** from the already-documented `account-login-gate:m2` false-PASS gap (that one's a missing explorer-failure-downgrade; this one's an overly-generous ambient-heading KB classification). Also: the site's AJAX "Checkout" control showed real click-loss in 3 independent encounters this run — CLI reports the click as issued, page never navigates — matching the project's long-documented "trusted click never reached the page" quirk closely enough to be worth checking whether that fallback covers this specific control. `account-login-gate:m2` itself wasn't exercised this run (no login-attempt flow got proposed). Zero daemon wedges.
- **webdriveruniversity.com**: stopped mid-flow (8 of 9) by explicit user request, not a crash. Solid partial results before the stop: contact-form, todo-list (the `press` keyboard-Enter-submit action worked cleanly and repeatedly), and form-controls all passed their real milestones. The AI Testing Playground auth-gate false-positive (heading-count fix, 2026-07-13) **did not reproduce** — that fix is holding. `login-portal-auth:m3` correctly failed, but m1/m2 recorded PASS despite the auth module itself logging a failed login underneath — a third site now reproducing the already-known "login-shaped-milestone failures aren't fed into the verdict-downgrade mechanism" gap (first found on lambdatest/expandtesting). New, unresolved: the Login Portal page uses a bare `onclick="validate()"` submit handler with no `preventDefault`/form action; across ~3 automated attempts no native-dialog-blocked log line ever appeared (no real alert detected), yet the URL briefly showed a trailing `?` and fields kept resetting — the explorer declared success reasoning a JS alert "validated" the login with no direct log evidence backing it. Worth someone checking by hand whether this specific alert+submit interaction is actually automatable via CDP the way the code assumes. Also re-confirmed (a 3rd site now) the statement-KB's per-listed-item classify-question inefficiency (see below).
- **beta.koyal.ai**: run for the first time since the 502 outage — backend confirmed recovered (a live login succeeded). Partially reset (`--sitemap --statements --recipes --auth`, kept env-based creds) to force a genuinely fresh explore rather than replaying pre-outage recipes, run with `AUTOQA_EXHAUSTIVE=true --deep-flows 3` to stay comparable to the last real baseline (45/9/3). **Never reached the test/flow-running phase at all before being killed — final result 0 PASS/0 FAIL/0 NEEDS-REVIEW, not comparable to anything.** What actually happened, per the delegated agent's full post-mortem (more complete than what an earlier, mid-run check suggested — corrected here): `/upload` resumed an existing draft (prior account activity) that already had a script PDF attached; the explorer repeatedly tried to "upload" against `ref=e5`, which turned out to be the wizard sidebar's own nav breadcrumb for "Upload file" — because the page-classifier's `wizard-upload-file` landmark signature (`snapshotAnyOf: ['Upload file', 'Story Type', 'Review transcript', 'Edit scenes']`) matches the wizard's *persistent sidebar*, visible on every step, not anything specific to the upload step itself — the exact "chrome landmarks must never identify a state" disease this project has fought before, now confirmed live on Koyal's own wizard. This produced ~12 doomed upload attempts across 2 full explorer calls, re-asking the same "give me a file path" CLI question **5 times over 20+ minutes with zero answers ever written** (the delegated agent had stopped actively polling — see phantom-monitor section below); a direct human answer did NOT actually unblock the upload (it kept failing "no working file input found" afterward too) — it only let the run continue past that point via a different path. **Two further, real findings**: (1) session/auth appeared lost twice mid-walk, both times right after a blank-page (`about:blank`) recovery reopened a URL and landed on `/login` instead — neither the deep-walker's nor `core/explorer.ts`'s own blank-page recovery verifies it actually landed on the requested *authenticated* page; the existing auth-wall check only fires at flow **entry**, not mid-walk, so both incidents drove the explorer into an unwinnable fake-signup + unguessable-OTP loop before the walk-level "no progress after 3 attempts" bound correctly stopped it. (2) **A poisoned flow was permanently written to `sitemap.json`** as a result: `walked-projects-list-create-yournext-video` now has milestones m3-m8 instructing future runs to fill Sign Up fields and click "Verify OTP" — pure artifacts of the misclassification/session-loss cascade, not real product steps. **This needs manual cleanup before any future resume** — approving/testing it as-is would guarantee noise or failure. Also flagged, unresolved: the explorer's own `done` call for a character-creation walk named the newly-"created" AI avatar `'KoreanMan'` — identical to one of the account's pre-existing characters — genuinely ambiguous whether this is coincidence or a false-positive creation claim; no screenshot exists at the exact moment to disambiguate. Plan-modal step-budget issue reproduced (ate 2 of 6 steps on the first call). S3 backend bug and the audio path were never reached/exercised. **The 2026-07-14-night idempotent-skip verification fix remains completely unvalidated against Koyal** — that mechanism lives in the test phase, which this run never reached. State preserved (`.autoqa-state/beta.koyal.ai/`, including the poisoned flow) for a future resume — but the poisoned milestones should be manually removed or the site should get a fresh sitemap reset before that resume, not a plain continue.

**A real, recurring operational pattern this session, distinct from any autoqa code bug — the "phantom monitor."** At least 5 separate times across 4 different delegated agents, an agent driving a long-running `npm run qa` background process + file-based Q&A loop stopped mid-run — after anywhere from 2 minutes to several minutes of real work — with some variant of "I'll wait for the monitor's notification" or "ending this turn, I'll act when notified." **No such monitor exists for this task.** This is the same failure class flagged in the 2026-07-14 night session (there, "waiting for an external notification" hit 3 of 6 agents) — this session it got worse, hitting the same webdriveruniversity agent three times in a row despite an explicit correction each time. What actually worked, eventually: not "please poll yourself" (tried twice, didn't stick), but handing the agent a **concrete blocking shell command** to run as a single tool call — a `timeout 600 bash -c 'while ...; sleep N; done'` loop that only returns when a real event happens (a question file appears, or the process exits) — which gives the agent an actual tool result to react to instead of a turn-boundary judgment call it kept getting wrong. Also directly useful: checking a delegated agent's *actual* process/session/inbox state yourself, rather than trusting its self-report, catches this immediately (the koyal upload stall was found this way, not from the agent's own status). **Lesson for any future session delegating a long-running background-process-plus-Q&A task to an agent: assume this will happen, brief the concrete-blocking-loop technique up front rather than the vaguer "poll yourself," and periodically verify state directly rather than trusting agent self-reports.**

**A second, smaller but now well-confirmed operational finding: the statement-KB candidate extractor treats every individual item on an enumerated/listing-style page as its own "new message" needing human classification, one at a time.** Confirmed generating long, repetitive, but ultimately harmless (all-noise) backlogs on at least 3 different sites this session: GreenKart's ~25-item grocery catalog (every vegetable/fruit name+price), lambdatest's product listings, and webdriveruniversity's ~30-widget AI Testing Playground (every numbered widget heading). Not a false-verdict bug — every one of these was correctly classified noise — but a real, generalizable efficiency problem worth someone looking at (e.g., batch-classify same-shaped repeated candidates instead of one CLI round-trip per item).

**Two architecture changes discussed at length with the user and queued as the next real follow-up work, after the regression suite + a planned filmarena.ai (new site, never tested before) validation:**

1. **Flow-runner: a single FAIL verdict at milestone N aborts every remaining milestone in that flow, silently.** Confirmed by reading the actual code (`flow-runner.ts` ~line 940-943: `if (step.result.verdict === 'fail') { ...; break; }`) — this is the literal mechanism behind globalsqa's own "broken at m2 — moving to next flow" log line, and it's worse for an uncaught exception (the-internet's bug above): no synthetic record at all if earlier milestones already passed. Planned fix: don't abort — let the loop continue to N+1 and give the *already-existing* guard-phase/`replayUpTo`/fast-forward repositioning logic (which currently never gets the chance to run for N+1 after a fail) a shot at recovering position; if repositioning genuinely fails, record an explicit "skipped — untested, upstream break at m{N}" entry rather than silently dropping (today's exception case) or blindly running from a corrupted position and minting a garbage verdict. A related but distinct sub-problem: LLM-overachievement (bstackdemo's m4/m5 above) needs its own "is this milestone's goal already satisfied by current state" pre-check, separate from the existing fast-forward-to-a-later-page mechanism (which doesn't apply when overachievement lands *past* the last milestone). Paired with a new structured per-milestone anomaly tag (`overachievement`/`session-leak`/`off-site`/`upstream-break`/`repositioned`/`crash-recovered`), kept separate from the pass/fail/needs-review/skipped verdict — this is what turns "had to manually read six step-summaries to understand 4 needs-reviews" (bstackdemo, this session) into something a report just shows you.
2. **Screenshot/vision as a second signal in verification, motivated by "we find lots of bugs but only visibility is the a11y tree."** The real argument isn't click-occlusion (though that's real too — the demoqa/globalsqa ad-hijack findings above are exactly that class) — it's that an a11y-tree-only oracle is *structurally blind* to a whole bug category: visual regressions, invisible/mis-styled text, layout breakage the tree reports as fine. Researched H Company's "Holo"/HoloTab computer-use models as a reference point (their documented architecture is screenshot + optional page-text → pixel-coordinate actions) — confirmed vision-as-corroborating-signal is a validated pattern elsewhere, but concluded autoqa should NOT move to coordinate-based click execution to get this (would reinherit the click-loss/scroll-drift fragility already fought hard on the DOM-ref path). Planned design: two distinct uses, not one — (a) occlusion/action-grounding during exploration can stay selective (only when retrying/stuck), but (b) the bug-detection/verification-oracle use must be systematic (every milestone), since on-demand-only would never catch the exact bugs the a11y tree can't see in the first place. Hard guardrail either way: vision must never autonomously flip a deterministic pass→fail or fail→pass — it only feeds the existing needs-review/human-escalation path — directly motivated by the demoqa verdict-flip bug found in the SAME session this was discussed. Cheap unlock: autoqa already screenshots every step for evidence, so this is wiring, not new capture infrastructure.

**Status at end of session**: eviltester (the last of the originally-queued 9 regression sites) not yet started; filmarena.ai (queued to run after) not yet started; GreenKart, webdriveruniversity, and koyal all intentionally stopped mid-run with state preserved for resume. If you're reading this and no later note exists below, all of the above is where things stand — resume in this order: finish/resume GreenKart + webdriveruniversity + koyal (all have real partial progress worth continuing from, not restarting), then eviltester, then filmarena.ai, then the two architecture changes above.

**Update, later the same day — all 9 original regression sites finished (GreenKart/webdriveruniversity/koyal resumed and completed or re-paused; eviltester run fresh), plus filmarena.ai (a brand-new site, first time tested).**

- **GreenKart, resumed and fully re-run**: see its own corrected entry above (158/5/3, the Teachable checkout safety finding, the human-verdict-override mistake).
- **webdriveruniversity, resumed and fully re-run**: 40 PASS/6 FAIL/1 NEEDS-REVIEW. The AI Testing Playground auth-gate fix held (no regression). Two things worth remembering: (1) a genuine site bug, fully root-caused via direct network/source inspection — the Login Portal page loads jQuery over plain `http://` on an HTTPS page, gets blocked as mixed content, so the site's own `preventDefault`-adding script never attaches; the `onclick="validate()"` handler still fires and submits correctly (confirmed via network capture of a real `run.html` request), but since nothing blocks the native form submit, it races and always wins, reloading with an empty query string and resetting fields — **login can never visibly succeed on this page, regardless of credential correctness**, a real product bug, not autoqa's. (2) the already-known login-shaped-milestone verdict-downgrade gap reconfirmed a 3rd time.
- **testpages.eviltester.com, run fresh**: 54 PASS/4 FAIL/0 NEEDS-REVIEW. The `/apps/` page-collapse fix held (22 pages). The standout: `ensureAuthenticated({forceAttempt:true})` searched **site-wide** for a login form instead of grounding at the milestone's current page, landing on an unrelated HTTP Basic Auth gate and submitting the wrong credential type against it — a new mechanism-level gap in the forced-auth path itself (distinct from the already-known goal-classification false positive that triggers it). The previously-suspected file-based Q&A stale-answer race did **not** reproduce this run (all 26 `statements.json` entries checked out correctly).
- **beta.koyal.ai, resumed then killed again**: never reached the test phase (0/0/0). See its own corrected entry above (poisoned sitemap flow, chrome-landmark-over-matches-sidebar, double mid-walk auth-loss, ambiguous character-naming). Idempotent-skip fix still completely unvalidated against this site.
- **filmarena.ai — first-time validation, and unlike every other site today, fixes were allowed (no historical baseline to regress against).** Confirmed to be **a Koyal AI product** ("BY KOYAL AI" on its own homepage; API backend lives at `beta.koyal.ai/v1/api/film-arena/v1/*`; shared login with koyal.ai accounts) — a public leaderboard/benchmark for generative AI models (28 tracked, 1.17M+ votes, blind pairwise voting) combined with an actual generation arena (describe a shot, pick Best/Top 3/Custom/Battle, generate video/image/video-edit content). **The delegated agent driving the regression pass never noticed or reported the Koyal connection at all — caught only via a manual cross-check** (opening the site directly in a separate agent-browser session and reading the homepage) — a real situational-awareness gap worth remembering: a delegated agent's own report is not a substitute for occasionally looking at the actual page yourself, especially for a site this closely tied to the project's primary target. Final: **50 PASS / 6 FAIL / 1 NEEDS-REVIEW**, 13 pages/14 edges/9 flows, ~48 minutes.
  - **Two real autoqa bugs found AND FIXED** (both in `flow-runner.ts`): (1) **recipe replay vs. fresh-marker mismatch** — any 'edit' milestone with an existing recipe generates a brand-new random verification marker every run, but `RecipePlayer.tryReplay` types the recipe's OLD literal recorded value verbatim; two independent random strings that can never match, hit on every walked-flow edit milestone on its first test-phase run. Fixed by reusing the recipe's last recorded fill value as the marker instead of generating an unrelated fresh one; re-validated live (3 affected milestones flipped fail→pass in isolated re-test). **This is very likely the SAME root cause as webdriveruniversity's "recipe replay never refreshes the marker" finding from earlier the same day** (see that entry above) — that finding was left diagnosed-not-fixed since it was a pure-observation pass; this fix should be checked against webdriveruniversity on its next run, it may already be resolved. (2) A new sub-case of the project's still-unconverged #2 recurring bug source: `isLoginShapedGoal` matched bare "login"/"sign in" in a pure UI-navigation goal ("confirm the login form is shown," no credential-entry involved), misrouting it through the full credentialed auth flow twice, burning 24 LLM steps. Fixed with a narrow exclusion — a bare login/sign-in match immediately followed by a UI-element noun (form/tab/page/screen) is only treated as real auth intent if the goal also contains credential-entry wording — validated against 9 cases (the live repro + 8 historical controls spanning this function's documented history) before shipping.
  - **Resolves an open question from earlier the same day**: a manual cross-check had found an unexplained discrepancy where one guest session hit "out of credits" after one generation attempt while the delegated agent's own guest session succeeded repeatedly. The full run's own findings explain it — a genuine `429` rate limit (`RateLimit-Remaining: 0` on `/v1/api/film-arena/v1/classify/prompt`) plus running out of guest credits, both self-induced by the walk+test volume of a single long automated session, not a mysterious allocation mechanism.
  - Non-pass bucket: 1 known nav-probe; 2 external/environmental (a genuine login failure because signup requires OTP email verification and no real inbox was reachable in this environment; the rate-limit/credits case above); 1 documented-not-fixed low-severity gap (a "▶" related-models control is sequential navigation, not an in-place carousel, so the flow's guessed successHint never matches — same conceptual class as the already-fixed `isSelectionShapedGoal`/`isSearchShapedGoal` exemptions, just a new goal-phrasing not yet covered, deliberately left rather than adding a third exemption function in an already-long session); 1 re-confirmation of the already-known, deliberately-deferred "login-shaped milestone failures don't feed the verdict-downgrade mechanism" gap (a false PASS on `auth-signup-login:m5` underneath a real, logged failure — now confirmed on yet another site).
  - Good practice worth repeating: the agent directly verified via before/after screenshots that a theme toggle (dark→light) genuinely works, confirming the explorer's own inability to see it in the accessibility snapshot is structural blindness to CSS-only changes, not a real defect — the kind of live-check that separates a documented gap from an unconfirmed guess.

**Status at end of session, corrected**: all 9 originally-queued regression sites are now done, plus filmarena.ai. Next up per the task queue: nothing else pending in the regression suite itself — proceed to the architecture work (flow-runner resilience, vision integration, blank-page-recovery auth verification) whenever picked up next.

## 2026-07-16 — filmarena.ai function-coverage deep-dive: two new fixes (walk choice-memory + flow-proposal mode enumeration), and the operating model that surfaced them

After the regression suite closed, the user pushed on a specific weakness: filmarena.ai's first run (task #10, 50/6/1) only ever exercised the DEFAULT "Best" generation mode — Battle/Top 3/Custom and the Image/Video-Edit output tabs were never touched. The user's testing philosophy, stated explicitly and worth internalizing for all future work: **NOT combinatorial coverage (every filter × setting permutation), but breadth across every distinct FUNCTION/workflow, each taken end-to-end, "always touch the ends" — you never know where an error hides, so cover every distinct path deeply, not the cross-product.**

**First, an honest self-caught mistake**: filmarena.ai's original run was launched WITHOUT `AUTOQA_EXHAUSTIVE=true` (plain mode), while koyal that same session got exhaustive mode. Exhaustive mode is what instructs the walker to actively create/edit/select real content rather than click through — so part of the shallow coverage was self-inflicted under-configuration, not a pure tool ceiling. (This is the same apples-to-oranges mistake CLAUDE.md already flags from 2026-07-14. Stop making it: match the mode to the site's complexity, and for creation-heavy Koyal-family products always use exhaustive.)

**Then two root causes were found and FIXED (both live-validated on a fresh exhaustive filmarena.ai run):**

1. **No memory across walk attempts → convergence/regression on the same options** (`agent/deep-walker.ts` + `agent/crawler.ts`). Live evidence from the pre-fix exhaustive run: across 6 separate deep-walk attempts on the same arena page, the walker picked "Best" or "Top 3" every time and NEVER touched Battle or Custom — because each walk attempt (and each of the 3 retries within a walk) starts as a fresh `explorer.achieveGoal()` call with zero memory of what a prior attempt already selected. It even regressed from Top 3 back to the default Best on a later attempt. Root cause: `advanceGoal()`'s "make a meaningful choice" instruction treats every radio/tab identically and nothing tracks what's been tried. **Fix**: added `DeepWalkerDeps.triedChoicesByPage` (a `Map<pageId, Set<label>>`), owned by `crawler.ts` and passed to EVERY `deepWalk()` call in one crawl so memory persists across separate walks, not just retries within one. After each `achieveGoal()`, any `click` action whose `resolvedRole` is `radio`/`tab` gets its `resolvedLabel` recorded. `advanceGoal()` now takes `triedChoices` and appends "Already tried: X, Y — pick an untried alternative, don't repeat or regress" to the CHOICES instruction (exhaustive mode only).
2. **Flow proposal never enumerated alternative modes as separate flows** (`agent/page-classifier.ts`'s `proposeFlows` prompt). The raw sitemap summary already lists all four mode labels as flat interactives — the LLM could see them — but the prompt only said "exercise features end-to-end," so it wrote ONE flow around one plausible mode and ignored the rest. **Fix**: added a "DISTINCT MODE/VARIANT COVERAGE" paragraph instructing the LLM to detect a cluster of mutually-exclusive alternative options (modes/tabs that are different WORKFLOWS, not parameters) and propose a SEPARATE flow per alternative so each distinct mode is tested at least once — while explicitly NOT multiplying by unrelated parameters (aspect ratio, duration, filters, sort — use defaults for those). This directly encodes the user's "functions not combinations" philosophy into the proposer.

**Validation (fresh exhaustive `--deep-flows 10` run, both fixes in place)**: the flow proposer now emitted distinct, correctly-separated flows — "Generate an image in **Best** mode", "Generate a video in **Battle** mode", "Generate a video edit in **Custom** mode" (naming Top 3 too) — i.e. all four modes and all three output types spread across flows, vs. the earlier run's Best-only. Confirmed live via `git`-clean `tsc --noEmit` and by reading the proposed flows out of `sitemap.json` before approving. The Custom-mode flow even surfaced real functional depth (Custom mode requires an extra "pick specific models" step before Generate enables, which Best doesn't). NOTE the honest caveat: this is a strong prompt-level nudge, not a hard guarantee — an LLM can still under-cover; the walk-memory fix biases toward diversity but doesn't enforce it. Also, generation itself on filmarena.ai gates behind a real sign-in (koyal.ai account) — the mode/tab SELECTION and prompt-entry milestones pass, but actually submitting a generation needs auth, so the deepest end (rendered output) is only reachable when logged in.

**Vision would NOT have fixed this** (asked directly by the user, worth recording): the accessibility snapshot already exposes all four mode radios with clear labels — discovery/perception was never the gap. The gap was planning (treat alternatives as separate targets) — a decision-logic problem, not a visibility one. Vision (task #12) remains valuable for its own distinct purpose (bugs the a11y tree structurally can't see: invisible text, layout breakage), not for this.

**Operating model this session (and its one real failure):** filmarena.ai was driven as a **plain terminal `npm run qa` background process + Claude directly acting as the human-in-the-loop** via the inbox file channel — NOT a delegated subagent (deliberately, to sidestep the phantom-monitor bug). This is the cleanest supervision model when the goal is close observation + live code fixes. Its one failure mode, hit once: an "Approve flows" prompt TIMED OUT unanswered while Claude was mid-task on something else, wasting an entire run at 0/0/0 (exploration saved, but no flows approved → nothing tested). Lesson: when driving a run directly, the approve-flows and login prompts are latency-sensitive — watch for them specifically. A second, subtler mistake same session: a fast "clear the classify backlog" bash loop that blanket-answered `n` (noise) to EVERY classify prompt by matching the prompt *type* — it wrongly noised `"User not found. Please sign up first."`, a genuine login-failure message. This is the exact documented watcher-scoping lesson (match the quoted CONTENT, not the prompt shape). Caught and corrected by deleting the poisoned KB entry (so it re-surfaces for real judgment) and adding an error/outcome-word guard (`error|not found|failed|invalid|success|…`) to the clearing loop so it STOPS for anything message-like instead of blindly noising it. The underlying driver of all this noise: the statement-KB fires one classify prompt PER distinct text label on a page (the long-flagged per-item inefficiency), and filmarena.ai is label-dense (a leaderboard is wall-to-wall headings/model-names), so a first run generates dozens — real efficiency problem (should batch same-shaped labels), documented again below.

## 2026-07-16 (continued) — committed everything + merged to main, two fresh koyal full-wipeout runs (7→54 PASS), two-step-logout + auto-discovery fixes, and a Slack product-bug reporter

**Git: committed and pushed the whole working tree, then merged autoqa → main.** Per explicit user go-ahead, committed everything uncommitted (my session fixes + prior-session autoqa work + the legacy audio/script/login harness changes I did not author) as `f13fbbb` on `autoqa` and pushed. **Secret hygiene held**: `happyflow/script-path-probe/` network-capture JSON files embed live Koyal JWT auth tokens — confirmed via scan, gitignored (`script-path-probe/` + `_run-logs/` added to `.gitignore`), and NOT committed (an inner `.gitignore` also already excluded `network*.json`/`console.json`). Then merged `autoqa` → `main` (`15671f3`, `-X theirs` on 25 add/add harness conflicts since autoqa is the source of truth; full-tree secret scan of main HEAD came back empty). Later the two-step-logout + Slack work was committed separately as `8d99b06` and pushed to `autoqa` (main is now one commit behind again — bring it up only on request).

**beta.koyal.ai, full wipeout, run #1 (validating the fixes so far): 7 PASS / 5 FAIL / 2 NEEDS-REVIEW — a BAD number, and the root cause was NOT the code.** Diagnosed by reading the actual log (after two wrong guesses from reasoning about architecture — lesson: read the log, don't infer): the very first LLM call of the run (the "which button logs me in?" call) hit a transient **Anthropic 529 overloaded** error; `commands/explore.ts` wraps the initial `ensureAuthenticated()` in a try/catch that just logs a warning and **continues unauthenticated with no retry**, so the crawl mapped only the public login page (1 page, 0 edges) and the flow-proposer built an almost entirely signup/auth-shaped flow list off that near-empty map. Login only happened later inside a flow, too late to widen the map. **This is a real, still-unfixed gap: the initial login has no retry-once fallback on a transient failure**, even though the LLM client retries individual calls. (Also compounded by the no-logout session-leak, below.)

**The logout problem, and the fix shipped for it (`8d99b06`).** Many koyal flows need a logged-OUT start (signup, password checks); with no discoverable logout, they all landed already-authenticated, failed at milestone 1, and — via the still-open `break`-on-fail (task #11) — died fast. Koyal's logout is real but hidden inside the "Shresth / PRO PLAN" profile dropdown (must click the profile block first). The old ask-once logout mechanism only supported ONE click target. **Fixes:** (1) `sitemap.ts` `learnedLogoutMenuOpener` + `flow-runner.ts` two-step "opener > logout" click sequence (ask-once now accepts `"Shresth > Logout"`); (2) `crawler.ts` + `guard.ts` (LOGOUT_RE exported) **auto-discover** a hidden logout during normal crawl click-probing — if a click reveals a Logout-labelled control that wasn't visible before, it records `(opener > logout)` automatically, so the ask often never fires. tsc clean. NOTE: I could not fully hand-verify the live two-step click during a manual test (my test clicks didn't open the menu — a limitation of the manual test, not proof the fix is wrong); it IS exercised by the auto-discovery path during real crawls.

**beta.koyal.ai, full wipeout, run #2 (fixes in place; Opus background-agent as the sole human-in-the-loop; ZERO manual steering): 54 PASS / 14 FAIL / 6 NEEDS-REVIEW, 223 LLM calls — a large, real improvement over run #1's 7/5/2.** This run logged in cleanly, deep-walked all four creative surfaces (projects/characters/assets×2 — including a real AI-avatar generation), the script wizard reached Visual Style, and — notably — a **dedicated Audio flow was proposed and walked** (audio WAV uploaded, reached scene-edit), the first time koyal's audio path got real depth rather than always defaulting to Script. That confirms the filmarena mode-coverage fix generalizes. The 14 FAILs bucket honestly: **10 known/by-design** (7 back-forward nav-state-loss probes + 3 `matrix-start-method` probe-design mismatches on the Script/Audio fork), **2 REAL Koyal backend product bugs** (the S3 scene-gen error, below), **2 test-side marker gaps**. The 6 NEEDS-REVIEW are 6 honest `skip`s the Opus answerer surfaced rather than coin-flipping (mostly "real bug vs. 8-step-budget-exhaustion" ambiguities, plus draft-contention and mis-authored-flow-premise cases). Re-confirmed live: 4+ deterministic-PASS-while-explorer-said-fail mismatches (task #17 territory), 9 "trusted click never reached the page" click-loss quirks (fallback handled them), 3 Anthropic 529s (one aborted a flow), and draft contention (a stale SCRIPT draft blocked a clean Audio-fork entry).

**Why no video was produced this run: the S3 scene-generation bug.** Both paths walked deep and reached the scene-generation stage — which is exactly where Koyal's backend throws `Failed to fetch JSON from S3: SyntaxError: Unexpected token '<', "<!DOCTYPE "... is not valid JSON` (an HTML error page returned where JSON was expected). Since that step builds the video, generation can't complete → no finished video. Same confirmed product bug as 2026-07-09/07-13. Documented in the new `autoqa/KNOWN_KOYAL_BUGS.md` as `koyal-s3-scene-generation-fetch` (Critical). (A "COMPLETED" badge seen earlier was a PRE-EXISTING finished project on the account, not one this run created.)

**Slack product-bug reporter (task #16, shipped `8d99b06`, live-validated).** New `core/slack-bugs.ts`, wired into `commands/test.ts`'s end-of-run finally (so it fires after EVERY run, including the combined `run` command which calls `testCommand`). Posts to the Slack bugs channel via `SLACK_BUGS_WEBHOOK_URL` (from `login/.env`, gitignored, never hardcoded). **Format is exactly four fields per bug — `Bug` / `Inputs` (file·plan·credentials-type-no-secrets·url) / `Reproduction` (numbered) / `Error log` (console·network·exception lines only) — and nothing else; posts NOTHING when zero product bugs** (no all-clear fluff). The product-bug filter that makes this precise: a FAILED milestone that ALSO carries real site-emitted error evidence (console error / JS exception / 5xx / 4xx-on-`/api/`) — which is exactly what excludes nav-probe fails and marker-gap fails and guarantees the Error-log field is never empty. Live-tested end-to-end against run #2's report: correctly posted ONLY the 2 S3 bugs (excluded the other 12 fails), confirmed `{"posted":true,"bugCount":2}` to the real channel.

**Operational notes this stretch:** (1) A prolonged **auto-mode safety-classifier outage** (the harness runs a fixed `claude-sonnet-5` model to approve write/launch actions; it went down repeatedly) blocked my own writes/agent-launches for a long stretch — independent of the session model, so switching to Opus didn't bypass it; the user exiting auto mode did. The live koyal run was never at risk because it auto-defaults unanswered prompts at 300s, and the answering was handed to a **background agent running on Opus** (`model: opus`, per user instruction — not Sonnet 5) that drove run #2 to completion by itself. (2) The "phantom monitor" pattern recurred and the reliable fix remains a single FOREGROUND blocking bash poll-loop, re-run each time it returns. (3) User preference recorded: when a classifier outage blocks an action in auto mode, they can approve it via `/permissions`.

## 2026-07-16 (late) — tasks #11 + #17 implemented and validated for no-regression (UNCOMMITTED on `autoqa`)

Did tasks #11 and #17 (user picked this pair over the design-heavy #12). Both are in `agent/flow-runner.ts`, `tsc --noEmit` clean, and **NOT yet committed** (working-tree changes on `autoqa`; commit/push next session unless you'd rather review first).

- **#11 — a failed milestone no longer abandons the rest of the flow.** The old `if (verdict==='fail') break;` (which silently dropped every remaining milestone) is replaced with: (a) if it was the FINAL milestone, just finish; (b) otherwise call new `tryRecoverAfterBreak()` — reposition to the next milestone's expected start (already-there, or replay prior recipes) and CONTINUE only if the next milestone's own guardPhases can be CONFIRMED (never guesses); (c) if it can't confirm, record the remaining milestones as explicit `skippedStep`s (verdict `needs-review`, reason "skipped — upstream break at m{N}", empty signals so the Slack filter ignores them) instead of dropping them. Also closed the **exception-path** silent-drop (the-internet.herokuapp.com variant): a `lastAttemptedIndex` tracker lets the outer catch record the crashing milestone as FAIL + the rest as skipped, instead of vanishing. New helpers: `emptySignals`, `skippedStep`, `tryRecoverAfterBreak`; `SignalBundle` added to the type import.
- **#17 — login-shaped-milestone false-PASS ("m4" class) fixed.** Added `loginFailureDowngrade` right after the existing `explorerFailureDowngrade`: when a login-shaped milestone (`explored === null`, routed through `ensureAuthenticated`) did NOT confirm auth this run (`loginShaped && !replayOk`) yet the deterministic layer recorded `pass`, downgrade it to `needs-review` (never upgrade). Mirrors the proven explorer-failure pattern. Catches the repeatedly-deferred false pass (lambdatest account-login-gate:m2, expandtesting user-auth-api:m3, webdriveruniversity login-portal-auth:m1/m2, koyal google-signup class).

**Validation — honest scope (fresh exhaustive saucedemo run, state wiped: 23 PASS / 6 FAIL / 6 NEEDS-REVIEW, 8 flows, 141 LLM calls):**
- ✅ Compiles, runs, no crash, **no regression** — happy-path milestones that completed passed; successful logins passed (confirming #17 did NOT wrongly downgrade a real login — the actual risk of that change).
- ✅ #11's new fail-branch **executed live**: `✗ login-invalid-credentials broken at m2 (final milestone) — flow done` — the final-milestone case fired and correctly recorded no spurious skips.
- ⚠️ **Neither HEADLINE branch was live-triggered this run**: no mid-flow hard-fail-with-downstream-milestones (#11's recover/skip path) and no *silently*-failing login-shaped milestone (#17's downgrade). saucedemo doesn't naturally produce either. So those two specific branches are validated by code-review + pattern-mirroring, NOT by a live trigger yet. **To fully close: a targeted koyal `test` run** — koyal reliably produces both (S3 bug fails a mid-flow milestone with downstream steps; login-shaped milestones can fail silently). Not yet done (koyal runs are slow).
- Run character: the 5 probe FAILs are the usual checkout back/forward nav-state-loss class (matches all historical saucedemo runs); the 6 NEEDS-REVIEW are all my own honest "skip" answers on checkout-info-fill milestones where the explorer exhausted its 8-step budget (driven partly by a "trusted click never reached the page" click-loss quirk this run) — i.e. the low-pass/high-needs-review is conservative answering in a validation run, not degradation. Raw counts not directly comparable to history (non-deterministic flow proposal; this run got 8 flows vs up to 11 historically). saucedemo history for reference: 43/12/9 (07-07), 72/8/4 & 51/8/4 (07-09), 27/3/4 (07-13), 25/2/0 (07-15).

## Known rough edges / next steps
- **Hidden-in-a-menu Logout — LARGELY FIXED 2026-07-16 (`8d99b06`), verify on OrangeHRM next time.** The ask-once mechanism now supports a two-step "opener > logout" click (`learnedLogoutMenuOpener` in sitemap.ts + flow-runner.ts), and the crawler auto-discovers a hidden Logout during normal click-probing (crawler.ts + exported guard.LOGOUT_RE). Built and validated on beta.koyal.ai ("Shresth > Logout"). Not yet re-run against OrangeHRM (its collapsed user-avatar dropdown is the original case) — confirm the same fix covers it. Residual: auto-discovery only fires if the crawler happens to click the menu-opener during probing; if it doesn't, it falls back to the ask-once prompt (still works, just not automatic).
- **Initial login has no retry-once on a transient failure (real gap, found 2026-07-16 on koyal).** `commands/explore.ts` wraps the pre-crawl `ensureAuthenticated()` in a try/catch that logs a warning and continues UNAUTHENTICATED — so a single transient error on the very first LLM call (observed: an Anthropic 529 "overloaded") kills login for the whole explore, collapsing the crawl to the public login page and starving flow proposal. The LLM client retries individual calls, but `ensureAuthenticated` itself is not retried at the explore level. Fix: retry the initial `ensureAuthenticated()` once (or a couple times with backoff) before giving up and continuing unauthenticated. Directly caused koyal run #1's 7/5/2 vs run #2's 54/14/6.
- demoqa's `widgets-datepicker-tabs:m2` milestone fails and hasn't been root-caused (found during the 2026-07-09 afternoon regression, not investigated further).
- Draft contention on Koyal still causes recovery churn on repeated runs, INCLUDING between a human's manual investigation session and an automated run against the same account (drafts are account-scoped, not session-scoped) — fresh-project-per-run strategy is still the fix, not yet built.
- ParaBank's own backend has genuine, confirmed intermittent concurrency/flakiness on login — same exact credentials succeed most of the time, fail occasionally even in single-session isolation. Not fixable client-side; the retry + non-fatal-continue handling (2026-07-09 afternoon) is the appropriate mitigation.
- practice.expandtesting.com's Register button is genuinely, repeatedly obstructed by a shifting ad iframe — a real site accessibility issue, not fixable from autoqa's side beyond what the explorer already tries (ad-dismiss attempts, ref re-resolution).
- `AgentBrowser.recycle()`'s specific-PID resolution can itself time out when the daemon is badly wedged, falling through to the broad (all-sessions) kill in exactly the worst-case scenario D7 was meant to avoid. Not yet hit with real collateral damage, but not yet fixed either.
- Junk URL pollution: SPAs serve content for any path (a `/company/sauce-labs` footer link became a phantom page) — consider soft-404 detection.
- Statement KBs are young — user should `review` and classify with real judgment (watcher heuristics are stand-ins).
- automationintesting.online: a fast-forward-reached variant of the silent-admin-auth-skip false-PASS risk remains open — two near-duplicate admin-login flows fast-forwarded past their own login milestones because an EARLIER flow in the same run had already authenticated the session, a different code path than bstackdemo's fast-forward fix and not covered by it.
- automationintesting.online: the explorer's blank-page recovery (2026-07-10 evening) doesn't help if the page is ALREADY blank at the very first snapshot of a fresh `achieveGoal()` call — `lastRealUrl` initializes to `about:blank` itself in that case.
- globalsqa.com: `isLoginShapedGoal`'s quoted-label fix (2026-07-10 evening) only strips QUOTED spans — an unquoted variant of the same phrasing ("Click the Bank Manager Login option...") still routes through the auth module. Harmless on this ungated site; could misfire on a site with a real gate.
- bstackdemo.com: `isLoginShapedGoal` routes "sign in" milestones through `ensureAuthenticated()`, which no-ops on any public-catalog-with-gated-features site (browsable while logged out) — proven fixable (manually automated the real login end-to-end), deliberately left unfixed since the correct fix touches `auth.ts`, a path cross-validated across many other sites, without time to re-validate that breadth in the same session.
- Verification never consults the explorer's own "I gave up" (`fail`) signal — `recordVerifiedStep`'s verdict can still PASS a milestone the explorer explicitly failed, by absence of negative signal alone. Real, generalizable false-PASS gap (found on bstackdemo.com); deferred because the report is written inside the verify call (no clean post-hoc override) and it touches `verification.ts` core with all-site blast radius.
- globalsqa.com's crawl seed (bare origin, needed generically since some apps hide behind a login URL with no redirect) pulls in an unrelated marketing blog sharing the same domain as the banking app, diluting crawl budget. A path-prefix-scoping fix was investigated and REJECTED — it would break the legitimate case of a deep-linked entry into a larger single-purpose app. Logged as a site characteristic to live with, not a bug to fix.
- Host machine had only ~7.3GB free of 228GB at one point during the 2026-07-10 evening session — a tight margin that plausibly contributes to instability independent of memory pressure. ~826MB of old installer DMGs in `~/Downloads` identified as safe, easy reclaimable space; not cleared without explicit user go-ahead. Free space dropped further to ~5.2GB by 2026-07-11 — trending the wrong way, worth revisiting.
- `AgentBrowser.recycle()`'s broad-kill fallback (when session-specific PID resolution fails/times out) is no longer theoretical — confirmed live on 2026-07-11: one agent's own crash-recovery recycle() killed a DIFFERENT, concurrently-running agent's browser daemon in a separate worktree. That agent self-healed via its own recycle mechanism, so no data was lost this time, but the underlying gap (broad kill collaterally hits unrelated concurrent sessions) is now reproduced, not just predicted.
- ecommerce-playground.lambdatest.io: `account-login-gate:m2` shows a false PASS — login-shaped milestones route through `ensureAuthenticated()`, whose failure is only logged, never fed into the same explorer-failure-downgrade mechanism (2026-07-11) that now covers the direct-explorer path. One-line fix direction: the login-shaped branch's failure needs to feed the same needs-review downgrade, but doing this without a fresh full re-run to live-verify risks an unverified regression on the session-restore/transient-auth surface — deliberately deferred.
- `normalizePath`/`matchPage` has now been broken by all 3 major "how a URL encodes page identity" schemes on 3 different real sites (hash-routing on globalsqa, query-string routing on lambdatest's OpenCart, trailing-slash inconsistency on the-internet.herokuapp.com) — the single highest-value bug source in this project's history. If a NEW site collapses its crawl to way fewer pages than expected, check this function and `matchPage`'s URL-pattern-only (never content-verified) matching FIRST, before assuming it's something else. **Update 2026-07-13**: a 4th failure mode hit on testpages.eviltester.com (a legitimate `urlIncludes` fragment matching every deeper sibling URL via substring inclusion, fixed) — this function keeps finding new ways to break; treat any future new-site crawl collapse the same way.
- **Auth-gate detection (`looksLikeAuthGate`/`hasVisiblePasswordInput`/the various mid-flow auth-wall checks) is the SECOND-highest-value recurring bug source, and unlike `normalizePath` it still doesn't look converged** — patched in nearly every batch (ParaBank's missing-accessible-name password field → expandtesting's demo-page-name false positive → automationintesting's unconditional-Logout-label trust → lambdatest's cross-flow contamination → webdriveruniversity's heading-count false positive AND a second, cruder duplicate bypass elsewhere in the same file, both 2026-07-13) with a new failure mode nearly every time. If a future site's flow keeps re-authenticating unnecessarily, or silently skips a real login milestone, suspect this area first — and consider whether it needs a structural rework rather than another one-off patch.
- `AgentBrowser.recycle()`'s broad-kill fallback has now fired for real in 4 confirmed live occurrences (batch 2 the-internet→GreenKart, batch 3 webdriveruniversity→eviltester, and 2026-07-13 night's koyal exhaustive re-run) — the last one had no collateral damage only because saucedemo had already finished by the time it fired, not because the underlying gap closed. Given a real, cheap-to-write repro now exists 4 times over, this is a strong candidate to actually fix next rather than defer again.
- Verification doesn't respect a legitimate explorer skip (2026-07-13 night, koyal exhaustive re-run): when `achieveGoal()` correctly recognizes an edit milestone is already done and calls `done` without typing the injected marker (idempotency working as designed), the deterministic verification layer still checks for that marker and fails the milestone for not finding text that was never supposed to appear. Live-reproduced twice in the same run (`walked-projects-list-create-your-next-video:m5`, `walked-characters-list-new-character:m2`). Distinct from the marker-survives-navigation gap (that one's about a marker typed then navigated away from; this one's about a marker never typed at all because skipping was correct). Diagnosed, not fixed — flagged for whoever picks this up next.
- Koyal's `/upload` script path now has a required "Select Your Plan" modal (Standard/Pro radio, disabled Continue until one is picked) blocking progress right after uploading — not documented in any prior session's notes, possibly new UI added since the last deep validation (2026-07-09). Milestones authored before this modal existed don't budget step-count for it, contributing to several "exceeded max exploration steps" needs-review verdicts on 2026-07-13 night. Worth checking if this is now a permanent fixture of the flow.
- The original "Start with Audio not clickable" finding (2026-07-13, first shallow koyal run) did NOT reproduce in the same night's exhaustive re-run — the same option-matrix probe reported both Script and Audio as selectable. Likely a flaky, transient repro rather than a persistent bug; downgrade confidence in that finding accordingly if it resurfaces without fresh live confirmation.
- Native `prompt()` dialogs that require a SPECIFIC typed value (not just accept/dismiss) are only partially supported (2026-07-13, webdriveruniversity.com): `resolveBlockingDialog` resolves the block correctly but always accepts with an empty value, so a milestone whose verification depends on the exact typed text appearing would false-fail. Disclosed, not fixed — needs the pending dialog's message surfaced to the LLM's own decision loop, a larger change than a one-off patch.
- A further, unconfirmed-mechanism suspected recurrence of the file-based Q&A answer-file race (2026-07-13, testpages.eviltester.com): 3 consecutive classify answers were each consumed by the WRONG (next) question, corrupting 2 KB entries (hand-corrected). The existing stale-answer-clearing mitigation did NOT prevent it. Candidate hypothesis (not live-instrumented/confirmed): the shared `answer.txt` filename has no per-question nonce, so a slow answerer can have a reply misattributed to the following question. Left unfixed deliberately — the shared-filename convention is relied on by this project's own multi-session operating instructions, and a protocol change needs its own dedicated validation pass.
- A stale-recipe operational note (2026-07-13, webdriveruniversity.com): recipes recorded for To-Do-List Enter-submission milestones *before* the new `press` action existed kept "passing" replay with an incomplete (fill-only, never-submitted) action sequence until manually cleared. After adding any new explorer action type, a fresh explore or targeted recipe reset for affected sites is worth doing — older recipes can't self-heal without a fresh success.
- No true cross-batch regression suite exists yet (2026-07-13 gap analysis, see above) — every fix to date has been live-verified only against the one site that found it, plus one static code-review after batch 1. The original 8 validation sites (saucedemo, koyal, automationexercise, demoqa, orangehrm, parabank, uitestingplayground, expandtesting) haven't been executed since 2026-07-09, despite ~25 fix commits landing in the shared crawl/auth/click/verdict code since then. **Update 2026-07-15**: this is now actually underway — 10 of 15 sites touched (see that date's section above) — but 3 of those (GreenKart, webdriveruniversity, koyal) were intentionally stopped mid-run and eviltester + filmarena.ai remain, so still not fully closed.
- **`flow-runner.ts`'s milestone loop aborts the entire rest of a flow on any plain FAIL verdict, silently** (2026-07-15, confirmed by reading the code, ~line 940-943: `if (step.result.verdict === 'fail') { break; }`) — not an edge case, the default behavior for any failed milestone for any reason. Worse for an uncaught exception (found same day on the-internet.herokuapp.com): zero record at all for milestones dropped after a crash, if earlier ones already passed. Planned fix discussed at length with the user, not yet implemented: don't break, let the existing guard-phase/replayUpTo/fast-forward repositioning logic (which already runs at the top of every loop iteration) get a chance to recover position for N+1; if it can't, record an explicit "skipped — untested, upstream break" entry instead of silently dropping or blindly running from a bad position. See 2026-07-15's own section for the full design discussion, including a related-but-distinct LLM-overachievement sub-problem and a planned structured per-milestone anomaly tag.
- **Screenshot/vision is not currently fed into verification at all** — the oracle is a11y-tree/DOM/console/network-only, structurally blind to visual-only bugs (invisible text, layout breakage, mis-styled controls). Discussed at length 2026-07-15 as the next real architecture addition: systematic (every milestone) for bug-detection, selective (only when stuck/retrying) for click-occlusion, and — non-negotiable — never allowed to autonomously flip a deterministic verdict, only feed the existing human-review path. Not yet designed in code.
- **Auth-gate detection found a further, still-different sub-case** (2026-07-15, automationintesting.online): `isLoginShapedGoal` matched the bare word "login" inside a verify-kind goal referencing a completed state ("...displayed **after login**"), not an instruction to authenticate — misrouting a verification milestone into the auth-delegation branch and silently deleting its own success check. Yet another new failure mode for the project's still-unconverged #2 recurring bug source.
- **The "login-shaped-milestone failures aren't fed into the verdict-downgrade mechanism" false-PASS gap (the "m4" class, task #17) — FIX IMPLEMENTED 2026-07-16 (uncommitted).** Added `loginFailureDowngrade` in flow-runner.ts (see the "2026-07-16 (late)" section above). Validated for no-regression on saucedemo (successful logins not wrongly downgraded); the actual downgrade-on-silent-failure branch is NOT yet live-triggered (needs a koyal `test` run). Reproduced historically on lambdatest account-login-gate:m2, expandtesting user-auth-api:m3, webdriveruniversity login-portal-auth:m1/m2, koyal google-signup.
- **The flow-runner `break`-on-fail that silently dropped all downstream milestones (task #11) — FIX IMPLEMENTED 2026-07-16 (uncommitted).** Now recovers position + continues, or records remaining milestones as explicit skips; also closed the exception-path silent-drop. See the "2026-07-16 (late)" section. Live-validated: no-regression + the final-milestone branch fired; the mid-flow recover/skip branch not yet live-triggered (needs koyal).
- **`guard.ts`'s `LOGOUT_RE` has no override path at all** (2026-07-15, automationintesting.online) — unlike every other destructive-action keyword, a Logout click is auto-denied unconditionally, not even via the `always`/`never` allowlist. Makes any flow milestone whose explicit goal is "click Logout" structurally unpassable, permanently, on any site. Distinct from the already-documented OrangeHRM collapsed-dropdown Logout gap (that one's about finding the control; this one's about the control being unconditionally denied even once found).
- **`Explorer.achieveGoal()` has no off-origin guard during milestone execution or flow replay** (2026-07-15, globalsqa.com + confirmed with a sharper root cause on GreenKart same day), unlike the crawler/deep-walker's existing preventive guards. globalsqa: an ad interstitial led a click off-site twice in one run before the explorer reactively (not preventively) caught it. GreenKart: a "JOIN NOW" subscription button drove a real 3-hop redirect all the way to a live Teachable checkout page (real order ID) with **zero guard prompt raised at all** — two compounding causes, precisely diagnosed: `guard.ts`'s destructive-keyword floor (`delete|pay|checkout|invite|revoke|logout`) doesn't cover "join"/"enroll"/"subscribe", AND the off-origin guard that exists only lives in `crawler.ts`/`deep-walker.ts`'s exploration-time edge mapping — a code path that never runs when replaying/testing an already-approved flow. **ACTION TAKEN**: `subscription-plan-selection` de-approved from GreenKart's flow set per the discovering agent's own recommendation (also review `career-accelerator-application`, same scope problem, lower severity). Not yet fixed at the code level — this is a real gap that could recur on any site with a payment/subscription-adjacent flow, not just GreenKart. A related, compounding mistake worth remembering: the deterministic layer correctly computed FAIL for this exact milestone, escalated it to a human as an ambiguous verdict, and the human (a delegated agent) answered "pass" — demonstrating that human-escalation is not automatically safer than the deterministic signal it's overriding, especially for off-origin/safety-relevant judgment calls.
- **The statement-KB candidate extractor flags every item on an enumerated/listing page as its own classify question, one at a time** — confirmed on 3 sites in one session (2026-07-15: GreenKart's ~25-item grocery catalog, lambdatest's product listings, webdriveruniversity's ~30-widget AI Testing Playground). Always correctly classified as noise so far, but a real efficiency problem — long, repetitive human-in-the-loop backlogs for zero verdict value. Worth batching same-shaped repeated candidates instead of one round-trip per item.
- **A recurring delegated-agent operational failure, the "phantom monitor"** (2026-07-15, hit ≥5 times across 4 agents this session, and previously flagged 2026-07-14 night hitting 3 of 6 agents): an agent driving a long-running background process + file-based Q&A loop stops mid-run believing an external notification/monitor will wake it up. None exists. "Please poll yourself" doesn't reliably fix it once it's happened; handing the agent a concrete blocking shell loop (`timeout N bash -c 'while ...; sleep M; done'`) to run as its own tool call works better — it returns a real result to react to instead of leaving a turn-boundary judgment call the agent keeps getting wrong. Brief this up front in any future session delegating this shape of task, and periodically verify agent-reported state directly rather than trusting self-reports.
- **lambdatest's AJAX "Checkout" control shows real click-loss** (2026-07-15) — 3 independent encounters in one run where the CLI reported a click as issued but the page never navigated. Matches the project's long-documented "trusted click never reached the page" quirk closely enough to be worth checking whether that fallback's coverage extends to this specific control.
- **webdriveruniversity's Login Portal page has an unresolved CDP-automatability question** (2026-07-15): a bare `onclick="validate()"` submit handler with no `preventDefault`/form action, where across ~3 automated attempts no native-dialog-blocked log line ever appeared (no real alert detected) yet fields kept resetting and the URL briefly showed a trailing `?` — the explorer declared success without clear supporting log evidence. Needs a human to check by hand whether this specific interaction is actually automatable via CDP the way the code assumes.
- **Koyal's draft-per-account contention produced a concrete, costly live repro** (2026-07-15): a fresh regression run's `/upload` resumed an existing draft (from prior, unrelated account activity) that already had a script PDF attached; the explorer didn't recognize this and retried an "upload" against the wizard sidebar's own nav breadcrumb (not a real dropzone), re-asking the same CLI question 5 times over 20+ minutes before a human directly intervened. The already-flagged "fresh-project-per-run entry strategy" fix would directly prevent this exact failure mode, not just reduce recovery churn.
- **The 2026-07-14-night idempotent-skip verification fix has never been validated live against Koyal** — it was diagnosed FROM two Koyal milestones, shipped and code-reviewed the same night, but Koyal's backend went down immediately after and stayed down until 2026-07-15, and that day's koyal run never even reached the test/flow-running phase before being killed (see below). Still genuinely unconfirmed against the site it was built for; worth prioritizing on the next koyal resume.
- **ACTION NEEDED before any koyal resume**: 2026-07-15's koyal run wrote a poisoned flow permanently into `.autoqa-state/beta.koyal.ai/sitemap.json` — `walked-projects-list-create-yournext-video`'s milestones m3-m8 now instruct Sign Up + "Verify OTP," artifacts of a session-loss cascade, not real product steps. Manually clean these up or do a fresh sitemap reset before resuming; approving/testing this flow as-is will guarantee noise or failure.
- **Koyal's own wizard confirms the project's #1 recurring bug source live**: the `wizard-upload-file` page-classifier landmark (`snapshotAnyOf: ['Upload file', 'Story Type', 'Review transcript', 'Edit scenes']`) matches the wizard's persistent sidebar — visible on every step, not just upload — so it can't distinguish "I'm on the upload step" from "I'm anywhere in this wizard." Drove ~12 doomed upload attempts in one 2026-07-15 run. Same "chrome landmarks must never identify a state" disease documented since this project's earliest sessions, now with a live Koyal-specific repro.
- **Blank-page (`about:blank`) recovery doesn't verify it landed on the requested *authenticated* page** (2026-07-15, koyal) — both the deep-walker's and `core/explorer.ts`'s own recovery paths just reopen a URL and move on; twice in one run this landed on `/login` instead of the intended authenticated page mid-walk, and since the existing auth-wall check only fires at flow entry (confirmed working correctly there), not mid-walk, the explorer got driven into an unwinnable fake-signup + unguessable-OTP loop each time before the walk-level "no progress after 3 attempts" bound caught it. Fixing the recovery to verify landing state (not just that *a* page loaded) would close this without touching the entry-level check at all. (Queued as task #14.)
- **SHIPPED 2026-07-16 (uncommitted, working tree): walk choice-memory + flow-proposal mode enumeration** — two fixes for the "only tests the default mode, never the alternatives" gap found on filmarena.ai. (1) `agent/deep-walker.ts` gained `DeepWalkerDeps.triedChoicesByPage` (a `Map<pageId, Set<label>>` owned by `crawler.ts`, shared across every `deepWalk()` in one crawl); `advanceGoal()` now takes `triedChoices` and, in exhaustive mode, tells the LLM which radio/tab options were already selected so it prefers an untried one; radio/tab clicks' `resolvedLabel` get recorded after each `achieveGoal()`. (2) `agent/page-classifier.ts`'s `proposeFlows` prompt gained a "DISTINCT MODE/VARIANT COVERAGE" paragraph: detect clusters of mutually-exclusive alternative workflows and propose a separate flow per alternative, explicitly NOT multiplying by parameters (aspect/duration/filters use defaults). Live-validated on a fresh exhaustive filmarena.ai run (proposer emitted separate Best/Battle/Custom+Top3 × Image/Video/VideoEdit flows vs. the earlier Best-only). Honest caveat: prompt-level nudge, not a hard guarantee — biases toward diversity, doesn't enforce it. `tsc --noEmit` clean. NOT yet committed and NOT yet validated on other sites (saucedemo/koyal/etc.) — a light cross-site regression check is warranted since `deep-walker.ts`/`crawler.ts`/`page-classifier.ts` are all-site shared code.
- **When driving a run directly (terminal process + Claude as human-in-the-loop, no delegated subagent), the approve-flows and login prompts are latency-critical** (2026-07-16): an unanswered "Approve flows" prompt timed out during a filmarena.ai run and wasted the entire run at 0/0/0 (exploration saved, zero flows tested). Watch for these specific prompt types the moment a run reaches proposal/auth. Relatedly, do NOT write a fast classify-clearing loop that blanket-answers `n` by matching the prompt TYPE — match the quoted CONTENT and STOP for anything error/outcome-shaped (`error|not found|failed|invalid|success|complete|denied|…`); a loop that noised everything wrongly classified `"User not found. Please sign up first."` (a real login-failure message) as noise. Same watcher-scoping lesson as always; corrected by deleting the poisoned KB entry so it re-surfaces for real judgment.
- **Slack bug-reporting integration requested** (2026-07-16, task #16): push real findings to a Slack channel (webhook or bot token, from env/secrets — never hardcoded) rather than only writing per-run `report.md`. Design (trigger scope, format, dedup) to confirm with user before building; likely a new reporter module alongside the existing markdown-report writer.

## 2026-07-17 — external #11/#17 live validation on a 10-flow koyal deep-run finds a new HIGH bug (task #15, `parseJsonFromLlm` crash, NOT yet fixed), tasks.md tracker introduced

Two separate jobs picked up from CLAUDE.md's 2026-07-16 end-state (tasks #11/#17 implemented, uncommitted) and worked in parallel without initially knowing about each other — see the operational lesson at the end.

**#11 — VALIDATED LIVE, both branches, no report-loss.** A clean-slate, unsupervised, 10-flow koyal deep-walk (generous budget, no manual steering) triggered the recover branch 2× (a mid-flow deterministic FAIL repositioned to a CONFIRMED guard-phase and continued, instead of the old silent `break` dropping everything after it) and the exception path 4× (see the crash below) — all 4 crashes were recorded as fails (3 as milestone crash-fails, 1 as a synthetic entry-fail), none vanished. The whole "silent report-loss" class task #11 targeted is closed.

**#17 — could not fire on this run.** A clean-auth koyal run proposes no login/signup/OTP milestone, so `loginFailureDowngrade` has nothing to act on. Still only validated by code-review + saucedemo no-regression — needs a login-gated site (lambdatest/expandtesting/webdriveruniversity) for real live validation. See `tasks.md`.

**New HIGH bug found — task #15, NOT YET FIXED: `parseJsonFromLlm` crashes the whole flow on a messy LLM reply.** Every explorer step asks the LLM "what should I click next?" and expects one small JSON reply (`{"action":"click","ref":"e5"}`); the parser's fallback grabs "everything from the first `{` to the last `}`". When the LLM occasionally returns **two** JSON objects back-to-back (or JSON + trailing prose/markdown), that grab glues them into one invalid blob — `JSON.parse` throws `"Unexpected non-whitespace character after JSON at position N"`, **uncaught**, which kills the *entire flow*, not just that one step. Hit **4 of 10 flows** this run (Create-YourNext-Video:m5, CREATE-OUTFIT:m10, the Audio-wizard entry, Create-a-new-character:m3). Root cause of why it never showed up before: it's a rare per-call dice roll (the LLM usually returns clean JSON) — this run simply made far more LLM calls than any prior one (10 flows, deep wizards, generation waits, up to 8 steps/milestone + probes = hundreds of "what next?" calls), so it was near-certain to hit the rare bad reply; complex prompts (bullet-point character descriptions, long concatenated labels) also nudge the LLM toward chattier multi-part replies. **Not a Koyal bug, not a new regression** — a latent fragility in reading the LLM's reply, exposed by a much bigger/deeper run than any before it. Proposed fix (two parts, **NOT implemented**, awaiting go-ahead):
1. **Reduce bad parses**: extract the first *complete, brace-balanced* JSON object (count braces, stop at the matching close) instead of first-`{`-to-last-`}`; wrap the fallback in try/catch so a bad parse returns a typed error, not an uncaught throw.
2. **Contain the blast radius (the important half)**: on an unparseable reply in `decideNextAction`, retry the LLM call once (it almost always returns clean JSON the 2nd time); if it still fails, treat it as one failed/needs-review step — never crash the flow.
Part 2 alone would have turned all 4 aborts into at-most-one-retried-step (likely 0 lost flows this run).

**Official tally: 58 PASS / 22 FAIL / 10 NEEDS-REVIEW, 0 Koyal product bugs, 0 #11/#17 regressions.** The validating session crashed mid-analysis before delivering the exact row-by-row breakdown (its own quick `awk` extraction only caught 4 of 32 non-pass rows — real table cells embed `|` characters from network-log dumps, breaking naive field-splitting on `|`). Re-derived precisely by reading its `.jsonl` transcript directly plus re-parsing `autoqa/reports/beta.koyal.ai/2026-07-17T08-18-16-412Z/report.md` with a proper extractor (split on `\n| `, regex-anchor the `| VERDICT | SEVERITY |` pair rather than counting `|`-delimited fields):
- **16 FAIL** — nav-state-loss probe collateral (10× back-forward, 4× abandon-resume, 2× matrix-start-method) — the known, by-design, first-class-detected SPA-history quirk.
- **4 FAIL** — the JSON-parse crash above.
- **2 FAIL** — flow-authoring/grounding gaps, not new: `walked-projects-list-create-yournext-video:m4` wants to fill a "character examples" textarea that isn't reachable from any screen the explorer's 9 navigation attempts actually landed on (likely a step nested deeper in character-creation, not on the Upload-File/Story-Type screens tried); `video-wizard-script-mode:m5` burned its whole 10-step budget navigating Story-Type→Concept-Driven→Next and never reached the transcript editor it needed to type into.
- **10 NEEDS-REVIEW** — legitimate step-budget exhaustion / "explorer correctly declined to guess" cases deep in Koyal's wizards (e.g. `"Exceeded max exploration steps (8)"`, `"Explorer stuck repeating 'wait||' — aborting"`), correctly downgraded rather than false-passed or false-failed.

Also surfaced this run (not new, not fixed): a "Report a Bug" widget traps the explorer inside wizards, blocking both video wizards from reaching scene-generation; draft contention blocked clean Audio-path coverage; the inline-processing regex misses Koyal's "Processing" *button-state* text so the explorer can't wait out multi-minute generations. **S3 scene-gen bug: 0 sightings this run** — exploration never reached the Edit-scenes step (bug-widget trap + 8-step budget), this is NOT evidence the backend bug is fixed. Positives confirming Koyal itself works: AI avatar generation (variants generated), asset regeneration ("started successfully!"), clean login, 10-page crawl, 10-flow proposal including a distinct Audio flow (confirms the 2026-07-16 mode-coverage fix generalizes), script wizard reached Theme→Style.

**Operational note: the validating session crashed mid-analysis** (its job's state showed `"source session not found"` on resume) right after getting explicit go-ahead to commit, pushing `c13c7e4`, and explaining the JSON-crash root cause to the user in full — but before answering the user's immediate follow-up ("so what exactly are the 22 fails and 10 needs-review"). Recovered cleanly by another job reading the dead session's own `.jsonl` transcript file directly (`~/.claude/projects/-Users-apple-Desktop-happyflow/<sessionId>.jsonl`) rather than losing the analysis — the transcript survives even when the interactive session doesn't.

**A second, independent job did a lighter earlier checkpoint the same day, before discovering the above run in progress.** It re-validated the SAME uncommitted 5-file diff (`crawler.ts`, `flow-runner.ts`, `page-classifier.ts`, `probes.ts`, `sitemap.ts` — duplicate-successHint dedup, soft-404/SPA-catchall filtering, probe-breaks-position repositioning, login-shaped-milestone handling, Koyal draft "fresh entry" prompt) with fresh-wipe runs on both original benchmark sites: **saucedemo 47 PASS/3 FAIL/1 NEEDS-REVIEW** (up from the documented 43/12/9 baseline — checkout now completes fully end to end), **koyal 28 PASS/13 FAIL/6 NEEDS-REVIEW** on a shallower (non-10-flow) pass. Every non-pass was either the known probe quirk or a false-fail from an imprecise auto-generated milestone goal (e.g. expecting a "Username" field saucedemo's checkout form doesn't have); critically, **no probe failure cascaded into a later milestone failing**, confirming the repositioning fix works. Two reusable operational lessons from this pass:
1. A watcher that blanket-denies every destructive-guard prompt (including saucedemo's own Checkout) starves a walk of ever reaching checkout at all — the correct policy is `always` for a demo shop's own core, non-destructive flow actions, not a blanket `no`.
2. **Two jobs validating the same site's state concurrently race on `.autoqa-state/<host>/*.json`** (plain last-save-wins file writes, no locking) — this produces confusing, hard-to-explain data fluctuations (pages/flows/walks appearing to shrink then regrow) that look exactly like corruption but are actually just a race between two writers. Before trusting an odd read of shared per-site state, check `ps aux` / job state for a live competing process on the same hostname first.

**`tasks.md` introduced as the new persistent, cross-session task tracker** (`/Users/apple/Desktop/happyflow/tasks.md`) — the built-in Claude Code task tool (TaskCreate/TaskList) is scoped to one session and invisible across jobs, which is exactly how this session's own "let me record this in the tracker (durable) before I wrap up" got lost when the job crashed. Compounding that, this doc's own ad-hoc "task #N" prose numbering has already collided twice independently (#14 was assigned to both "blank-page recovery doesn't verify authenticated landing" (2026-07-15/16) and, today, "koyal upload-recognition loop is slow" — and #16 to both the (already-shipped) Slack integration and, today, "live-validate #17"). `tasks.md` renumbers around these collisions and is now the single source of truth for open work — check it first at the start of any session, update it directly rather than adding another ad-hoc "task #N" mention in this file.
